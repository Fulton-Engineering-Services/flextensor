{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Overview","text":""},{"location":"#flextensor","title":"FlexTensor","text":"<p>FlexTensor is a tensor offloading library for PyTorch that enables running large models on limited GPU memory by intelligently offloading tensors between GPU and CPU memory.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Automatic Model Patching: Offload model layers without modifying model code</li> <li>Manual Control: Fine-grained control with <code>offload_block</code> context managers</li> <li>Smart Profiling: Automatic warmup and profiling for optimal performance</li> <li>Wildcard Support: Use patterns like <code>\"layers.*\"</code> to offload multiple modules</li> <li>Profile Persistence: Save and load offloading profiles for faster startup</li> <li>Lazy Model Initialization: Load models from saved profiles with optimized weight loading</li> <li>Shared Memory: Optional shared memory subsystem for cross-process tensor coordination</li> </ul>"},{"location":"#quick-example","title":"Quick Example","text":"<pre><code>import torch\nimport flextensor\nfrom flextensor import OffloadConfig\n\n# Load your model\nmodel = YourModel()\n\n# Configure offloading\nconfig = OffloadConfig(\n    gpu_device=0,\n    warmup_iters=1,\n    profile_iters=10,\n    module_patterns=[\"embed\", \"layers.*\", \"head\"],\n)\n\n# Patch model - no code changes needed\nmodel = flextensor.offload(model, config=config)\n\n# Use model normally\noutput = model(input_tensor)\n</code></pre>"},{"location":"#documentation","title":"Documentation","text":"<ul> <li> <p> Getting Started</p> <p>Install FlexTensor and get up and running in minutes</p> <p> Quick Start</p> </li> <li> <p> Configuration</p> <p>Understand all configuration options and how to tune them</p> <p> Configuration</p> </li> <li> <p> How-To Guides</p> <p>Solve specific problems with step-by-step instructions</p> <p> Troubleshooting</p> </li> <li> <p> API Reference</p> <p>Complete technical reference for all functions and classes</p> <p> API Reference</p> </li> </ul>"},{"location":"#performance","title":"Performance","text":"<p>FlexTensor targets less than 5% latency overhead compared to a baseline without offloading. See the Dashboard for benchmark results.</p>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#installation","title":"Installation","text":""},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.10 or higher</li> <li>PyTorch 2.5 or higher (with CUDA support)</li> <li>CUDA-capable GPU</li> </ul>"},{"location":"installation/#install-from-package","title":"Install from Package","text":"<p>FlexTensor is hosted on NVIDIA GitLab Artifactory. Use the following command to install:</p> <pre><code>pip install flextensor \\\n    --extra-index-url https://pypi.org/simple\n</code></pre>"},{"location":"installation/#install-from-source","title":"Install from Source","text":"<p>Clone the repository and install with:</p> <pre><code>git clone https://github.com/ai-dynamo/flextensor.git\ncd flex-tensor\npip install .\n</code></pre>"},{"location":"installation/#development-installation","title":"Development Installation","text":"<p>For development, install with all optional dependencies:</p> <pre><code>pip install -e \".[all]\"\n</code></pre> <p>This includes:</p> <ul> <li>Testing tools (pytest, pytest-cov)</li> <li>Pre-commit hooks</li> <li>Debugging tools (pudb)</li> <li>Documentation generation tools (mkdocs, mike)</li> </ul>"},{"location":"installation/#verify-installation","title":"Verify Installation","text":"<pre><code>import flextensor\nimport torch\n\nprint(f\"CUDA available: {torch.cuda.is_available()}\")\nprint(f\"FlexTensor version: {flextensor.__version__}\")\n</code></pre>"},{"location":"quick-start/","title":"Quick Start","text":""},{"location":"quick-start/#quick-start","title":"Quick Start","text":"<p>Get FlexTensor running in minutes. This guide covers installation, basic usage, and key concepts.</p>"},{"location":"quick-start/#installation","title":"Installation","text":"<p>Install FlexTensor using the NVIDIA GitLab Artifactory:</p> <pre><code>pip install flextensor \\\n    --extra-index-url https://pypi.org/simple\n</code></pre> <p>Verify CUDA is available:</p> <pre><code>import torch\nassert torch.cuda.is_available(), \"CUDA required\"\n</code></pre>"},{"location":"quick-start/#how-it-works","title":"How It Works","text":"<p>FlexTensor manages tensor movement automatically through a short learning phase (warmup and profile) before switching to optimized inference. You don't manage these phases directly \u2014 FlexTensor handles them during the first few iterations.</p> <p>For a deeper explanation of the warmup, profile, and inference states and the decisions made during each, see Internal States.</p>"},{"location":"quick-start/#basic-usage","title":"Basic Usage","text":"<pre><code>import flextensor\nfrom flextensor import OffloadConfig\n\n# Your existing model\nmodel = YourModel()\n\n# Configure offloading\nconfig = OffloadConfig(\n    gpu_device=0,              # GPU to use\n    warmup_iters=1,            # Iterations for tensor discovery\n    profile_iters=10,          # Iterations for timing measurement\n    module_patterns=[\"layers.*\"],  # Which modules to offload\n)\n\n# Patch the model\nmodel = flextensor.offload(model, config=config)\n\n# Use normally - first warmup_iters + profile_iters iterations are warmup/profile\nfor batch in dataloader:\n    output = model(batch)  # FlexTensor handles everything\n</code></pre> <p>Single-thread only</p> <p>FlexTensor is not thread-safe. All stages \u2014 offloading setup, warmup, profiling, and inference \u2014 must run on the same thread. Do not call <code>offload()</code>, run forward passes on a patched model, or access the offload manager from multiple threads in parallel. If you need per-thread offloading, create a separate named manager and model per thread.</p>"},{"location":"quick-start/#module-path-patterns","title":"Module Path Patterns","text":"<p>The <code>module_patterns</code> field in <code>OffloadConfig</code> specifies which modules to offload using path patterns:</p> Pattern Matches <code>\"layers.*\"</code> All modules under <code>model.layers</code> <code>\"encoder.block_*\"</code> <code>encoder.block_0</code>, <code>encoder.block_1</code>, etc. <code>\"attention.?\"</code> Single-character suffixes like <code>attention.q</code> <pre><code>config = OffloadConfig(\n    module_patterns=[\n        \"embed\",           # Exact match\n        \"layers.*\",        # Wildcard\n        \"head\",\n    ],\n)\nmodel = flextensor.offload(model, config=config)\n</code></pre> <p>Module patterns can also be set via the <code>FT_MODULE_PATTERNS</code> environment variable as a comma-separated list:</p> <pre><code>FT_MODULE_PATTERNS=\"layers.*,embed,head\" python my_script.py\n</code></pre>"},{"location":"quick-start/#key-configuration-options","title":"Key Configuration Options","text":"<p>The most commonly tuned options are:</p> <ul> <li><code>module_patterns</code> \u2014 which modules to offload (supports <code>*</code> and <code>?</code> wildcards, default <code>[\"*\"]</code>; use specific patterns such as <code>model.layers.*</code> for better per-layer pipelining)</li> <li><code>warmup_iters</code> \u2014 iterations for tensor discovery (default <code>1</code>)</li> <li><code>profile_iters</code> \u2014 iterations for timing measurement (default <code>10</code>)</li> </ul> <p>See Configuration for the full list of options and explanations.</p>"},{"location":"quick-start/#profile-caching","title":"Profile Caching","text":"<p>Skip warmup/profile on subsequent runs by saving and loading profiles:</p> <pre><code>om = flextensor.get_offload_manager()\n\n# First run: save profile after warmup completes\nconfig = OffloadConfig(\n    module_patterns=[\"layers.*\"],\n    profile_read_only=False,  # Allow saving profiles\n)\nmodel = om.offload(model, config=config)\nfor _ in range(config.warmup_iters + config.profile_iters):\n    model(sample_input)\nom.save_profile(\"/tmp/profiles/my_model\")\n\n# Later runs: load profile, skip warmup/profile\nom = flextensor.get_offload_manager()\nom.set_config(config)\nom.load_profile(\"/tmp/profiles/my_model\", model=model)\n</code></pre> <p>Profile read-only mode</p> <p>By default, <code>profile_read_only</code> is <code>False</code>, allowing both saving and loading of profiles. Set <code>profile_read_only=True</code> to prevent accidental overwrites in production.</p>"},{"location":"quick-start/#verify-its-working","title":"Verify It's Working","text":"<pre><code>usage = flextensor.get_gpu_memory_usage()\nprint(f\"GPU memory: {usage.total_mb:.1f} MB\")\n</code></pre>"},{"location":"quick-start/#next-steps","title":"Next Steps","text":"<ul> <li>Configuration -- All options explained</li> <li>Troubleshooting -- Debug issues</li> <li>Internal States -- How the state machine works</li> <li>Tensor Discovery -- How untraced tensors are found</li> </ul>"},{"location":"adr/","title":"Index","text":""},{"location":"adr/#architecture-decision-records-adrs","title":"Architecture Decision Records (ADRs)","text":"<p>This directory contains Architecture Decision Records (ADRs) for the FlexTensor project.</p>"},{"location":"adr/#what-is-an-adr","title":"What is an ADR?","text":"<p>An Architecture Decision Record (ADR) captures an important architectural decision made during the development of this project, along with its context and consequences. ADRs help communicate the reasoning behind technical choices to current and future team members.</p>"},{"location":"adr/#why-do-we-use-adrs","title":"Why do we use ADRs?","text":"<ul> <li>Document rationale: Capture the \"why\" behind architectural decisions, not just the \"what\"</li> <li>Historical context: Understand decisions made in the past and their constraints</li> <li>Communication: Share knowledge across team members and with future contributors</li> <li>Learning: Review past decisions to improve future ones</li> <li>Prevent revisiting: Avoid reopening settled discussions without new information</li> </ul>"},{"location":"adr/#adr-format","title":"ADR Format","text":"<p>We follow a lightweight format based on Michael Nygard's ADR template:</p> <ul> <li>Title: Short, descriptive name (e.g., \"Use Forward Patching for Module Offloading\")</li> <li>Date: When the decision was made</li> <li>Status: Proposed, Accepted, Deprecated, Superseded</li> <li>Context: What factors led to this decision? What constraints existed?</li> <li>Decision: What we decided to do and why</li> <li>Alternatives Considered: What other options were evaluated and why were they rejected?</li> <li>Consequences: What are the positive, negative, and neutral impacts?</li> </ul>"},{"location":"adr/#how-to-create-a-new-adr","title":"How to Create a New ADR","text":"<ol> <li>Copy <code>template.md</code> to a new file: <code>NNNN-short-title.md</code> (e.g., <code>0002-use-cuda-graphs.md</code>)</li> <li>Fill in all sections with relevant information</li> <li>Commit the ADR with your code changes or as a separate commit</li> <li>Update the index below</li> </ol>"},{"location":"adr/#adr-index","title":"ADR Index","text":"Number Title Status Date 0001 Forward Patching for Module Offloading Accepted 2026-01-23 0002 Configuration-based Module Patterns for Offloading Accepted 2026-01-23 0003 Custom Type Handlers for TensorProcessor Accepted 2026-02-16"},{"location":"adr/#adr-statuses","title":"ADR Statuses","text":"<ul> <li>Proposed: Under discussion, not yet decided</li> <li>Accepted: Decision has been made and implemented</li> <li>Deprecated: Still in effect but discouraged for new work</li> <li>Superseded: Replaced by a newer ADR (link to the replacement)</li> </ul>"},{"location":"adr/0001-forward-patching-for-module-offloading/","title":"0001 forward patching for module offloading","text":""},{"location":"adr/0001-forward-patching-for-module-offloading/#adr-0001-forward-patching-for-module-offloading","title":"ADR-0001: Forward Patching for Module Offloading","text":"<p>Date: 2026-01-23</p> <p>Status: Accepted</p>"},{"location":"adr/0001-forward-patching-for-module-offloading/#context","title":"Context","text":"<p>FlexTensor provides tensor offloading capabilities by intercepting module forward passes to wrap them with offload context managers. The initial implementation used an <code>OffloadModule</code> wrapper class that inherits from <code>nn.Module</code> and wraps target modules.</p> <p>However, this wrapper approach has several significant issues:</p> <ol> <li> <p>Hierarchy changes: When a module is wrapped, <code>model.layers[0]</code> returns an <code>OffloadModule</code> instance instead of the actual layer (e.g., <code>TransformerBlock</code>). This breaks code that navigates the model hierarchy expecting specific types.</p> </li> <li> <p>isinstance checks fail: Framework code that uses <code>isinstance(module, SpecificLayerType)</code> to identify layer types fails because the module is wrapped in <code>OffloadModule</code>, which is not a subclass of the original layer type. This is critical for vLLM integration, where the framework extensively checks layer types:</p> </li> <li>Pipeline parallelism checks: <code>vllm/model_executor/models/utils.py:255</code> - <code>isinstance(module, (StageMissingLayer, PPMissingLayer))</code></li> <li>MoE layer detection: <code>vllm/model_executor/models/qwen3_moe.py:679-683</code></li> <li> <p>Our tests demonstrate this works with patching: <code>test_forward_patching.py::test_patched_module_preserves_isinstance</code></p> </li> <li> <p>Complex attribute delegation: The wrapper requires custom <code>__getattr__</code> implementation to delegate attribute access to the wrapped module. This is error-prone and doesn't handle all edge cases:</p> </li> <li>Bug discovered with vLLM's <code>quant_method</code> attribute access - see deleted test file that documented this issue</li> <li> <p>PyTorch's <code>__getattr__</code> delegation must handle <code>_modules</code>, <code>_parameters</code>, and <code>_buffers</code> specially to avoid recursion</p> </li> <li> <p>Serialization complications: <code>state_dict()</code> and <code>torch.save()</code> require special handling because the wrapper adds an extra layer to the module hierarchy. With wrappers, <code>state_dict()</code> keys include the wrapper (<code>module.layer1.module.weight</code> instead of <code>module.layer1.weight</code>). Our tests verify patching preserves this: <code>test_forward_patching.py::test_patched_module_state_dict</code>.</p> </li> <li> <p>Debug complexity: Stack traces and model inspection tools show <code>OffloadModule</code> wrappers rather than the actual module types, making debugging harder.</p> </li> </ol> <p>The FlexTensor team explored forward patching in earlier experiments (note: the <code>experiments/</code> directory has since been removed), where forward patching was used successfully, but the production implementation initially chose wrappers for perceived simplicity.</p>"},{"location":"adr/0001-forward-patching-for-module-offloading/#decision","title":"Decision","text":"<p>We will replace the <code>OffloadModule</code> wrapper with direct forward method patching.</p> <p>Implementation approach:</p> <ol> <li>Patch the forward method: Replace <code>module.forward</code> with a method that wraps the original forward in an offload context manager</li> <li>Store original for restoration: Save the original forward as <code>module._ft_original_forward_func</code> (unbound class method) to enable cleanup</li> <li>Track patched modules: Maintain a list of patched modules in <code>OffloadManager._patched_modules</code> for proper cleanup during <code>release()</code></li> <li>Use unbound class methods: Store the unbound forward function from the class, not a bound method, to ensure correct behavior when models are copied during state transitions</li> <li>Use types.MethodType: Bind the patched forward as a method so it receives <code>self</code> correctly</li> </ol> <p>Key implementation details:</p> <pre><code>def _patch_module_forward(self, module: nn.Module, offload_name: str) -&gt; None:\n    \"\"\"Patch module's forward to include offload context.\"\"\"\n    import functools\n    import types\n\n    # Get the UNBOUND forward function from the class, not a bound method.\n    # This is critical: bound methods capture `self`, which breaks when models\n    # are copied during state transitions (warmup \u2192 profile \u2192 inference).\n    original_forward_func = type(module).forward\n    offload_manager = self\n\n    def patched_forward(self_module, *args, **kwargs):\n        with offload_manager.offload_block(offload_name):\n            # Call the unbound forward function with self_module explicitly\n            return original_forward_func(self_module, *args, **kwargs)\n\n    patched_forward = functools.wraps(original_forward_func)(patched_forward)\n\n    # Store original for restoration (unbound function, not bound method)\n    module._ft_original_forward_func = original_forward_func\n    module._ft_offload_name = offload_name\n\n    # Bind patched_forward as a method to the module\n    module.forward = types.MethodType(patched_forward, module)\n</code></pre> <p>Why unbound functions instead of bound methods?</p> <p>FlexTensor transitions through multiple states (warmup \u2192 profile \u2192 inference), and during these transitions, the model may be copied or replaced with a prepared version that has property getters for tensor offloading. If we captured a bound method in the closure, it would remain bound to the OLD module even after the model is replaced, causing tensor accesses to use the wrong module (one without property getters). By using unbound functions and binding <code>self</code> dynamically via <code>types.MethodType</code>, the patched forward always uses the correct current module.</p> <p>This approach is simpler, more direct, and avoids the complexity of wrapper objects while correctly handling model state transitions.</p>"},{"location":"adr/0001-forward-patching-for-module-offloading/#alternatives-considered","title":"Alternatives Considered","text":"<p>We evaluated three main approaches for intercepting module forward passes:</p>"},{"location":"adr/0001-forward-patching-for-module-offloading/#alternative-1-module-wrapper-currentoriginal-implementation","title":"Alternative 1: Module Wrapper (Current/Original Implementation)","text":"<p>Approach: Create an <code>OffloadModule</code> class that inherits from <code>nn.Module</code> and wraps the target module, delegating calls through <code>__getattr__</code>.</p> <p>Pros: - Object-oriented and feels \"Pythonic\" with a dedicated class - Clear encapsulation of offload logic - Easy to identify wrapped modules via <code>isinstance(module, OffloadModule)</code></p> <p>Cons: - Changes model hierarchy (<code>model.layers[0]</code> returns <code>OffloadModule</code>, not actual layer) - Breaks <code>isinstance(module, OriginalType)</code> checks - wrapper is not a subclass of the original module type - Requires complex <code>__getattr__</code> delegation to forward attribute access to wrapped module - Complicates serialization - <code>state_dict()</code> keys include wrapper hierarchy - Harder to debug - stack traces show <code>OffloadModule.forward</code> instead of actual layer name - Issues with frameworks that inspect module types (vLLM's <code>PPMissingLayer</code> checks fail)</p> <p>Why rejected: The hierarchy changes and <code>isinstance</code> breakage are fundamental issues that complicate integration with external frameworks. The delegation complexity adds maintenance burden.</p>"},{"location":"adr/0001-forward-patching-for-module-offloading/#alternative-2-pytorch-hooks","title":"Alternative 2: PyTorch Hooks","text":"<p>Approach: Use <code>register_forward_pre_hook()</code> and <code>register_forward_hook()</code> to wrap execution in offload context.</p> <p>Pros: - Official PyTorch API with documented behavior - Preserves model hierarchy completely - Hooks can be registered/removed dynamically - No custom <code>__getattr__</code> or class wrapping needed - Works well with PyTorch internals</p> <p>Cons: - Context manager pattern is awkward (enter in pre-hook, exit in post-hook) - Need to store context manager state between hooks (e.g., in dict) - Hooks are called for every forward pass, adding slight overhead - Pre-hook and post-hook separation makes code harder to follow - Error handling between pre/post hooks is complex</p> <p>Why rejected: While technically sound, the split between pre/post hooks makes the context manager pattern awkward. The need to manage state between hooks adds complexity without clear benefits over direct patching.</p>"},{"location":"adr/0001-forward-patching-for-module-offloading/#alternative-3-dynamic-subclassing","title":"Alternative 3: Dynamic Subclassing","text":"<p>Approach: Dynamically change the module's <code>__class__</code> to a subclass that overrides <code>forward()</code>.</p> <p>Pros: - Preserves most hierarchy aspects - <code>isinstance</code> checks still work (subclass relationship maintained) - No delegation needed - Can override other methods if needed</p> <p>Cons: - Unusual Python pattern (changing <code>__class__</code> at runtime) - Creates new classes dynamically (memory overhead) - Potential issues with pickling/serialization - May confuse static analysis tools - Less clear in debugging (shows dynamically created class) - Unclear interaction with <code>torch.compile</code> and JIT</p> <p>Why rejected: While technically possible, changing <code>__class__</code> at runtime is unconventional and could cause subtle issues with serialization, introspection tools, and compilation. The uncertainty around edge cases outweighs the benefits.</p>"},{"location":"adr/0001-forward-patching-for-module-offloading/#alternative-4-forward-method-patching-selected","title":"Alternative 4: Forward Method Patching (Selected)","text":"<p>Approach: Replace <code>module.forward</code> with a closure that wraps the original forward in an offload context manager.</p> <p>Pros: - Simplest implementation (direct function replacement with closure) - Preserves model hierarchy completely (module identity unchanged) - <code>isinstance</code> checks work perfectly (module class unchanged) - No attribute delegation needed (all attributes directly accessible) - Native serialization works without changes (<code>state_dict()</code> keys unchanged) - Clear in stack traces (shows actual module type) - Well-established pattern in Python (decorators, monkey-patching) and used internally by PyTorch hooks - Easy to restore original behavior (store <code>_ft_original_forward_func</code>) - Compatible with <code>functools.wraps</code> for preserving function metadata</p> <p>Cons: - Function assignment is less \"object-oriented\" - Type checkers may warn (suppressed with <code># type: ignore</code>) - Less conventional than subclassing</p> <p>Why selected: This approach provides the best balance of simplicity, correctness, and compatibility. It directly solves the hierarchy and <code>isinstance</code> issues without introducing new complexity. The pattern is well-established in Python (decorators, hooks) and PyTorch internals.</p>"},{"location":"adr/0001-forward-patching-for-module-offloading/#consequences","title":"Consequences","text":""},{"location":"adr/0001-forward-patching-for-module-offloading/#positive","title":"Positive","text":"<ul> <li>Preserves model hierarchy: <code>model.layers[0]</code> returns the actual layer, not a wrapper. Model navigation works as expected.</li> <li>isinstance checks work: Framework code using <code>isinstance(module, LayerClass)</code> continues to work correctly.</li> <li>No attribute delegation needed: All attributes are directly accessible without custom <code>__getattr__</code> logic.</li> <li>Native serialization: <code>state_dict()</code>, <code>torch.save()</code>, and <code>torch.load()</code> work without special handling.</li> <li>Better debugging: Stack traces and introspection show actual module types, not wrappers.</li> <li>Simpler code: Less code to maintain, fewer edge cases to handle.</li> <li>torch.compile compatibility: More likely to work with compilation modes that inspect module structure.</li> </ul>"},{"location":"adr/0001-forward-patching-for-module-offloading/#negative","title":"Negative","text":"<ul> <li>Breaking change: Code that imports <code>OffloadModule</code> directly will break. This is acceptable because:</li> <li><code>OffloadModule</code> was not documented as part of the public API</li> <li>The high-level <code>flextensor.offload()</code> API remains unchanged</li> <li>Internal use is limited to FlexTensor itself</li> <li>Function assignment in Python: Assigning to <code>module.forward</code> is less conventional than subclassing, though it's a well-established pattern (used in PyTorch hooks, decorators, etc.)</li> <li>Type checking challenges: Static type checkers may warn about assigning to <code>forward</code> method. We suppress these with <code># type: ignore[method-assign]</code> comments.</li> </ul>"},{"location":"adr/0001-forward-patching-for-module-offloading/#neutral","title":"Neutral","text":"<ul> <li>Different debugging experience: When stepping through code, debuggers will show the patched closure rather than a class. This is neither better nor worse, just different.</li> <li>Restoration required: We must track and restore original forwards during cleanup. The wrapper approach also required cleanup (removing wrappers from parent modules).</li> <li>Attribute storage: Using <code>_ft_*</code> prefixed attributes on modules is unconventional but safe. Alternative approaches (WeakKeyDictionary) would be more complex.</li> </ul>"},{"location":"adr/0001-forward-patching-for-module-offloading/#references","title":"References","text":""},{"location":"adr/0001-forward-patching-for-module-offloading/#internal-code-references","title":"Internal Code References","text":"<ul> <li>Forward patching implementation: <code>flextensor/offload_manager.py::_patch_module_forward</code></li> <li>Test suite for patching behavior: <code>tests/unit/test_forward_patching.py</code></li> <li>vLLM integration using patching: <code>flextensor/contrib/vllm/worker.py</code></li> <li>Original experiment with forward patching: <code>experiments/202504-vllm_model_offload/README.md</code> (note: this file has been removed)</li> </ul>"},{"location":"adr/0001-forward-patching-for-module-offloading/#external-references","title":"External References","text":"<ul> <li>PyTorch Module hooks documentation: https://pytorch.org/docs/stable/generated/torch.nn.Module.html#torch.nn.Module.register_forward_hook</li> <li>PyTorch <code>state_dict</code> documentation: https://pytorch.org/docs/stable/generated/torch.nn.Module.html#torch.nn.Module.state_dict</li> <li>vLLM model layer type checking examples:</li> <li>Pipeline parallelism checks: https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/models/utils.py#L255</li> <li>MoE layer detection: https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/models/qwen3_moe.py#L679-L683</li> <li>Python <code>functools.wraps</code> for preserving function metadata: https://docs.python.org/3/library/functools.html#functools.wraps</li> </ul>"},{"location":"adr/0002-configuration-based-module-patterns/","title":"0002 configuration based module patterns","text":""},{"location":"adr/0002-configuration-based-module-patterns/#adr-0002-configuration-based-module-patterns-for-offloading","title":"ADR-0002: Configuration-based Module Patterns for Offloading","text":"<p>Date: 2026-01-23</p> <p>Status: Accepted</p>"},{"location":"adr/0002-configuration-based-module-patterns/#context","title":"Context","text":"<p>FlexTensor needs to identify which modules in a model should be offloaded. The original API required users to explicitly pass a list of module paths to the <code>offload()</code> function:</p> <pre><code>model = om.offload(model, [\"embed\", \"layers.*\", \"head\"])\n</code></pre> <p>This approach had several issues:</p> <ol> <li> <p>Inconsistent configuration: Module paths were specified via function parameter while all other settings (GPU device, warmup iterations, etc.) were configured via <code>OffloadConfig</code>. This created an inconsistent API where some configuration came from the config object and some from function arguments.</p> </li> <li> <p>No environment variable support: Users couldn't configure module patterns via environment variables like other settings (<code>FT_ENABLED</code>, <code>FT_GPU_DEVICE</code>, etc.). This limited deployment flexibility, especially in containerized environments where environment variables are the preferred configuration method.</p> </li> <li> <p>vLLM integration complexity: The <code>FlexTensorOffloadWorker</code> needed to determine appropriate module patterns for each model architecture. Without a unified configuration mechanism, this logic was scattered and hard to maintain.</p> </li> <li> <p>Config file support: Module patterns couldn't be loaded from configuration files (YAML, JSON, INI), requiring code changes to modify which modules are offloaded.</p> </li> </ol> <p>The FlexTensor configuration system already supports loading from multiple sources with clear precedence: file &lt; environment variables &lt; kwargs. Module patterns should follow the same pattern.</p>"},{"location":"adr/0002-configuration-based-module-patterns/#decision","title":"Decision","text":"<p>We will move module patterns into <code>OffloadConfig</code> as a first-class configuration field, removing the <code>module_paths</code> parameter from the <code>offload()</code> API.</p> <p>Implementation approach:</p> <ol> <li>Add <code>module_patterns</code> field to <code>OffloadConfig</code>: A list of module path patterns with wildcard support, defaulting to <code>[\"*\"]</code> (offload all modules)</li> </ol> <pre><code>class OffloadConfig(BaseModel):\n    # ... existing fields ...\n    module_patterns: list[str] = Field(\n        default_factory=lambda: [\"*\"],\n        description=\"List of module path patterns to offload (supports wildcards)\",\n    )\n</code></pre> <ol> <li>Support environment variable configuration: Add <code>FT_MODULE_PATTERNS</code> as a comma-separated list of patterns</li> </ol> <pre><code>FT_MODULE_PATTERNS=\"model.layers.*,lm_head,embed_tokens\" vllm serve ...\n</code></pre> <ol> <li>Support config file loading: Module patterns can be specified in YAML, JSON, or INI files</li> </ol> <pre><code># flextensor.yaml\nenabled: true\nmodule_patterns:\n  - model.layers.*\n  - lm_head\n  - embed_tokens\n</code></pre> <ol> <li>Simplify <code>offload()</code> API: Remove <code>module_paths</code> parameter; patterns come from <code>config.module_patterns</code></li> </ol> <pre><code># Before\nmodel = om.offload(model, [\"layers.*\", \"head\"], config)\n\n# After\nconfig = OffloadConfig(module_patterns=[\"layers.*\", \"head\"])\nmodel = om.offload(model, config)\n</code></pre> <ol> <li>Use <code>*</code> as default: When no patterns are specified, default to <code>[\"*\"]</code> which matches all modules. This provides a reasonable default for quick experimentation.</li> </ol>"},{"location":"adr/0002-configuration-based-module-patterns/#alternatives-considered","title":"Alternatives Considered","text":""},{"location":"adr/0002-configuration-based-module-patterns/#alternative-1-keep-module_paths-as-separate-parameter","title":"Alternative 1: Keep module_paths as Separate Parameter","text":"<p>Approach: Maintain the current API where module paths are passed as a separate argument to <code>offload()</code>.</p> <p>Pros: - No API change required - Explicit separation between \"what to offload\" and \"how to offload\" - Backward compatible</p> <p>Cons: - Inconsistent with other configuration (all settings in config except this one) - No environment variable support for patterns - Cannot load patterns from config files - Users must modify code to change offload targets</p> <p>Why rejected: The inconsistency with the rest of the configuration system creates a poor user experience. Users expect all FlexTensor settings to be configurable via environment variables and config files.</p>"},{"location":"adr/0002-configuration-based-module-patterns/#alternative-2-auto-detect-module-patterns-from-model-architecture","title":"Alternative 2: Auto-detect Module Patterns from Model Architecture","text":"<p>Approach: Automatically detect and select appropriate modules based on model architecture introspection (e.g., find transformer layers, embeddings, heads).</p> <p>Pros: - Zero-configuration experience for common models - No need to understand model internals - Adapts automatically to new architectures</p> <p>Cons: - Complex heuristics that may not work for all models - Magic behavior that's hard to debug - May select too many or too few modules - Different model families use different naming conventions - Adds maintenance burden for heuristics</p> <p>Why rejected: While appealing in theory, automatic detection is fragile and hard to debug. When it works, it's convenient; when it doesn't, users have no recourse. Configuration-based approach gives users explicit control.</p>"},{"location":"adr/0002-configuration-based-module-patterns/#alternative-3-decorator-based-module-marking","title":"Alternative 3: Decorator-based Module Marking","text":"<p>Approach: Users mark modules to offload using decorators or model attributes.</p> <pre><code>class MyModel(nn.Module):\n    @flextensor.offloadable\n    def __init__(self):\n        self.layers = OffloadableModuleList([...])\n</code></pre> <p>Pros: - Explicit marking in model definition - No configuration needed at runtime - Self-documenting</p> <p>Cons: - Requires model code modification - Cannot use with third-party models (vLLM, HuggingFace) - Not compatible with pre-trained model loading - Intrusive API</p> <p>Why rejected: FlexTensor must work with existing models without modification. Most users work with pre-trained models from HuggingFace or other sources that cannot be modified.</p>"},{"location":"adr/0002-configuration-based-module-patterns/#alternative-4-configuration-based-module-patterns-selected","title":"Alternative 4: Configuration-based Module Patterns (Selected)","text":"<p>Approach: Add <code>module_patterns</code> as a field in <code>OffloadConfig</code>, loadable from environment variables and config files.</p> <p>Pros: - Consistent with existing configuration system - Environment variable support (<code>FT_MODULE_PATTERNS</code>) - Config file support (YAML, JSON, INI) - Explicit user control - Works with any model without modification - Easy to experiment (change env var, not code) - Follows twelve-factor app principles for configuration</p> <p>Cons: - API change (removal of <code>module_paths</code> parameter) - Users must learn pattern syntax - Default <code>[\"*\"]</code> may not be optimal for all cases</p> <p>Why selected: This approach provides the best balance of flexibility, consistency, and usability. It integrates naturally with the existing configuration system and follows established patterns for application configuration.</p>"},{"location":"adr/0002-configuration-based-module-patterns/#consequences","title":"Consequences","text":""},{"location":"adr/0002-configuration-based-module-patterns/#positive","title":"Positive","text":"<ul> <li>Unified configuration: All FlexTensor settings are now in <code>OffloadConfig</code>, including module patterns</li> <li>Environment variable support: Deploy different configurations via <code>FT_MODULE_PATTERNS</code> without code changes</li> <li>Config file support: Define patterns in YAML/JSON/INI files for version control and reproducibility</li> <li>Simplified API: <code>offload()</code> takes only model and optional config, reducing argument complexity</li> <li>Container-friendly: Easily configure via environment variables in Kubernetes, Docker, etc.</li> <li>Consistent precedence: Module patterns follow the same file &lt; env &lt; kwargs precedence as other settings</li> </ul>"},{"location":"adr/0002-configuration-based-module-patterns/#negative","title":"Negative","text":"<ul> <li>Breaking API change: Existing code using <code>offload(model, module_paths)</code> must be updated to use <code>OffloadConfig(module_patterns=...)</code>. This is mitigated by:</li> <li>The library is pre-1.0 and breaking changes are expected</li> <li>The change is straightforward to migrate</li> <li>Error messages guide users to the new API</li> <li>Default behavior change: Default <code>[\"*\"]</code> offloads all modules, which may surprise users expecting no offloading by default. However, this is appropriate since:</li> <li><code>offload()</code> is explicitly called when offloading is desired</li> <li>Users calling <code>offload()</code> expect something to be offloaded</li> </ul>"},{"location":"adr/0002-configuration-based-module-patterns/#neutral","title":"Neutral","text":"<ul> <li>Pattern syntax unchanged: The wildcard pattern syntax (<code>layers.*</code>, <code>model.*.attention</code>) remains the same, just configured differently</li> <li>vLLM integration unchanged: <code>FlexTensorOffloadWorker</code> continues to work, now using <code>FT_MODULE_PATTERNS</code> for custom patterns</li> </ul>"},{"location":"adr/0002-configuration-based-module-patterns/#references","title":"References","text":""},{"location":"adr/0002-configuration-based-module-patterns/#internal-code-references","title":"Internal Code References","text":"<ul> <li>Configuration implementation: <code>flextensor/config.py::OffloadConfig.module_patterns</code></li> <li>Environment variable parsing: <code>flextensor/config.py::_load_from_env</code></li> <li>vLLM worker integration: <code>flextensor/contrib/vllm/worker.py::FlexTensorOffloadWorker</code></li> <li>Unit tests: <code>tests/unit/test_config.py</code></li> </ul>"},{"location":"adr/0002-configuration-based-module-patterns/#external-references","title":"External References","text":"<ul> <li>Twelve-factor app configuration principles: https://12factor.net/config</li> <li>Pydantic Field documentation: https://docs.pydantic.dev/latest/concepts/fields/</li> </ul>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/","title":"0003 custom type handlers for tensor processor","text":""},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#adr-0003-custom-type-handlers-for-tensorprocessor","title":"ADR-0003: Custom Type Handlers for TensorProcessor","text":"<p>Date: 2026-02-16</p> <p>Status: Accepted</p>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#context","title":"Context","text":"<p><code>TensorProcessor</code> is the core mechanism for walking a PyTorch model's attribute tree and applying per-tensor transformations (pinning to memory, moving to GPU, benchmarking, etc.).  The base class's <code>_apply_on()</code> method previously contained a hardcoded cascade of <code>isinstance</code> checks to decide how each module attribute should be processed:</p> <pre><code># Before (hardcoded in _apply_on)\nif isinstance(attr_value, torch.Tensor):\n    updated_attr = self.process(attr_value)\nif isinstance(attr_value, set) and len(attr_value) &gt; 0:\n    updated_attr = set()\n    for value in attr_value:\n        updated_attr.add(self.process(value))\nif isinstance(attr_value, collections.OrderedDict):\n    ...\nelif isinstance(attr_value, dict):\n    ...\n</code></pre> <p>This design has several problems:</p> <ol> <li> <p>Not extensible for new attribute types: Real-world models (especially in vLLM, DeepSeek, and Megatron-LM) use custom data structures on modules \u2014 shared weight containers, exotic parameter wrappers, metadata objects holding tensors.  When <code>_apply_on()</code> encounters these, it silently skips the tensors inside them, leading to missed offloading, stale device placements, or crashes.</p> </li> <li> <p>No way for users to intervene: The only extension point was subclassing <code>TensorProcessor</code> and overriding <code>_apply_on()</code> entirely \u2014 a fragile approach that forces users to duplicate the whole iteration and type-check logic and breaks whenever FlexTensor adds new built-in handling.</p> </li> <li> <p><code>nn.Parameter</code> preservation was ad-hoc: Operations like <code>tensor.to(device)</code> and <code>tensor.pin_memory()</code> strip the <code>nn.Parameter</code> wrapper, returning a plain <code>torch.Tensor</code>.  Each subclass had to remember to re-wrap parameters manually.  Missing this caused silent bugs in id()-based tensor tracking (<code>tensors_map</code>, <code>traced_tensors</code>).</p> </li> <li> <p>Hardcoded type checks are fragile: Adding support for a single new container type (e.g. <code>NamedTuple</code>, <code>dataclass</code>) required editing the base class \u2014 violating the Open/Closed Principle and risking regressions in all existing subclasses.</p> </li> </ol>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#decision","title":"Decision","text":"<p>We will replace the hardcoded <code>isinstance</code> cascade in <code>_apply_on()</code> with a type handler dispatch system, allowing custom attribute types to be processed without modifying <code>TensorProcessor</code> internals.</p>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#core-abstractions","title":"Core abstractions","text":"<ol> <li><code>TypeHandler</code> Protocol \u2014 A lightweight interface with two methods:</li> <li><code>can_handle(value) -&gt; bool</code> \u2014 returns <code>True</code> if the handler should process this attribute value.</li> <li> <p><code>process_attribute(value, ctx) -&gt; Any</code> \u2014 processes the value, using <code>ctx</code> for tensor operations.</p> </li> <li> <p><code>ProcessingContext</code> \u2014 A context object passed to handlers that wraps the owning processor and exposes safe helper methods (<code>process()</code>, <code>process_and_preserve()</code>, <code>dispatch()</code>).  This prevents handlers from depending on processor internals while still giving them full access to tensor processing capabilities including <code>nn.Parameter</code> preservation.</p> </li> <li> <p>Built-in type handlers \u2014 The three formerly-hardcoded cases are refactored into public handler classes (<code>TensorTypeHandler</code>, <code>SetTypeHandler</code>, <code>DictTypeHandler</code>) that sit at the end of the dispatch chain.</p> </li> </ol>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#handler-priority-chain","title":"Handler priority chain","text":"<p>When <code>_apply_on()</code> processes a module attribute, handlers are checked in order:</p> <ol> <li>Instance-level custom handlers (registered via <code>register_type_handler()</code> or the <code>type_handlers</code> constructor parameter)</li> <li>Global handlers (registered via <code>TensorProcessor.register_global_type_handler()</code>)</li> <li>Built-in handlers (tensor, set, dict)</li> </ol> <p>The first handler whose <code>can_handle()</code> returns <code>True</code> wins.  Within each tier, later registrations take higher priority (inserted at index 0).</p>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#nnparameter-preservation","title":"<code>nn.Parameter</code> preservation","text":"<p>A new <code>preserve_parameter_type()</code> function centralizes the logic to re-wrap <code>nn.Parameter</code> after processing.  Two modes are supported via the <code>force_update_nn_parameters</code> constructor flag:</p> <ul> <li><code>False</code> (default): Updates <code>Parameter.data</code> in-place, preserving object identity for id()-based tracking.</li> <li><code>True</code>: Creates a new <code>nn.Parameter</code>, used when building independent model copies (e.g. profile models) where identity should intentionally differ.</li> </ul> <p><code>ProcessingContext.process_and_preserve()</code> exposes this to handlers as a single convenient call.</p>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#registration-api","title":"Registration API","text":"<pre><code># Per-instance handler (highest priority)\nprocessor = MoveToGPUTensorProcessor(device)\nprocessor.register_type_handler(MyHandler())\n\n# Or via constructor\nprocessor = MoveToGPUTensorProcessor(device, type_handlers=[MyHandler()])\n\n# Global handler (applies to all processors)\nTensorProcessor.register_global_type_handler(MyHandler())\nTensorProcessor.clear_global_type_handlers()  # cleanup\n</code></pre>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#naming-rationale","title":"Naming rationale","text":"<p>We chose \"Type Handler\" over alternatives:</p> <ul> <li>\"Plugin\": Too generic; implies a broader extensibility architecture.  This is specifically about dispatching attribute processing by type.</li> <li>\"Custom Ops\": Strongly collides with PyTorch's <code>torch.ops</code> / custom operator concept.  Would be confusing for PyTorch users.</li> <li>\"Attribute Processor\": Collides with the existing <code>TensorProcessor</code> naming.</li> <li>\"Value Handler\": Less descriptive \u2014 \"type\" signals the dispatch-by-type pattern.</li> </ul> <p>\"Type Handler\" follows established patterns (Django form field handlers, marshmallow type serializers) and precisely describes what the abstraction does.</p>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#alternatives-considered","title":"Alternatives Considered","text":""},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#alternative-1-override-_apply_on-in-subclasses","title":"Alternative 1: Override <code>_apply_on()</code> in subclasses","text":"<p>Approach: Each subclass that needs custom type handling overrides <code>_apply_on()</code> and adds its own <code>isinstance</code> checks alongside the base ones.</p> <p>Pros: - No new abstractions needed - Standard inheritance pattern</p> <p>Cons: - Forces duplicating the entire iteration and type-check logic in each subclass - Breaks whenever the base class adds new built-in type handling - No way for users to add handling without subclassing every processor they use - Violates Open/Closed Principle</p> <p>Why rejected: Fragile and not user-extensible.  Users integrating custom model architectures (vLLM workers, DeepSeek inference) would need to subclass every processor variant.</p>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#alternative-2-functoolssingledispatch-based-dispatch","title":"Alternative 2: <code>functools.singledispatch</code> based dispatch","text":"<p>Approach: Use Python's <code>singledispatch</code> to register processing functions per type.</p> <p>Pros: - Standard library mechanism - Familiar pattern for Pythonistas - Type-safe dispatch</p> <p>Cons: - Dispatches on exact type, not on predicates \u2014 cannot handle \"any object with attribute X\" patterns - No support for priority ordering between handlers - Global mutable state (dispatch table) with no instance-level overrides - Awkward integration with <code>ProcessingContext</code> (no natural way to pass context) - Cannot handle duck-typed custom containers common in ML frameworks</p> <p>Why rejected: ML model attributes frequently use duck-typing and custom wrappers where predicate-based dispatch (<code>can_handle()</code>) is more appropriate than exact-type dispatch.</p>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#alternative-3-visitor-pattern","title":"Alternative 3: Visitor Pattern","text":"<p>Approach: Define a <code>TensorVisitor</code> with <code>visit_tensor()</code>, <code>visit_dict()</code>, <code>visit_set()</code>, etc. methods.</p> <p>Pros: - Well-known design pattern - Clear separation of traversal and processing</p> <p>Cons: - Requires a <code>visit_*</code> method for every type, defeating extensibility for unknown types - Adding a new type requires updating the visitor interface (same problem as hardcoded checks) - Overly formal for the simple dispatch needed here - No predicate-based matching for custom types</p> <p>Why rejected: The Visitor pattern works well when the set of types is fixed and known.  Our problem is specifically that the set of types is open and determined by external model architectures.</p>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#alternative-4-type-handler-dispatch-selected","title":"Alternative 4: Type Handler Dispatch (Selected)","text":"<p>Approach: A <code>TypeHandler</code> protocol with <code>can_handle()</code> / <code>process_attribute()</code> methods, checked in priority order (instance \u2192 global \u2192 built-in).</p> <p>Pros: - Open for extension without modifying base class (Open/Closed Principle) - Predicate-based dispatch handles duck-typed and custom types - Priority ordering gives users control over handler precedence - <code>ProcessingContext</code> provides safe, documented access to processor capabilities - Built-in handlers are just regular handlers, no special casing - <code>Protocol</code> class provides structural typing without requiring inheritance - Instance-level and global scopes cover both targeted and broad use cases</p> <p>Cons: - New abstraction to learn (<code>TypeHandler</code>, <code>ProcessingContext</code>) - Linear scan of handlers per attribute (negligible for realistic handler counts)</p> <p>Why selected: Best fit for the open-type problem.  Predicate dispatch is the right tool when the type set is unbounded.  The handler chain is simple to understand and extend.</p>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#consequences","title":"Consequences","text":""},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#positive","title":"Positive","text":"<ul> <li>User-extensible: Custom model architectures (vLLM shared weights, DeepSeek MoE containers, Megatron distributed parameters) can be handled without forking FlexTensor.</li> <li>Cleaner <code>_apply_on()</code>: The method body shrinks from a multi-branch <code>isinstance</code> cascade to a simple handler dispatch loop.</li> <li>Centralised <code>nn.Parameter</code> preservation: <code>preserve_parameter_type()</code> and <code>ProcessingContext.process_and_preserve()</code> eliminate a class of bugs where parameter wrappers were silently stripped.</li> <li>Testable in isolation: Each handler can be unit-tested independently via <code>ProcessingContext</code>.</li> <li>Backward compatible: Existing <code>TensorProcessor</code> subclasses continue to work unchanged \u2014 they only override <code>process()</code>, not <code>_apply_on()</code>.</li> </ul>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#negative","title":"Negative","text":"<ul> <li>New concepts to learn: Contributors need to understand <code>TypeHandler</code>, <code>ProcessingContext</code>, and the priority chain.  Mitigated by docstrings and the Protocol pattern being well-known in Python.</li> <li>Global handler state: <code>_global_type_handlers</code> is mutable class-level state.  Tests must call <code>clear_global_type_handlers()</code> in teardown.  This is an accepted trade-off for the convenience of framework-wide handler registration.</li> </ul>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#intentional-behavioural-improvement-set-element-processing","title":"Intentional behavioural improvement: set element processing","text":"<p>The original hardcoded set handling called <code>self.process(value)</code> directly on each element \u2014 no handler dispatch, no <code>nn.Parameter</code> preservation.  The new built-in <code>SetTypeHandler</code> routes elements through <code>ctx.dispatch()</code>, which:</p> <ol> <li>Checks custom type handlers for each element (enabling recursive dispatch for exotic types nested inside sets).</li> <li>Falls back to <code>process_and_preserve()</code> for tensors, which applies <code>nn.Parameter</code> re-wrapping.</li> </ol> <p>This is an intentional improvement: the original omission of Parameter preservation in sets was an oversight, not a deliberate choice, and it was inconsistent with how dict values were handled.</p> <p>Escape hatch \u2014 If the improved behaviour causes issues with a specific model, register <code>LegacySetTypeHandler</code> on the processor instance to restore the original <code>self.process()</code>-only semantics:</p> <pre><code>from flextensor.tensor_processors import LegacySetTypeHandler\n\nprocessor = MoveToGPUTensorProcessor(device)\nprocessor.register_type_handler(LegacySetTypeHandler())\n</code></pre>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#neutral","title":"Neutral","text":"<ul> <li>Performance: The handler dispatch adds a linear scan per attribute.  With the typical 3 built-in + 0\u20132 custom handlers, this is negligible compared to tensor transfer times.</li> <li><code>_apply_on()</code> complexity shifts: Instead of hardcoded branches, the complexity now lives in the handler chain assembly.  The total complexity is similar, just better organized.</li> </ul>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#references","title":"References","text":""},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#internal-code-references","title":"Internal Code References","text":"<ul> <li>Type handler implementation: <code>flextensor/tensor_processors.py::TypeHandler</code></li> <li><code>ProcessingContext</code> implementation: <code>flextensor/tensor_processors.py::ProcessingContext</code></li> <li>Built-in handlers: <code>flextensor/tensor_processors.py::TensorTypeHandler</code>, <code>SetTypeHandler</code>, <code>DictTypeHandler</code></li> <li>Handler dispatch in <code>_apply_on()</code>: <code>flextensor/tensor_processors.py::TensorProcessor._apply_on</code></li> <li>Unit tests: <code>tests/unit/test_tensor_processors.py::TestTypeHandlerSystem</code></li> </ul>"},{"location":"adr/0003-custom-type-handlers-for-tensor-processor/#external-references","title":"External References","text":"<ul> <li>Python <code>Protocol</code> class documentation: https://docs.python.org/3/library/typing.html#typing.Protocol</li> <li>Chain of Responsibility pattern: https://refactoring.guru/design-patterns/chain-of-responsibility</li> <li>PyTorch <code>nn.Parameter</code> documentation: https://pytorch.org/docs/stable/generated/torch.nn.Parameter.html</li> </ul>"},{"location":"adr/0004-percentage-based-gpu-memory-limit/","title":"0004 percentage based gpu memory limit","text":""},{"location":"adr/0004-percentage-based-gpu-memory-limit/#adr-0004-percentage-based-gpu-memory-limit-configuration","title":"ADR-0004: Percentage-Based GPU Memory Limit Configuration","text":"<p>Date: 2026-03-02</p> <p>Status: Accepted</p>"},{"location":"adr/0004-percentage-based-gpu-memory-limit/#context","title":"Context","text":"<p>The <code>max_gpu_mem_bytes</code> configuration field in <code>OffloadConfig</code> currently accepts only an absolute byte value (<code>int | None</code>). When set, strategies switch from latency mode to memory mode and keep peak GPU usage within that budget.</p> <p>Specifying an absolute byte count is fragile in practice:</p> <ul> <li>Users must know the exact total memory of the target GPU and calculate the byte value themselves.</li> <li>The same configuration cannot be reused across GPUs with different memory sizes (e.g., A100 40GB   vs A100 80GB) without manual adjustment.</li> <li>A percentage-based limit (e.g., 0.9 = \"use up to 90% of GPU memory\") is more intuitive, portable,   and aligns with how users reason about memory budgets.</li> </ul> <p>The field is consumed in a single place \u2014 <code>OffloadManager._initialize_tensor_manager()</code> \u2014 before being passed to strategy constructors (<code>AdaptiveStrategy</code>, <code>KnapsackStrategy</code>, <code>GlobalOffloadStrategy</code>, etc.) which all expect <code>max_gpu_mem_bytes: int | None</code>. The env var is <code>FT_MAX_GPU_MEM_BYTES</code>.</p> <p>The resolution from fraction to bytes requires querying the GPU at runtime via <code>torch.cuda.mem_get_info(device)</code> or <code>torch.cuda.get_device_properties(device).total_memory</code>.</p>"},{"location":"adr/0004-percentage-based-gpu-memory-limit/#decision","title":"Decision","text":"<p>Option D (replace with fraction-only) implemented via a deprecate-first approach:</p> <ul> <li>Add <code>max_gpu_mem_fraction: float | None</code> (default <code>0.9</code>) as the primary field in <code>OffloadConfig</code>.</li> <li>Deprecate <code>max_gpu_mem_bytes</code> in v0.4; remove in v0.5.</li> <li>Resolution from fraction to bytes occurs in <code>OffloadManager._initialize_tensor_manager()</code> via   <code>torch.cuda.get_device_properties(config.gpu_device).total_memory</code>.</li> <li>The SHM namespace hash uses resolved bytes (not the raw fraction), so different GPU SKUs produce   different namespaces automatically \u2014 correct for homogeneous device fleets where all workers   share the same GPU SKU.</li> <li><code>max_gpu_mem_fraction</code> naming chosen for internal consistency with <code>max_gpu_mem_bytes</code> (direct   suffix swap), taking precedence over ecosystem alignment with <code>gpu_memory_utilization</code> (vLLM).</li> <li>Default <code>0.9</code> aligns with vLLM and TensorRT-LLM conventions.</li> </ul>"},{"location":"adr/0004-percentage-based-gpu-memory-limit/#options","title":"Options","text":""},{"location":"adr/0004-percentage-based-gpu-memory-limit/#option-a-add-a-new-max_gpu_mem_fraction-field-additive-backward-compatible","title":"Option A: Add a new <code>max_gpu_mem_fraction</code> field (additive, backward-compatible)","text":"<p>Add a second field alongside the existing one:</p> <pre><code>max_gpu_mem_bytes: int | None = Field(default=None, ge=0)\nmax_gpu_mem_fraction: float | None = Field(default=None, gt=0.0, le=1.0)\n</code></pre> <p>A pydantic <code>model_validator</code> ensures at most one is set. Resolution happens in <code>OffloadManager._initialize_tensor_manager()</code>:</p> <pre><code>if config.max_gpu_mem_fraction is not None:\n    total = torch.cuda.get_device_properties(config.gpu_device).total_memory\n    max_gpu_mem_bytes = int(total * config.max_gpu_mem_fraction)\n</code></pre> <p>Strategies continue to receive <code>max_gpu_mem_bytes: int</code> \u2014 zero downstream changes.</p> Pros Fully backward-compatible; explicit field names; easy pydantic validation; env vars are clear (<code>FT_MAX_GPU_MEM_FRACTION=0.9</code>); no changes to strategy code. Cons Two fields for the same concept; users must know which one to use; slightly larger config surface."},{"location":"adr/0004-percentage-based-gpu-memory-limit/#option-b-union-type-single-field-accepting-int-bytes-or-float-fraction","title":"Option B: Union type \u2014 single field accepting <code>int</code> (bytes) or <code>float</code> (fraction)","text":"<p>Replace the existing field with a union:</p> <pre><code>max_gpu_mem: int | float | None = Field(default=None)\n</code></pre> <p>Convention: <code>int</code> \u2192 absolute bytes, <code>float</code> in (0.0, 1.0] \u2192 fraction of total GPU memory.</p> Pros Single field; concise API; one env var (<code>FT_MAX_GPU_MEM</code>). Cons Breaking rename (<code>max_gpu_mem_bytes</code> \u2192 <code>max_gpu_mem</code>); implicit semantics based on Python type (<code>1</code> = 1 byte vs <code>1.0</code> = 100% \u2014 subtle); JSON/YAML cannot distinguish <code>int</code> vs <code>float</code> for values like <code>1</code> vs <code>1.0</code>; requires custom pydantic validator; harder to document."},{"location":"adr/0004-percentage-based-gpu-memory-limit/#option-c-string-based-with-units-eg-90-8gb","title":"Option C: String-based with units (e.g., <code>\"90%\"</code>, <code>\"8GB\"</code>)","text":"<p>Accept a string with an explicit unit suffix, plus raw <code>int</code> for backward compatibility:</p> <pre><code>max_gpu_mem: str | int | None = Field(default=None)\n</code></pre> <p>Supports patterns like <code>\"90%\"</code>, <code>\"8GB\"</code>, <code>\"4096MB\"</code>, and bare <code>int</code> (bytes).</p> Pros Very user-friendly from config files and env vars (<code>FT_MAX_GPU_MEM=90%</code>); self-documenting values; extensible to other units. Cons Custom parser required; easy to mistype (<code>\"90 %\"</code>, <code>\"8 GB\"</code>); pydantic validation becomes entirely custom; breaking rename; string parsing adds complexity and error surface."},{"location":"adr/0004-percentage-based-gpu-memory-limit/#option-d-replace-with-max_gpu_mem_fraction-only-breaking","title":"Option D: Replace with <code>max_gpu_mem_fraction</code> only (breaking)","text":"<p>Remove the bytes field entirely:</p> <pre><code>max_gpu_mem_fraction: float | None = Field(default=None, gt=0.0, le=1.0)\n</code></pre> Pros Simplest API; no ambiguity; single field with clear semantics. Cons Breaking change \u2014 users with absolute byte limits lose that capability; percentage-based limits are less precise when exact byte control is needed; existing configs and env vars stop working."},{"location":"adr/0004-percentage-based-gpu-memory-limit/#comparison-matrix","title":"Comparison Matrix","text":"Criteria A (additive) B (union) C (string) D (replace) Backward compatible Yes No No No Strategy code changes None None None None Config surface Two fields One field One field One field Env var clarity Clear Ambiguous Clear Clear JSON/YAML safe Yes No (int/float) Yes Yes Parsing complexity Low Medium High Low Absolute byte support Yes Yes Yes No"},{"location":"adr/0004-percentage-based-gpu-memory-limit/#consequences","title":"Consequences","text":""},{"location":"adr/0004-percentage-based-gpu-memory-limit/#positive","title":"Positive","text":"<ul> <li>Users can specify GPU memory limits as a portable fraction, making configs reusable across   different GPU SKUs.</li> <li>Aligns with the mental model of \"use X% of my GPU\" which is how most users think about memory   budgets.</li> <li>Resolution to bytes is a single point of logic in <code>OffloadManager</code>, keeping strategies unchanged.</li> <li>Default <code>0.9</code> puts all configs in memory mode out of the box, reducing accidental VRAM exhaustion.</li> </ul>"},{"location":"adr/0004-percentage-based-gpu-memory-limit/#negative","title":"Negative","text":"<ul> <li>Fraction-based limits require a CUDA device to be available at config resolution time (not at   config construction time), adding a runtime dependency.</li> <li>The default behavior change (from <code>None</code>/latency mode to <code>0.9</code>/memory mode) may surprise users   upgrading from earlier versions. Documented explicitly in the CHANGELOG.</li> </ul>"},{"location":"adr/0004-percentage-based-gpu-memory-limit/#neutral","title":"Neutral","text":"<ul> <li>The <code>target_gpu_mem_bytes</code> field may eventually need the same treatment (fraction support).</li> <li>The SHM namespace hash using resolved bytes (not raw fraction) means two processes targeting the   same fraction on different GPU SKUs will produce different namespaces \u2014 correct behavior for   fleet deployments with mixed hardware.</li> </ul>"},{"location":"adr/0004-percentage-based-gpu-memory-limit/#references","title":"References","text":"<ul> <li><code>flextensor/config.py</code> \u2014 <code>OffloadConfig.max_gpu_mem_bytes</code> field definition</li> <li><code>flextensor/offload_manager.py</code> \u2014 strategy construction in <code>_initialize_tensor_manager()</code></li> <li><code>flextensor/strategy/utils.py</code> \u2014 <code>validate_memory_params()</code> shared validation</li> <li><code>torch.cuda.mem_get_info()</code> / <code>torch.cuda.get_device_properties()</code> \u2014 runtime GPU memory query</li> </ul>"},{"location":"adr/template/","title":"Template","text":""},{"location":"adr/template/#adr-nnnn-short-title-of-solved-problem-and-solution","title":"ADR-NNNN: [Short Title of Solved Problem and Solution]","text":"<p>Date: YYYY-MM-DD</p> <p>Status: [Proposed | Accepted | Deprecated | Superseded by ADR-XXXX]</p>"},{"location":"adr/template/#context","title":"Context","text":"<p>What is the issue that we're seeing that is motivating this decision or change?</p> <ul> <li>Describe the forces at play (technical, political, social, project)</li> <li>What constraints exist?</li> <li>What is the current situation?</li> <li>Why does this matter?</li> </ul> <p>Keep this section factual and objective.</p>"},{"location":"adr/template/#decision","title":"Decision","text":"<p>What is the change that we're proposing and/or doing?</p> <ul> <li>State the decision clearly and concisely</li> <li>Explain the reasoning behind the decision</li> <li>Describe the implementation approach if relevant</li> <li>Include any key principles or guidelines that informed the choice</li> </ul> <p>Use active voice: \"We will...\", \"We chose to...\", \"We decided...\"</p>"},{"location":"adr/template/#alternatives-considered","title":"Alternatives Considered","text":"<p>What other options did we evaluate? Why did we reject them?</p> <p>For each alternative: - Describe the approach - List its advantages (pros) - List its disadvantages (cons) - Explain why it wasn't chosen</p> <p>This section helps future readers understand the decision space and prevents revisiting settled discussions.</p>"},{"location":"adr/template/#consequences","title":"Consequences","text":"<p>What becomes easier or more difficult to do because of this change?</p>"},{"location":"adr/template/#positive","title":"Positive","text":"<ul> <li>What benefits does this decision provide?</li> <li>What problems does it solve?</li> <li>What becomes easier?</li> </ul>"},{"location":"adr/template/#negative","title":"Negative","text":"<ul> <li>What drawbacks or challenges does this introduce?</li> <li>What becomes more difficult?</li> <li>What risks are we accepting?</li> </ul>"},{"location":"adr/template/#neutral","title":"Neutral","text":"<ul> <li>What changes but is neither clearly positive nor negative?</li> <li>What trade-offs are we making?</li> </ul>"},{"location":"adr/template/#references","title":"References","text":"<ul> <li>Links to related issues, pull requests, or external documentation</li> <li>Related ADRs</li> <li>Research or articles that informed the decision</li> </ul>"},{"location":"api/","title":"Overview","text":""},{"location":"api/#api-reference","title":"API Reference","text":"<p>Technical reference for FlexTensor's public API. All pages below are auto-generated from source docstrings.</p>"},{"location":"api/#simplified-api","title":"Simplified API","text":"<p>Module-level convenience functions \u2014 the recommended starting point for most users.</p> <ul> <li><code>DEFAULT_MANAGER_NAME</code> \u2014 Name used by <code>get_offload_manager()</code> when no explicit name is provided (value: <code>\"default\"</code>)</li> <li><code>init()</code> \u2014 Pre-initialize the tensor manager (optional, called automatically by <code>offload()</code>)</li> <li><code>offload()</code> \u2014 Patch a model for automatic tensor offloading</li> <li><code>get_offload_manager()</code> \u2014 Get or create an offload manager singleton</li> <li><code>set_config()</code> \u2014 Set configuration for an offload manager</li> <li><code>offload_block()</code> \u2014 Context manager for manual offloading control</li> <li><code>get_gpu_memory_usage()</code> \u2014 Get GPU memory usage (inference mode only)</li> <li><code>save_profile()</code> / <code>load_profile()</code> \u2014 Profile persistence</li> <li><code>release()</code> \u2014 Release resources and restore model</li> <li><code>load_model_from_profile()</code> \u2014 Load a model from a saved profile with optimized weight loading</li> </ul>"},{"location":"api/#offloadmanager","title":"OffloadManager","text":"<p>The <code>OffloadManager</code> class provides full control over the offloading lifecycle. Obtain an instance via <code>get_offload_manager()</code>.</p>"},{"location":"api/#configuration","title":"Configuration","text":"<ul> <li><code>OffloadConfig</code> \u2014 All offloading parameters with sensible defaults</li> <li><code>load_config()</code> \u2014 Load config from files, env vars, and kwargs</li> <li><code>GPUMemoryUsage</code> \u2014 GPU memory usage breakdown</li> </ul>"},{"location":"api/#strategies","title":"Strategies","text":"<p>Offloading strategies for <code>OffloadConfig.load_strategy</code>:</p> <ul> <li><code>Strategy</code> \u2014 Protocol all strategies implement</li> <li><code>KnapsackStrategy</code> \u2014 Dynamic programming optimization (default)</li> <li><code>GreedyStrategy</code> \u2014 Largest tensors first</li> <li><code>GlobalOffloadStrategy</code> \u2014 Global optimization across layers</li> <li>And more...</li> </ul>"},{"location":"api/#advanced","title":"Advanced","text":"<p>Lower-level components for custom workflows:</p> <ul> <li><code>TensorManager</code> \u2014 Direct control over warmup, profiling, and inference phases</li> <li><code>TensorBenchmarkMode</code> \u2014 Benchmark mode base class</li> <li><code>BenchmarkReplace</code> / <code>PreloadToDevice</code> \u2014 Benchmark implementations</li> </ul>"},{"location":"api/advanced/","title":"Advanced","text":""},{"location":"api/advanced/#advanced-api","title":"Advanced API","text":"<p>Lower-level components for custom workflows. In most cases, use the Simplified API or <code>OffloadManager</code> instead.</p> <p><code>TensorManager</code> provides direct control over warmup, profiling, and inference phases. See Internal States for details on how these components work together.</p>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager","title":"TensorManager","text":"<pre><code>TensorManager(device_gpu, pinned_memory, tensor_manager_load_strategy, release_tensors=True, benchmark_cls: type[TensorBenchmarkMode] = BenchmarkReplace, direct_mode=True, use_trace_tensor=False, loader_type='allocation_block_transfer', remove_layers_operations=None, rearrange_transfers=False, min_compute_transfer_gap=1, blocks=4, move_top_level_buffers_to_gpu=True, use_shm=False, enable_untraced_tensor_discovery=True, enable_module_tracker=True, enable_diagnostics: bool = False)\n</code></pre> <p>Initialize TensorManager with configurable tensor loading strategy.</p> PARAMETER DESCRIPTION <code>device_gpu</code> <p>GPU device to use</p> <p> </p> <code>pinned_memory</code> <p>Whether to use pinned memory</p> <p> </p> <code>tensor_manager_load_strategy</code> <p>Strategy for loading tensors</p> <p> </p> <code>release_tensors</code> <p>Whether to release tensors after use</p> <p> DEFAULT: <code>True</code> </p> <code>benchmark_cls</code> <p>Benchmark class to use</p> <p> TYPE: <code>type[TensorBenchmarkMode]</code> DEFAULT: <code>BenchmarkReplace</code> </p> <code>direct_mode</code> <p>Whether to enable direct mode</p> <p> DEFAULT: <code>True</code> </p> <code>use_trace_tensor</code> <p>Whether to use trace tensor functionality</p> <p> DEFAULT: <code>False</code> </p> <code>loader_type</code> <p>Type of tensor loader to use. Options: - \"strategy\": Uses TensorStrategyLoader - \"raw_block_transfer\": Uses PreallocatedBatchTransferTensorLoader with RawBlockController - \"allocation_block_transfer\": Uses PreallocatedBatchTransferTensorLoader with AllocationBlockController</p> <p> DEFAULT: <code>'allocation_block_transfer'</code> </p> <code>remove_layers_operations</code> <p>List of operations to remove layers from the strategy map</p> <p> DEFAULT: <code>None</code> </p> <code>rearrange_transfers</code> <p>Whether to enable transfer rearrangement optimization (default: True)</p> <p> DEFAULT: <code>False</code> </p> <code>min_compute_transfer_gap</code> <p>Minimum gap between compute and transfer layers for optimization (default: 1)</p> <p> DEFAULT: <code>1</code> </p> <code>blocks</code> <p>Number of blocks to use for the block transfer loaders (default: 4)</p> <p> DEFAULT: <code>4</code> </p> <code>move_top_level_buffers_to_gpu</code> <p>Whether to move top-level model buffers to GPU during warmup initialization (default: True)</p> <p> DEFAULT: <code>True</code> </p> <code>enable_untraced_tensor_discovery</code> <p>Whether to discover untraced tensors (e.g., FP8 weights passed to Triton kernels) using discovery strategies (default: True)</p> <p> DEFAULT: <code>True</code> </p> <code>enable_module_tracker</code> <p>Whether to enable ModuleTracker for manual traps when forward patching is not used (default: True)</p> <p> DEFAULT: <code>True</code> </p> <code>enable_diagnostics</code> <p>Whether to log diagnostic information (layer duration statistics, block assignment table) after strategy computation (default: False)</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.set_model","title":"set_model","text":"<pre><code>set_model(model)\n</code></pre>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.initialize_warmup","title":"initialize_warmup","text":"<pre><code>initialize_warmup()\n</code></pre>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.initialize_profile","title":"initialize_profile","text":"<pre><code>initialize_profile()\n</code></pre>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.initialize_inference","title":"initialize_inference","text":"<pre><code>initialize_inference()\n</code></pre>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.prepare_warmup_mode","title":"prepare_warmup_mode","text":"<pre><code>prepare_warmup_mode()\n</code></pre>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.prepare_profile_mode","title":"prepare_profile_mode","text":"<pre><code>prepare_profile_mode()\n</code></pre>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.prepare_infer_mode","title":"prepare_infer_mode","text":"<pre><code>prepare_infer_mode()\n</code></pre> <p>Prepare inference mode from fresh profiling data.</p> <p>This method is called after profiling to compute the load strategy and create the appropriate loader for inference.</p>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.trap","title":"trap","text":"<pre><code>trap(name)\n</code></pre>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.release_memory","title":"release_memory","text":"<pre><code>release_memory()\n</code></pre>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.get_gpu_memory_usage","title":"get_gpu_memory_usage","text":"<pre><code>get_gpu_memory_usage() -&gt; GPUMemoryUsage\n</code></pre> <p>Get GPU memory usage by FlexTensor in inference mode.</p> <p>Returns the memory used by GPU transfer blocks and unmapped tensors that were moved to GPU. This method should be called after the manager has transitioned to inference mode (after warmup and profile phases).</p> RETURNS DESCRIPTION <code>GPUMemoryUsage</code> <p>Memory breakdown with per-component bytes and MB values. See <code>GPUMemoryUsage</code> for field details.</p> <p> TYPE: <code>GPUMemoryUsage</code> </p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If called before inference mode is initialized (tensor_layer_loader is None)</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; tensor_manager.initialize_inference()\n&gt;&gt;&gt; usage = tensor_manager.get_gpu_memory_usage()\n&gt;&gt;&gt; print(f\"GPU blocks: {usage.blocks_mb:.2f} MB\")\n&gt;&gt;&gt; print(f\"Unmapped tensors: {usage.unmapped_tensors_mb:.2f} MB\")\n&gt;&gt;&gt; print(f\"Total: {usage.total_mb:.2f} MB\")\n</code></pre>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.get_memory_transfer_stats","title":"get_memory_transfer_stats","text":"<pre><code>get_memory_transfer_stats() -&gt; dict[int, float] | None\n</code></pre> <p>Get the memory transfer statistics computed during profiling.</p> RETURNS DESCRIPTION <code>dict[int, float] | None</code> <p>Dictionary mapping tensor size (bytes) to transfer time (ms),</p> <code>dict[int, float] | None</code> <p>or None if profiling has not yet completed.</p>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.benchmark_context","title":"benchmark_context","text":"<pre><code>benchmark_context(iterations: int = 10) -&gt; Any\n</code></pre> <p>Create a benchmarking context that automatically integrates results into TensorManager.</p> <p>This method provides a context manager that creates a benchmark instance using the configured benchmark_cls, and automatically copies the benchmark results to the TensorManager's stats attributes upon context exit.</p> PARAMETER DESCRIPTION <code>iterations</code> <p>Number of benchmark iterations to perform</p> <p> TYPE: <code>int</code> DEFAULT: <code>10</code> </p> RETURNS DESCRIPTION <code>Any</code> <p>Context manager that yields the benchmark instance (TensorBenchmarkMode).</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; with tensor_manager.benchmark_context() as benchmark:\n...     model = {name: layer.to(torch.bfloat16) for name, layer in model.items()}\n&gt;&gt;&gt; # Stats are automatically integrated into tensor_manager\n</code></pre>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.run_profile_suite","title":"run_profile_suite","text":"<pre><code>run_profile_suite(callback, model=None, direct_mode=True)\n</code></pre> <p>Run all three phases (warmup, profile, direct_mode) in sequence with a callback.</p> <p>This method automates the three-phase process: 1. Warmup phase: Collects initial tensor statistics 2. Profile phase: Collects detailed layer statistics with tensor loading 3. Direct mode phase: Profiles with direct tensor access (if enabled)</p> PARAMETER DESCRIPTION <code>callback</code> <p>Function to call for each phase. Should accept model as argument.</p> <p> </p> <code>model</code> <p>The model to prepare</p> <p> DEFAULT: <code>None</code> </p> <code>direct_mode</code> <p>Whether to enable direct mode profiling</p> <p> DEFAULT: <code>True</code> </p> RETURNS DESCRIPTION <p>The inference results</p>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.save_profile","title":"save_profile","text":"<pre><code>save_profile(profile_directory: str) -&gt; None\n</code></pre> <p>Save current profile to directory.</p> PARAMETER DESCRIPTION <code>profile_directory</code> <p>Directory to save profile to</p> <p> TYPE: <code>str</code> </p>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.load_profile","title":"load_profile","text":"<pre><code>load_profile(profile_directory: str, model) -&gt; None\n</code></pre> <p>Load profile from directory and restore state to model.</p> <p>This method loads a saved TensorManagerState and restores it to the manager, but does NOT configure loaders or finalize the manager for inference. It only sets the internal tensor_manager_state and associates the model.</p> <p>After calling this method, you MUST call the following methods in order:</p> <ol> <li> <p>initialize_warmup() - Configures tensor layer loaders via prepare_infer_load_mode()    and patches the model with inference traps via prepare_final_model().</p> </li> <li> <p>initialize_profile() - Prepares the model for profiling mode. When a saved state    exists, this is a no-op and returns the model as-is. However, if there are changes    that require re-profiling, this method handles the profile mode setup.</p> </li> <li> <p>initialize_inference() - Finalizes the manager for inference mode and releases    the internal model reference (sets self.model = None) to free memory.</p> </li> </ol> Example <p>tm = TensorManager(device_gpu=\"cuda:0\", ...) tm.load_profile(\"/path/to/profile\", model) model = tm.initialize_warmup()  # Required: configures loaders and patches model model = tm.initialize_profile()  # Prepares profiling (no-op if state exists) model = tm.initialize_inference()  # Releases model reference</p> PARAMETER DESCRIPTION <code>profile_directory</code> <p>Directory containing the profile.</p> <p> TYPE: <code>str</code> </p> <code>model</code> <p>Model to restore state to. The model should match the architecture used when the profile was originally saved.</p> <p> </p> Note <p>Future versions may add an optional parameter to automatically run finalization (initialize_warmup, initialize_profile, initialize_inference) after loading the profile.</p>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.load_profile--now-the-model-is-ready-for-inference","title":"Now the model is ready for inference","text":""},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.load_state","title":"load_state","text":"<pre><code>load_state(profile_directory: str)\n</code></pre> <p>Load profile state from directory without restoring.</p> <p>Use this when you need access to the state object before restoring, e.g., to read state.gpu_tensors_names or state.view_tensors_names.</p> PARAMETER DESCRIPTION <code>profile_directory</code> <p>Directory containing the profile</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>TensorManagerState</code> <p>The loaded state object</p>"},{"location":"api/advanced/#flextensor.tensor_manager.TensorManager.restore_state","title":"restore_state","text":"<pre><code>restore_state(model, state)\n</code></pre> <p>Delegate to TensorManagerStateHandler for state restoration.</p>"},{"location":"api/advanced/#benchmark-modes","title":"Benchmark Modes","text":"<p>These are used internally by <code>OffloadManager</code> and exposed for users who need custom benchmark instrumentation.</p>"},{"location":"api/advanced/#flextensor.benchmark_tensor_mode.TensorBenchmarkMode","title":"TensorBenchmarkMode","text":"<pre><code>TensorBenchmarkMode(device_gpu: device, pinned_memory: bool = True, iterations: int = 10, pinned_memory_limit_mb: float | None = None)\n</code></pre> <p>               Bases: <code>TorchFunctionMode</code>, <code>ABC</code></p> <p>Abstract base class for tensor benchmarking modes.</p> <p>This class defines the common interface that all tensor benchmark implementations must follow to be used interchangeably as benchmark_cls parameter in TensorManager.</p> <p>Initialize tensor benchmarking mode.</p> PARAMETER DESCRIPTION <code>device_gpu</code> <p>Target GPU device for benchmarking</p> <p> TYPE: <code>device</code> </p> <code>pinned_memory</code> <p>Whether to use pinned memory for tensors</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>iterations</code> <p>Number of benchmark iterations</p> <p> TYPE: <code>int</code> DEFAULT: <code>10</code> </p> <code>pinned_memory_limit_mb</code> <p>Maximum size of tensors that will be copied to pinned_memory</p> <p> TYPE: <code>float | None</code> DEFAULT: <code>None</code> </p>"},{"location":"api/advanced/#flextensor.benchmark_tensor_mode.TensorBenchmarkMode.get_results","title":"get_results  <code>abstractmethod</code>","text":"<pre><code>get_results() -&gt; dict[str, Any]\n</code></pre> <p>Get benchmark results.</p> RETURNS DESCRIPTION <code>dict[str, Any]</code> <p>Dictionary containing benchmark results with keys:</p> <code>dict[str, Any]</code> <ul> <li>tensor_statistics_map: Mapping of tensor IDs to statistics</li> </ul> <code>dict[str, Any]</code> <ul> <li>tensors_map: Mapping of tensor IDs to tensors</li> </ul>"},{"location":"api/advanced/#flextensor.benchmark_tensor_mode.BenchmarkReplace","title":"BenchmarkReplace","text":"<pre><code>BenchmarkReplace(device_gpu: device, pinned_memory: bool = True, iterations: int = 10, pinned_memory_limit_mb: float | None = None)\n</code></pre> <p>               Bases: <code>TensorBenchmarkMode</code></p> <p>Benchmark mode that measures tensor transfer times and collects statistics.</p> <p>This implementation intercepts tensor operations, measures transfer times from CPU to GPU, and collects comprehensive statistics about tensor usage.</p> <p>Initialize BenchmarkReplace for tensor benchmarking.</p> PARAMETER DESCRIPTION <code>device_gpu</code> <p>Target GPU device for benchmarking</p> <p> TYPE: <code>device</code> </p> <code>pinned_memory</code> <p>Whether to use pinned memory for tensors</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>iterations</code> <p>Number of benchmark iterations</p> <p> TYPE: <code>int</code> DEFAULT: <code>10</code> </p> <code>pinned_memory_limit_mb</code> <p>Maximum size of tensors that will be copied to pinned_memory</p> <p> TYPE: <code>float | None</code> DEFAULT: <code>None</code> </p>"},{"location":"api/advanced/#flextensor.benchmark_tensor_mode.BenchmarkReplace.get_results","title":"get_results","text":"<pre><code>get_results() -&gt; dict[str, Any]\n</code></pre> <p>Get benchmark results.</p> RETURNS DESCRIPTION <code>dict[str, Any]</code> <p>Dictionary containing benchmark results with keys:</p> <code>dict[str, Any]</code> <ul> <li>tensor_statistics_map: Mapping of tensor IDs to statistics</li> </ul> <code>dict[str, Any]</code> <ul> <li>tensors_map: Mapping of tensor IDs to tensors</li> </ul>"},{"location":"api/advanced/#flextensor.benchmark_tensor_mode.PreloadToDevice","title":"PreloadToDevice","text":"<pre><code>PreloadToDevice(device_gpu: device, pinned_memory: bool = True, iterations: int = 10, pinned_memory_limit_mb: float | None = None)\n</code></pre> <p>               Bases: <code>TensorBenchmarkMode</code></p> <p>Benchmark mode that immediately transfers tensors to GPU without timing measurements.</p> <p>This implementation provides a simple preloading strategy without collecting detailed benchmark statistics, suitable for scenarios where you just want tensors moved to GPU without measurement overhead.</p> <p>Initialize PreloadToDevice for simple tensor preloading.</p> PARAMETER DESCRIPTION <code>device_gpu</code> <p>Target GPU device for preloading</p> <p> TYPE: <code>device</code> </p> <code>pinned_memory</code> <p>Whether to use pinned memory for tensors (ignored in this implementation)</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>iterations</code> <p>Number of benchmark iterations (ignored in this implementation)</p> <p> TYPE: <code>int</code> DEFAULT: <code>10</code> </p> <code>pinned_memory_limit_mb</code> <p>Maximum size of tensors that will be copied to pinned_memory</p> <p> TYPE: <code>float | None</code> DEFAULT: <code>None</code> </p>"},{"location":"api/advanced/#flextensor.benchmark_tensor_mode.PreloadToDevice.get_results","title":"get_results","text":"<pre><code>get_results() -&gt; dict[str, Any]\n</code></pre> <p>Get benchmark results.</p> RETURNS DESCRIPTION <code>dict[str, Any]</code> <p>Dictionary containing benchmark results with keys:</p> <code>dict[str, Any]</code> <ul> <li>tensor_statistics_map: Mapping of tensor IDs to statistics (empty for this implementation)</li> </ul> <code>dict[str, Any]</code> <ul> <li>tensors_map: Mapping of tensor IDs to tensors</li> </ul>"},{"location":"api/configuration/","title":"Configuration","text":""},{"location":"api/configuration/#configuration","title":"Configuration","text":"<p>Configuration classes and loading utilities for FlexTensor offloading behavior.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig","title":"OffloadConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Configuration for offload manager.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.enabled","title":"enabled  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>enabled: bool = Field(default=True)\n</code></pre> <p>Whether to enable offload.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.gpu_device","title":"gpu_device  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>gpu_device: int = Field(default=0, ge=0)\n</code></pre> <p>GPU device index to use.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.pinned_memory","title":"pinned_memory  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>pinned_memory: bool = Field(default=True)\n</code></pre> <p>Whether to use pinned memory for CPU-to-GPU transfers.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.use_shared_memory","title":"use_shared_memory  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>use_shared_memory: Annotated[bool, deprecated(_USE_SHARED_MEMORY_MSG)] = Field(default=False, deprecated=_USE_SHARED_MEMORY_MSG)\n</code></pre> <p>Whether to use shared memory.</p> <p>.. deprecated:: v0.4     Use :attr:<code>shm_enabled</code> instead. Will be removed in v0.5.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.shm_enabled","title":"shm_enabled  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>shm_enabled: bool = Field(default=False)\n</code></pre> <p>Enable cross-process weight sharing via POSIX shared memory.</p> <p>When True, model weights are stored in shared memory so multiple processes (e.g., vLLM data-parallel replicas) share a single copy in CPU RAM. Replaces the deprecated <code>use_shared_memory</code> field. Can also be set via the <code>FT_SHM_ENABLED</code> env var.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.shm_namespace","title":"shm_namespace  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>shm_namespace: str | None = Field(default=None)\n</code></pre> <p>Base namespace for shared memory blocks.</p> <p>If None, auto-derived from model path + config hash. Can also be set via FT_SHM_NAMESPACE env var.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.shm_wait_timeout","title":"shm_wait_timeout  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>shm_wait_timeout: float = Field(default=0.0, ge=0.0)\n</code></pre> <p>Hard wall-clock timeout (seconds) for followers waiting on creator.</p> <p>0 means no hard limit \u2014 rely on heartbeat liveness detection only. Can also be set via FT_SHM_WAIT_TIMEOUT env var.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.warmup_iters","title":"warmup_iters  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>warmup_iters: int = Field(default=1, ge=0)\n</code></pre> <p>Number of warmup iterations before profiling begins.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.profile_iters","title":"profile_iters  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>profile_iters: int = Field(default=10, ge=0)\n</code></pre> <p>Number of profiling iterations to collect statistics.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.knapsack_scale","title":"knapsack_scale  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>knapsack_scale: float = Field(default=1.0, gt=0.0)\n</code></pre> <p>Knapsack algorithm scale factor.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.transfer_mode","title":"transfer_mode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>transfer_mode: str = Field(default='allocation_block_transfer')\n</code></pre> <p>Transfer strategy name.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.enable_direct_mode","title":"enable_direct_mode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>enable_direct_mode: bool = Field(default=True)\n</code></pre> <p>Whether to use direct tensor access.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.release_tensors","title":"release_tensors  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>release_tensors: bool = Field(default=True)\n</code></pre> <p>Whether to release GPU memory after layer execution.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.rearrange_transfers","title":"rearrange_transfers  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>rearrange_transfers: bool = Field(default=False)\n</code></pre> <p>Whether to optimize transfer scheduling.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.compute_transfer_gap","title":"compute_transfer_gap  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>compute_transfer_gap: int = Field(default=1, ge=0)\n</code></pre> <p>Gap between compute and transfer layers for pipelining.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.num_blocks","title":"num_blocks  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>num_blocks: int = Field(default=4, gt=0)\n</code></pre> <p>Number of memory blocks for block transfer loaders.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.min_blocks","title":"min_blocks  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>min_blocks: int = Field(default=4, ge=2)\n</code></pre> <p>Minimum number of blocks for assignment optimization search range.</p> <p>Lower values give the optimizer more freedom and reduce GPU memory used by FlexTensor's memory blocks, but may hurt pipelining throughput. Must be &gt;= 2 (pipelined execution requires at least two blocks). Can also be set via the <code>FT_MIN_BLOCKS</code> env var.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.max_gpu_mem_fraction","title":"max_gpu_mem_fraction  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_gpu_mem_fraction: float | None = Field(default=_DEFAULT_MAX_GPU_MEM_FRACTION, gt=0.0, le=1.0)\n</code></pre> <p>Target maximum GPU memory as a fraction of total device memory (e.g. 0.9 = 90%).</p> <p>When set, the strategy switches to memory mode and keeps peak GPU usage within this budget. When <code>None</code>, the strategy operates in latency mode (no memory constraint). Defaults to 0.9. Can also be set via the <code>FT_MAX_GPU_MEM_FRACTION</code> env var.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.max_gpu_mem_bytes","title":"max_gpu_mem_bytes  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_gpu_mem_bytes: Annotated[int | None, deprecated(_MAX_GPU_MEM_BYTES_MSG)] = Field(default=None, ge=0, deprecated=_MAX_GPU_MEM_BYTES_MSG)\n</code></pre> <p>Hard GPU memory limit in bytes (never exceed).</p> <p>.. deprecated:: v0.4     Use :attr:<code>max_gpu_mem_fraction</code> instead. Will be removed in v0.5.     Env var <code>FT_MAX_GPU_MEM_BYTES</code> is similarly deprecated; use <code>FT_MAX_GPU_MEM_FRACTION</code>.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.profile_storage_dir","title":"profile_storage_dir  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>profile_storage_dir: str | None = Field(default=None)\n</code></pre> <p>Directory for profile persistence.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.profile_read_only","title":"profile_read_only  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>profile_read_only: bool = Field(default=False)\n</code></pre> <p>Only load profiles, saving is disabled.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.load_strategy","title":"load_strategy  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>load_strategy: Annotated[Strategy, WithJsonSchema({type: object})] | None = Field(default=None)\n</code></pre> <p>Override automatic strategy selection.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.enable_tracing","title":"enable_tracing  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>enable_tracing: bool = Field(default=False)\n</code></pre> <p>Whether to enable tensor tracing for debugging.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.enable_instrumentation","title":"enable_instrumentation  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>enable_instrumentation: bool = Field(default=False)\n</code></pre> <p>Whether to capture component init args for debugging.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.instrumentation_output_dir","title":"instrumentation_output_dir  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>instrumentation_output_dir: str = Field(default='.flextensor/instrumentation')\n</code></pre> <p>Instrumentation output directory.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.module_patterns","title":"module_patterns  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>module_patterns: list[str] = Field(default_factory=lambda: ['*'])\n</code></pre> <p>Module path patterns to offload (supports <code>*</code> and <code>?</code> wildcards).</p> <p>Can also be set via the <code>FT_MODULE_PATTERNS</code> env var as a comma-separated list.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.enable_untraced_tensor_discovery","title":"enable_untraced_tensor_discovery  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>enable_untraced_tensor_discovery: bool = Field(default=True)\n</code></pre> <p>Whether to discover untraced tensors (e.g. FP8 weights passed to Triton kernels).</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.enable_module_tracker","title":"enable_module_tracker  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>enable_module_tracker: bool = Field(default=True)\n</code></pre> <p>Whether to enable ModuleTracker for manual traps when forward patching is not used.</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.enable_diagnostics","title":"enable_diagnostics  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>enable_diagnostics: bool = Field(default=False)\n</code></pre> <p>Whether to log diagnostic information (layer duration statistics, block assignment table) after strategy computation</p>"},{"location":"api/configuration/#flextensor.config.OffloadConfig.all_warmup_iters","title":"all_warmup_iters  <code>property</code>","text":"<pre><code>all_warmup_iters: int\n</code></pre> <p>Total warmup iterations including profile iterations.</p>"},{"location":"api/configuration/#configuration-loading","title":"Configuration Loading","text":""},{"location":"api/configuration/#flextensor.config.load_config","title":"load_config","text":"<pre><code>load_config(config_path: str | Path | None = None, env_prefix: str = 'FT_', use_env: bool = True, config_file_env_var: str = 'FT_CONFIG_FILE', **kwargs) -&gt; OffloadConfig\n</code></pre> <p>Load OffloadConfig with precedence: file &lt; env vars &lt; kwargs.</p> <p>This is the unified configuration loading function that combines file and environment variable loading with proper precedence.</p> <p>Precedence order (highest wins):</p> <ol> <li>kwargs (explicit arguments)</li> <li>Environment variables (if use_env=True)</li> <li>Config file values</li> <li>OffloadConfig defaults</li> </ol> <p>When loading from environment only (no file), <code>enabled</code> defaults to False. You must explicitly set FT_ENABLED=1 to enable offloading.</p> <p>Supported file formats (auto-detected by extension):</p> <ul> <li>INI (.conf, .ini) \u2014 requires [flextensor] section</li> <li>JSON (.json)</li> <li>YAML (.yaml, .yml)</li> </ul> PARAMETER DESCRIPTION <code>config_path</code> <p>Path to configuration file. If None, checks config_file_env_var.</p> <p> TYPE: <code>str | Path | None</code> DEFAULT: <code>None</code> </p> <code>env_prefix</code> <p>Prefix for environment variable names (default: \"FT_\")</p> <p> TYPE: <code>str</code> DEFAULT: <code>'FT_'</code> </p> <code>use_env</code> <p>Whether to load and override with environment variables (default: True)</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>config_file_env_var</code> <p>Environment variable for config file path (default: \"FT_CONFIG_FILE\")</p> <p> TYPE: <code>str</code> DEFAULT: <code>'FT_CONFIG_FILE'</code> </p> <code>**kwargs</code> <p>Additional keyword arguments to override all other values</p> <p> DEFAULT: <code>{}</code> </p> RETURNS DESCRIPTION <code>OffloadConfig</code> <p>OffloadConfig instance</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; # Full integration: file + env + kwargs\n&gt;&gt;&gt; config = load_config(\"flextensor.conf\")\n&gt;&gt;&gt;\n&gt;&gt;&gt; # With env override: FT_GPU_DEVICE=2 overrides file value\n&gt;&gt;&gt; config = load_config(\"flextensor.conf\")\n&gt;&gt;&gt;\n&gt;&gt;&gt; # Env only (no file, enabled defaults to False)\n&gt;&gt;&gt; config = load_config()\n</code></pre>"},{"location":"api/configuration/#flextensor.config.load_config_from_file","title":"load_config_from_file","text":"<pre><code>load_config_from_file(config_path: str | Path | None = None, env_var: str = 'FT_CONFIG_FILE', **kwargs) -&gt; OffloadConfig\n</code></pre> <p>Load OffloadConfig from a configuration file (no env var override).</p> <p>This is a convenience wrapper around load_config() that loads from file only, without environment variable overrides.</p> <p>Supported file formats (auto-detected by extension):</p> <ul> <li>INI (.conf, .ini) \u2014 requires [flextensor] section</li> <li>JSON (.json)</li> <li>YAML (.yaml, .yml)</li> </ul> PARAMETER DESCRIPTION <code>config_path</code> <p>Path to configuration file. If None, uses env_var.</p> <p> TYPE: <code>str | Path | None</code> DEFAULT: <code>None</code> </p> <code>env_var</code> <p>Environment variable name for config file path (default: \"FT_CONFIG_FILE\")</p> <p> TYPE: <code>str</code> DEFAULT: <code>'FT_CONFIG_FILE'</code> </p> <code>**kwargs</code> <p>Additional keyword arguments to override file values</p> <p> DEFAULT: <code>{}</code> </p> RETURNS DESCRIPTION <code>OffloadConfig</code> <p>OffloadConfig instance with values from file and kwargs</p> RAISES DESCRIPTION <code>FileNotFoundError</code> <p>If the config file does not exist</p> <code>ValueError</code> <p>If the file format is not supported or file is invalid</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; config = load_config_from_file(\"flextensor.conf\")\n&gt;&gt;&gt; config.enabled\nTrue\n</code></pre>"},{"location":"api/configuration/#flextensor.config.load_config_from_env","title":"load_config_from_env","text":"<pre><code>load_config_from_env(prefix: str = 'FT_', **kwargs) -&gt; OffloadConfig\n</code></pre> <p>Load OffloadConfig from environment variables (no file).</p> <p>This is a convenience wrapper around load_config() that loads from environment variables only, without file loading.</p> <p>Environment variables should be named as {prefix}{FIELD_NAME} in uppercase. For example, with the default prefix \"FT_\":</p> <ul> <li>FT_ENABLED=1 (required to enable offloading)</li> <li>FT_GPU_DEVICE=1</li> <li>FT_WARMUP_ITERS=5</li> </ul> <p>Note: When loading from environment, <code>enabled</code> defaults to False. You must explicitly set FT_ENABLED=1 to enable offloading.</p> <p>Type conversions:</p> <ul> <li>bool: Case-insensitive parsing of \"true\"/\"false\", \"1\"/\"0\", \"yes\"/\"no\", etc.</li> <li>int: Direct integer conversion</li> <li>float: Direct float conversion</li> <li>str: Used as-is</li> <li>None: Case-insensitive \"none\" or \"null\"</li> </ul> PARAMETER DESCRIPTION <code>prefix</code> <p>Prefix for environment variable names (default: \"FT_\")</p> <p> TYPE: <code>str</code> DEFAULT: <code>'FT_'</code> </p> <code>**kwargs</code> <p>Additional keyword arguments to override environment variables</p> <p> DEFAULT: <code>{}</code> </p> RETURNS DESCRIPTION <code>OffloadConfig</code> <p>OffloadConfig instance with values from environment variables and kwargs</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import os\n&gt;&gt;&gt; os.environ[\"FT_ENABLED\"] = \"1\"\n&gt;&gt;&gt; os.environ[\"FT_GPU_DEVICE\"] = \"1\"\n&gt;&gt;&gt; config = load_config_from_env()\n&gt;&gt;&gt; config.enabled\nTrue\n&gt;&gt;&gt; config.gpu_device\n1\n</code></pre>"},{"location":"api/configuration/#types","title":"Types","text":""},{"location":"api/configuration/#flextensor.types.GPUMemoryUsage","title":"GPUMemoryUsage  <code>dataclass</code>","text":"<pre><code>GPUMemoryUsage(blocks_bytes: int, unmapped_tensors_bytes: int, total_bytes: int)\n</code></pre> <p>GPU memory usage by FlexTensor in inference mode.</p> <p>This dataclass provides a breakdown of GPU memory allocated by FlexTensor for tensor offloading operations. It includes memory used for transfer blocks and unmapped tensors that are moved to GPU.</p> ATTRIBUTE DESCRIPTION <code>blocks_bytes</code> <p>Memory in bytes used by GPU transfer blocks for CPU-to-GPU tensor transfers.</p> <p> TYPE: <code>int</code> </p> <code>unmapped_tensors_bytes</code> <p>Memory in bytes used by tensors that are not managed by the offload strategy and were moved directly to GPU.</p> <p> TYPE: <code>int</code> </p> <code>total_bytes</code> <p>Total GPU memory in bytes used by FlexTensor.</p> <p> TYPE: <code>int</code> </p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import flextensor\n&gt;&gt;&gt; from flextensor import OffloadConfig\n&gt;&gt;&gt; om = flextensor.get_offload_manager()\n&gt;&gt;&gt; config = OffloadConfig(module_patterns=[\"layers.*\"])\n&gt;&gt;&gt; model = om.offload(model, config=config)\n&gt;&gt;&gt; # Run warmup and profile iterations...\n&gt;&gt;&gt; for _ in range(config.all_warmup_iters):\n...     model(input)\n&gt;&gt;&gt; # Now in inference mode, get memory usage\n&gt;&gt;&gt; usage = om.get_gpu_memory_usage()\n&gt;&gt;&gt; print(f\"FlexTensor using {usage.total_mb:.2f} MB GPU memory\")\n</code></pre>"},{"location":"api/configuration/#flextensor.types.GPUMemoryUsage.blocks_mb","title":"blocks_mb  <code>property</code>","text":"<pre><code>blocks_mb: float\n</code></pre> <p>Memory used by GPU transfer blocks in megabytes.</p>"},{"location":"api/configuration/#flextensor.types.GPUMemoryUsage.unmapped_tensors_mb","title":"unmapped_tensors_mb  <code>property</code>","text":"<pre><code>unmapped_tensors_mb: float\n</code></pre> <p>Memory used by unmapped tensors in megabytes.</p>"},{"location":"api/configuration/#flextensor.types.GPUMemoryUsage.total_mb","title":"total_mb  <code>property</code>","text":"<pre><code>total_mb: float\n</code></pre> <p>Total GPU memory used in megabytes.</p>"},{"location":"api/offload-manager/","title":"OffloadManager","text":""},{"location":"api/offload-manager/#offloadmanager","title":"OffloadManager","text":"<p>The <code>OffloadManager</code> class provides full control over the offloading lifecycle. Obtain an instance with <code>get_offload_manager()</code> rather than constructing one directly.</p>"},{"location":"api/offload-manager/#flextensor.offload_manager.OffloadManager","title":"OffloadManager","text":"<pre><code>OffloadManager(name: str)\n</code></pre> <p>Simplified offload manager API.</p> <p>Provides a high-level interface for tensor offloading with automatic state management, profiling, and module patching capabilities.</p>"},{"location":"api/offload-manager/#flextensor.offload_manager.OffloadManager.init","title":"init","text":"<pre><code>init(config: OffloadConfig | None = None)\n</code></pre>"},{"location":"api/offload-manager/#flextensor.offload_manager.OffloadManager.set_config","title":"set_config","text":"<pre><code>set_config(config: OffloadConfig)\n</code></pre> <p>Set configuration for the offload manager.</p> PARAMETER DESCRIPTION <code>config</code> <p>OffloadConfig instance with settings</p> <p> TYPE: <code>OffloadConfig</code> </p>"},{"location":"api/offload-manager/#flextensor.offload_manager.OffloadManager.offload","title":"offload","text":"<pre><code>offload(model: Module, config: OffloadConfig | None = None) -&gt; Module\n</code></pre> <p>Offload modules in the model based on config patterns.</p> <p>Patches modules matching config.module_patterns to automatically manage tensor transfers. Supports wildcards in module patterns.</p> PARAMETER DESCRIPTION <code>model</code> <p>PyTorch model to patch</p> <p> TYPE: <code>Module</code> </p> <code>config</code> <p>OffloadConfig to use for offload (patterns from config.module_patterns)</p> <p> TYPE: <code>OffloadConfig | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>Module</code> <p>A wrapper around the model that tracks state transitions</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; model = MyModel()\n&gt;&gt;&gt; om = get_offload_manager()\n&gt;&gt;&gt; config = OffloadConfig(module_patterns=[\"embed\", \"layers.*\", \"head\"])\n&gt;&gt;&gt; model = om.offload(model, config)\n</code></pre>"},{"location":"api/offload-manager/#flextensor.offload_manager.OffloadManager.offload_block","title":"offload_block","text":"<pre><code>offload_block(name: str) -&gt; Any\n</code></pre> <p>Create an offload block context manager.</p> PARAMETER DESCRIPTION <code>name</code> <p>Name of the offload block (used for tracking)</p> <p> TYPE: <code>str</code> </p> RETURNS DESCRIPTION <code>Any</code> <p>OffloadBlock context manager (Trap, WarmupTrap, TrapInfer, etc.)</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; with om.offload_block(\"encoder\"):\n...     output = encoder(input)\n</code></pre>"},{"location":"api/offload-manager/#flextensor.offload_manager.OffloadManager.get_gpu_memory_usage","title":"get_gpu_memory_usage","text":"<pre><code>get_gpu_memory_usage() -&gt; GPUMemoryUsage\n</code></pre> <p>Get GPU memory usage by FlexTensor in inference mode.</p> <p>Returns the memory used by GPU transfer blocks and unmapped tensors that were moved to GPU. This method should be called after the manager has transitioned to inference mode (after warmup and profile phases).</p> RETURNS DESCRIPTION <code>GPUMemoryUsage</code> <p>Memory breakdown with per-component bytes and MB values. See <code>GPUMemoryUsage</code> for field details.</p> <p> TYPE: <code>GPUMemoryUsage</code> </p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If called before inference mode (before all warmup and profile iterations have completed)</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; om = get_offload_manager()\n&gt;&gt;&gt; config = OffloadConfig(module_patterns=[\"layers.*\"])\n&gt;&gt;&gt; model = om.offload(model, config=config)\n&gt;&gt;&gt; # Run warmup and profile iterations\n&gt;&gt;&gt; for _ in range(config.all_warmup_iters):\n...     model(input)\n&gt;&gt;&gt; # Now in inference mode, get memory usage\n&gt;&gt;&gt; usage = om.get_gpu_memory_usage()\n&gt;&gt;&gt; print(f\"FlexTensor GPU memory: {usage.total_mb:.2f} MB\")\n&gt;&gt;&gt; print(f\"  Transfer blocks: {usage.blocks_mb:.2f} MB\")\n&gt;&gt;&gt; print(f\"  Unmapped tensors: {usage.unmapped_tensors_mb:.2f} MB\")\n</code></pre>"},{"location":"api/offload-manager/#flextensor.offload_manager.OffloadManager.save_profile","title":"save_profile","text":"<pre><code>save_profile(profile_directory: str | None = None) -&gt; None\n</code></pre> <p>Save current offload profile to directory.</p> PARAMETER DESCRIPTION <code>profile_directory</code> <p>Directory to save profile to. If None, uses config.profile_storage_dir.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If tensor manager is not initialized or no profile data exists.</p> <code>ValueError</code> <p>If no directory is provided and config.profile_storage_dir is not set, or if config.profile_read_only is True.</p>"},{"location":"api/offload-manager/#flextensor.offload_manager.OffloadManager.load_profile","title":"load_profile","text":"<pre><code>load_profile(profile_directory: str | None = None, model: Module | None = None) -&gt; None\n</code></pre> <p>Load offload profile from directory and restore state to model.</p> PARAMETER DESCRIPTION <code>profile_directory</code> <p>Directory containing the profile. If None, uses config.profile_storage_dir.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>model</code> <p>Model to restore state to. If None, uses the current model.</p> <p> TYPE: <code>Module | None</code> DEFAULT: <code>None</code> </p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If no model is available to restore state to.</p> <code>FileNotFoundError</code> <p>If profile directory or file doesn't exist.</p> <code>ValueError</code> <p>If no directory is provided and config.profile_storage_dir is not set.</p>"},{"location":"api/offload-manager/#flextensor.offload_manager.OffloadManager.release","title":"release","text":"<pre><code>release()\n</code></pre>"},{"location":"api/simplified/","title":"Simplified API","text":""},{"location":"api/simplified/#simplified-api","title":"Simplified API","text":"<p>Module-level convenience functions \u2014 the recommended way to use FlexTensor. These wrap the <code>OffloadManager</code> singleton for common use cases.</p>"},{"location":"api/simplified/#flextensor.offload_manager.DEFAULT_MANAGER_NAME","title":"DEFAULT_MANAGER_NAME  <code>module-attribute</code>","text":"<pre><code>DEFAULT_MANAGER_NAME: str = 'default'\n</code></pre> <p>Name used by :func:<code>get_offload_manager</code> when no explicit name is provided.</p>"},{"location":"api/simplified/#flextensor.offload_manager.init","title":"init","text":"<pre><code>init(config: OffloadConfig | None = None, name: str = DEFAULT_MANAGER_NAME)\n</code></pre>"},{"location":"api/simplified/#flextensor.offload_manager.offload","title":"offload","text":"<pre><code>offload(model: Module, config: OffloadConfig | None = None, name: str = DEFAULT_MANAGER_NAME) -&gt; Module\n</code></pre> <p>Offload modules using the specified offload manager.</p> PARAMETER DESCRIPTION <code>model</code> <p>PyTorch model to patch.</p> <p> TYPE: <code>Module</code> </p> <code>config</code> <p>OffloadConfig to use for offload (patterns from config.module_patterns).</p> <p> TYPE: <code>OffloadConfig | None</code> DEFAULT: <code>None</code> </p> <code>name</code> <p>Name for the offload manager. Defaults to DEFAULT_MANAGER_NAME.</p> <p> TYPE: <code>str</code> DEFAULT: <code>DEFAULT_MANAGER_NAME</code> </p> RETURNS DESCRIPTION <code>Module</code> <p>The patched model.</p>"},{"location":"api/simplified/#flextensor.offload_manager.get_offload_manager","title":"get_offload_manager","text":"<pre><code>get_offload_manager(name: str = DEFAULT_MANAGER_NAME) -&gt; OffloadManager\n</code></pre> <p>Get or create an OffloadManager singleton.</p> PARAMETER DESCRIPTION <code>name</code> <p>Name for the offload manager. Defaults to DEFAULT_MANAGER_NAME.</p> <p> TYPE: <code>str</code> DEFAULT: <code>DEFAULT_MANAGER_NAME</code> </p> RETURNS DESCRIPTION <code>OffloadManager</code> <p>OffloadManager instance</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If the named manager was created by a different thread.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; om = get_offload_manager()\n&gt;&gt;&gt; om.set_config(OffloadConfig(gpu_device=0))\n</code></pre>"},{"location":"api/simplified/#flextensor.offload_manager.DEFAULT_MANAGER_NAME","title":"DEFAULT_MANAGER_NAME  <code>module-attribute</code>","text":"<pre><code>DEFAULT_MANAGER_NAME: str = 'default'\n</code></pre> <p>Name used by :func:<code>get_offload_manager</code> when no explicit name is provided.</p>"},{"location":"api/simplified/#flextensor.offload_manager.set_config","title":"set_config","text":"<pre><code>set_config(config: OffloadConfig, name: str = DEFAULT_MANAGER_NAME)\n</code></pre> <p>Set config for the specified offload manager.</p> PARAMETER DESCRIPTION <code>config</code> <p>OffloadConfig to apply.</p> <p> TYPE: <code>OffloadConfig</code> </p> <code>name</code> <p>Name for the offload manager. Defaults to DEFAULT_MANAGER_NAME.</p> <p> TYPE: <code>str</code> DEFAULT: <code>DEFAULT_MANAGER_NAME</code> </p>"},{"location":"api/simplified/#flextensor.offload_manager.offload_block","title":"offload_block","text":"<pre><code>offload_block(block_name: str, name: str = DEFAULT_MANAGER_NAME) -&gt; Any\n</code></pre> <p>Create offload block using the specified offload manager.</p> PARAMETER DESCRIPTION <code>block_name</code> <p>Name for the offload block.</p> <p> TYPE: <code>str</code> </p> <code>name</code> <p>Name for the offload manager. Defaults to DEFAULT_MANAGER_NAME.</p> <p> TYPE: <code>str</code> DEFAULT: <code>DEFAULT_MANAGER_NAME</code> </p>"},{"location":"api/simplified/#flextensor.offload_manager.get_gpu_memory_usage","title":"get_gpu_memory_usage","text":"<pre><code>get_gpu_memory_usage(name: str = DEFAULT_MANAGER_NAME) -&gt; GPUMemoryUsage\n</code></pre> <p>Get GPU memory usage by the specified offload manager.</p> <p>Returns the memory used by GPU transfer blocks and unmapped tensors. Must be called after the manager has transitioned to inference mode.</p> PARAMETER DESCRIPTION <code>name</code> <p>Name for the offload manager. Defaults to DEFAULT_MANAGER_NAME.</p> <p> TYPE: <code>str</code> DEFAULT: <code>DEFAULT_MANAGER_NAME</code> </p> RETURNS DESCRIPTION <code>GPUMemoryUsage</code> <p>GPUMemoryUsage dataclass with memory breakdown in bytes and megabytes.</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If called before inference mode.</p>"},{"location":"api/simplified/#flextensor.offload_manager.save_profile","title":"save_profile","text":"<pre><code>save_profile(profile_directory: str | None = None, name: str = DEFAULT_MANAGER_NAME) -&gt; None\n</code></pre> <p>Save profile using the specified offload manager.</p> PARAMETER DESCRIPTION <code>profile_directory</code> <p>Directory to save profile to. If None, uses config.profile_storage_dir.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>name</code> <p>Name for the offload manager. Defaults to DEFAULT_MANAGER_NAME.</p> <p> TYPE: <code>str</code> DEFAULT: <code>DEFAULT_MANAGER_NAME</code> </p>"},{"location":"api/simplified/#flextensor.offload_manager.load_profile","title":"load_profile","text":"<pre><code>load_profile(profile_directory: str | None = None, model: Module | None = None, name: str = DEFAULT_MANAGER_NAME) -&gt; None\n</code></pre> <p>Load profile using the specified offload manager.</p> PARAMETER DESCRIPTION <code>profile_directory</code> <p>Directory containing the profile. If None, uses config.profile_storage_dir.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>model</code> <p>Model to restore state to. If None, uses the current model.</p> <p> TYPE: <code>Module | None</code> DEFAULT: <code>None</code> </p> <code>name</code> <p>Name for the offload manager. Defaults to DEFAULT_MANAGER_NAME.</p> <p> TYPE: <code>str</code> DEFAULT: <code>DEFAULT_MANAGER_NAME</code> </p>"},{"location":"api/simplified/#flextensor.offload_manager.release","title":"release","text":"<pre><code>release(name: str = DEFAULT_MANAGER_NAME)\n</code></pre> <p>Release the specified offload manager.</p> PARAMETER DESCRIPTION <code>name</code> <p>Name for the offload manager. Defaults to DEFAULT_MANAGER_NAME.</p> <p> TYPE: <code>str</code> DEFAULT: <code>DEFAULT_MANAGER_NAME</code> </p>"},{"location":"api/simplified/#lazy-model-initialization","title":"Lazy Model Initialization","text":""},{"location":"api/simplified/#flextensor.lazy_model_init.load_model_from_profile","title":"load_model_from_profile","text":"<pre><code>load_model_from_profile(tensor_manager: TensorManager, profile_directory: str, model_class: type[Module], model_args: tuple = (), safetensor_path: str | None = None, model_kwargs: dict | None = None, buffer_device: str | None = None, diagnose: bool = False, strict: bool = False) -&gt; Module\n</code></pre> <p>Load a model from a saved profile state with optimized weight loading.</p> <p>This function creates a model with parameters on meta device and buffers on GPU, then loads only the necessary weights from safetensors based on the profile state. This is more memory-efficient than loading the full model.</p> PARAMETER DESCRIPTION <code>tensor_manager</code> <p>The TensorManager instance to use for state restoration</p> <p> TYPE: <code>TensorManager</code> </p> <code>profile_directory</code> <p>Directory containing the profile (profile.json)</p> <p> TYPE: <code>str</code> </p> <code>model_class</code> <p>The model class to instantiate (e.g., Transformer)</p> <p> TYPE: <code>type[Module]</code> </p> <code>model_args</code> <p>Positional arguments for the model constructor</p> <p> TYPE: <code>tuple</code> DEFAULT: <code>()</code> </p> <code>safetensor_path</code> <p>Path to the safetensors file. Required.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>model_kwargs</code> <p>Keyword arguments for the model constructor</p> <p> TYPE: <code>dict | None</code> DEFAULT: <code>None</code> </p> <code>buffer_device</code> <p>Device for model buffers (default: uses tensor manager's device)</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>diagnose</code> <p>If True, run diagnostics on nested parameter tracking</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>strict</code> <p>If True, raise KeyError when any tensor key from the profile is missing in the safetensors file. Default False.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> RETURNS DESCRIPTION <code>Module</code> <p>The loaded model in eval mode, ready for use with tensor_manager</p> RAISES DESCRIPTION <code>KeyError</code> <p>If strict=True and any profile tensor keys are missing from safetensors</p> Example <p>from flextensor import TensorManager from flextensor.lazy_model_init import load_model_from_profile</p> <p>tensor_manager = TensorManager(device_gpu, ...) model = load_model_from_profile( ...     tensor_manager, ...     \"./profile\", ...     Transformer, ...     model_args=(model_args,), ...     safetensor_path=\"/path/to/model.safetensors\", ... ) tensor_manager.set_model(model) model.set_tensor_manager(tensor_manager) model = tensor_manager.initialize_warmup()</p>"},{"location":"api/strategies/","title":"Strategies","text":""},{"location":"api/strategies/#strategies","title":"Strategies","text":"<p>Offloading strategies determine which tensors to move from GPU to CPU memory. Pass a strategy instance to <code>OffloadConfig(load_strategy=...)</code>.</p>"},{"location":"api/strategies/#strategy-protocol","title":"Strategy Protocol","text":""},{"location":"api/strategies/#flextensor.strategy.protocol.Strategy","title":"Strategy","text":"<p>               Bases: <code>Protocol</code></p> <p>Protocol for offload strategies.</p> <p>All offload strategies must implement this protocol to be usable with the OffloadConfig and TensorManager.</p>"},{"location":"api/strategies/#flextensor.strategy.protocol.Strategy.compute","title":"compute","text":"<pre><code>compute(layer_stats: list[LayerStatistics], memory_stats: dict[int, float] | None = None) -&gt; StrategyResult\n</code></pre> <p>Compute offload strategy.</p> PARAMETER DESCRIPTION <code>layer_stats</code> <p>List of layer statistics containing tensor info and durations.</p> <p> TYPE: <code>list[LayerStatistics]</code> </p> <code>memory_stats</code> <p>Optional dict mapping tensor size (bytes) to transfer time (ms).</p> <p> TYPE: <code>dict[int, float] | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>StrategyResult</code> <p>StrategyResult containing strategy_map and optional block_data.</p>"},{"location":"api/strategies/#result-types","title":"Result Types","text":""},{"location":"api/strategies/#flextensor.strategy.protocol.StrategyResult","title":"StrategyResult  <code>dataclass</code>","text":"<pre><code>StrategyResult(strategy_map: dict[str, list[TensorStatistics]], block_data: BlockStrategyData | None = None)\n</code></pre> <p>Result from strategy.compute().</p> <p>All strategies return this structure for consistency. TensorManager uses the appropriate fields based on loader_type.</p>"},{"location":"api/strategies/#flextensor.strategy.protocol.StrategyResult.strategy_map","title":"strategy_map  <code>instance-attribute</code>","text":"<pre><code>strategy_map: dict[str, list[TensorStatistics]]\n</code></pre> <p>Maps layer label to list of tensors to offload.</p>"},{"location":"api/strategies/#flextensor.strategy.protocol.StrategyResult.block_data","title":"block_data  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>block_data: BlockStrategyData | None = None\n</code></pre> <p>Block allocation data. None if n_blocks=0 or not computed.</p>"},{"location":"api/strategies/#flextensor.strategy.protocol.BlockStrategyData","title":"BlockStrategyData  <code>dataclass</code>","text":"<pre><code>BlockStrategyData(label_to_size_map: dict[str, int], allocation_ordered: dict[int, list[str]], block_sizes: dict[int, int] | list[int], label_to_block_id: dict[str, int], transfer_to_compute_map: dict[str, str])\n</code></pre> <p>Block allocation data computed by strategies.</p> <p>This contains all data needed for block-based loaders to execute pipelined CPU\u2192GPU transfers.</p>"},{"location":"api/strategies/#flextensor.strategy.protocol.BlockStrategyData.label_to_size_map","title":"label_to_size_map  <code>instance-attribute</code>","text":"<pre><code>label_to_size_map: dict[str, int]\n</code></pre> <p>Maps layer label to total size of tensors to offload.</p>"},{"location":"api/strategies/#flextensor.strategy.protocol.BlockStrategyData.allocation_ordered","title":"allocation_ordered  <code>instance-attribute</code>","text":"<pre><code>allocation_ordered: dict[int, list[str]]\n</code></pre> <p>Maps block_id to ordered list of layer labels assigned to it.</p>"},{"location":"api/strategies/#flextensor.strategy.protocol.BlockStrategyData.block_sizes","title":"block_sizes  <code>instance-attribute</code>","text":"<pre><code>block_sizes: dict[int, int] | list[int]\n</code></pre> <p>Size of each block in bytes (max layer size per block).</p>"},{"location":"api/strategies/#flextensor.strategy.protocol.BlockStrategyData.label_to_block_id","title":"label_to_block_id  <code>instance-attribute</code>","text":"<pre><code>label_to_block_id: dict[str, int]\n</code></pre> <p>Maps layer label to assigned block index.</p>"},{"location":"api/strategies/#flextensor.strategy.protocol.BlockStrategyData.transfer_to_compute_map","title":"transfer_to_compute_map  <code>instance-attribute</code>","text":"<pre><code>transfer_to_compute_map: dict[str, str]\n</code></pre> <p>Maps transfer layer label to compute layer label for pipelining.</p>"},{"location":"api/strategies/#knapsack-strategies","title":"Knapsack Strategies","text":""},{"location":"api/strategies/#flextensor.strategy.knapsack.KnapsackStrategy","title":"KnapsackStrategy","text":"<pre><code>KnapsackStrategy(scale: float = 1.0, cyclic: bool = True, group_size: int = 1, threshold_mb: float = 0.1, max_gpu_mem_bytes: int | None = None, target_gpu_mem_bytes: int | None = None, n_blocks: int = 4)\n</code></pre> <p>Knapsack-based offloading strategy using duration-based optimization.</p> <p>Initialize KnapsackStrategy for tensor offloading.</p> PARAMETER DESCRIPTION <code>scale</code> <p>Duration multiplier (baseline). <code>0.9</code> = 10% safety margin, <code>1.0</code> = exact fit, <code>2.0</code> = allow 100% overhead. When <code>max_gpu_mem_bytes</code> is set, scale may be increased automatically to meet the memory constraint.</p> <p> TYPE: <code>float</code> DEFAULT: <code>1.0</code> </p> <code>cyclic</code> <p>Whether to apply cyclic strategy extension.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>group_size</code> <p>Number of layers to merge into groups.</p> <p> TYPE: <code>int</code> DEFAULT: <code>1</code> </p> <code>threshold_mb</code> <p>Minimum tensor size threshold in MB.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.1</code> </p> <code>max_gpu_mem_bytes</code> <p>Hard GPU memory limit in bytes (never exceed). When <code>None</code>, latency mode \u2014 respect <code>scale</code> strictly.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>target_gpu_mem_bytes</code> <p>Soft GPU memory target in bytes (optimizer aims here). Avoids over-offloading when GPU memory is abundant. If set without <code>max_gpu_mem_bytes</code>, the target is used as the hard limit. Defaults to <code>max_gpu_mem_bytes</code> when <code>None</code>.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>n_blocks</code> <p>Number of blocks for block-based loaders. Set to 0 to skip block data computation.</p> <p> TYPE: <code>int</code> DEFAULT: <code>4</code> </p>"},{"location":"api/strategies/#flextensor.strategy.knapsack.KnapsackStrategy.compute","title":"compute","text":"<pre><code>compute(layer_stats: list[LayerStatistics], memory_stats: dict[int, float] | None = None) -&gt; StrategyResult\n</code></pre> <p>Compute offload strategy using knapsack optimization.</p> PARAMETER DESCRIPTION <code>layer_stats</code> <p>List of layer statistics containing tensor info and durations.</p> <p> TYPE: <code>list[LayerStatistics]</code> </p> <code>memory_stats</code> <p>Optional dict mapping tensor size (bytes) to transfer time (ms). If not provided, extracts from layer stats.</p> <p> TYPE: <code>dict[int, float] | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>StrategyResult</code> <p>StrategyResult containing strategy_map and optional block_data.</p>"},{"location":"api/strategies/#flextensor.strategy.knapsack.KnapsackBlockStrategy","title":"KnapsackBlockStrategy","text":"<pre><code>KnapsackBlockStrategy(scale: float = 1.0, group_size: int = 1, threshold_mb: float = 0.1, max_gpu_mem_bytes: int | None = None, target_gpu_mem_bytes: int | None = None, n_blocks: int = 4)\n</code></pre> <p>Block-based knapsack strategy that uses externally provided memory transfer statistics.</p> <p>This strategy expects memory_stats to be provided by TensorManager, which handles the live GPU benchmarking. This separates concerns: TensorManager manages profiling, strategies focus on optimization logic.</p> <p>Initialize KnapsackBlockStrategy for tensor offloading.</p> PARAMETER DESCRIPTION <code>scale</code> <p>Duration multiplier (baseline). <code>0.9</code> = 10% safety margin, <code>1.0</code> = exact fit, <code>2.0</code> = allow 100% overhead. When <code>max_gpu_mem_bytes</code> is set, scale may be increased automatically to meet the memory constraint.</p> <p> TYPE: <code>float</code> DEFAULT: <code>1.0</code> </p> <code>group_size</code> <p>Number of layers to merge into groups.</p> <p> TYPE: <code>int</code> DEFAULT: <code>1</code> </p> <code>threshold_mb</code> <p>Minimum tensor size threshold in MB.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.1</code> </p> <code>max_gpu_mem_bytes</code> <p>Hard GPU memory limit in bytes (never exceed). When <code>None</code>, latency mode \u2014 respect <code>scale</code> strictly.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>target_gpu_mem_bytes</code> <p>Soft GPU memory target in bytes (optimizer aims here). If set without <code>max_gpu_mem_bytes</code>, the target is used as the hard limit. Defaults to <code>max_gpu_mem_bytes</code> when <code>None</code>.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>n_blocks</code> <p>Number of blocks for block-based loaders.</p> <p> TYPE: <code>int</code> DEFAULT: <code>4</code> </p>"},{"location":"api/strategies/#flextensor.strategy.knapsack.KnapsackBlockStrategy.compute","title":"compute","text":"<pre><code>compute(layer_stats: list[LayerStatistics], memory_stats: dict[int, float] | None = None) -&gt; StrategyResult\n</code></pre> <p>Compute offload strategy using block-based knapsack optimization.</p> PARAMETER DESCRIPTION <code>layer_stats</code> <p>List of layer statistics containing tensor info and durations.</p> <p> TYPE: <code>list[LayerStatistics]</code> </p> <code>memory_stats</code> <p>Dict mapping tensor size (bytes) to transfer time (ms). Should be provided by TensorManager via live GPU benchmarking. If not provided, falls back to extracting from layer stats.</p> <p> TYPE: <code>dict[int, float] | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>StrategyResult</code> <p>StrategyResult containing strategy_map and block_data.</p>"},{"location":"api/strategies/#flextensor.strategy.knapsack.AdaptiveKnapsackStrategy","title":"AdaptiveKnapsackStrategy","text":"<pre><code>AdaptiveKnapsackStrategy(scale: float = 1.0, loader_type: str = 'strategy', cyclic: bool = True, group_size: int = 1, threshold_mb: float = 0.1, max_gpu_mem_bytes: int | None = None, target_gpu_mem_bytes: int | None = None)\n</code></pre> <p>Adaptive strategy that selects between KnapsackStrategy and KnapsackBlockStrategy based on the loader type configuration.</p> <p>This provides a unified interface that automatically selects the appropriate strategy implementation based on how tensors will be loaded during inference.</p> <p>Initialize AdaptiveKnapsackStrategy.</p> PARAMETER DESCRIPTION <code>scale</code> <p>Duration multiplier (baseline). See <code>KnapsackStrategy</code> for details.</p> <p> TYPE: <code>float</code> DEFAULT: <code>1.0</code> </p> <code>loader_type</code> <p>Type of tensor loader to use. Options: - \"strategy\": Uses KnapsackStrategy (profiled tensor data) - \"raw_block_transfer\": Uses KnapsackBlockStrategy - \"allocation_block_transfer\": Uses KnapsackBlockStrategy</p> <p> TYPE: <code>str</code> DEFAULT: <code>'strategy'</code> </p> <code>cyclic</code> <p>Whether to apply cyclic strategy extension.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>group_size</code> <p>Number of layers to merge into groups.</p> <p> TYPE: <code>int</code> DEFAULT: <code>1</code> </p> <code>threshold_mb</code> <p>Minimum tensor size threshold in MB.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.1</code> </p> <code>max_gpu_mem_bytes</code> <p>Hard GPU memory limit in bytes (never exceed). When <code>None</code>, latency mode \u2014 respect <code>scale</code> strictly.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>target_gpu_mem_bytes</code> <p>Soft GPU memory target in bytes. If set without <code>max_gpu_mem_bytes</code>, the target is used as the hard limit.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p>"},{"location":"api/strategies/#flextensor.strategy.knapsack.AdaptiveKnapsackStrategy.compute","title":"compute","text":"<pre><code>compute(layer_stats: list[LayerStatistics], memory_stats: dict[int, float] | None = None) -&gt; StrategyResult\n</code></pre> <p>Compute offload strategy using the appropriate strategy implementation.</p> PARAMETER DESCRIPTION <code>layer_stats</code> <p>List of layer statistics containing tensor info and durations.</p> <p> TYPE: <code>list[LayerStatistics]</code> </p> <code>memory_stats</code> <p>Optional dict mapping tensor size (bytes) to transfer time (ms).</p> <p> TYPE: <code>dict[int, float] | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>StrategyResult</code> <p>StrategyResult containing strategy_map and block_data.</p>"},{"location":"api/strategies/#simple-strategies","title":"Simple Strategies","text":""},{"location":"api/strategies/#flextensor.strategy.simple.GreedyStrategy","title":"GreedyStrategy","text":"<pre><code>GreedyStrategy(threshold_mb: float = 0.1, n_blocks: int = 4)\n</code></pre> <p>Greedy offloading strategy based on cumulative compute time heuristic.</p> <p>Initialize GreedyStrategy.</p> PARAMETER DESCRIPTION <code>threshold_mb</code> <p>Minimum tensor size threshold in MB.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.1</code> </p> <code>n_blocks</code> <p>Number of blocks for block-based loaders.</p> <p> TYPE: <code>int</code> DEFAULT: <code>4</code> </p>"},{"location":"api/strategies/#flextensor.strategy.simple.GreedyStrategy.compute","title":"compute","text":"<pre><code>compute(layer_stats: list[LayerStatistics], memory_stats: dict[int, float] | None = None) -&gt; StrategyResult\n</code></pre> <p>Compute greedy offload strategy.</p> PARAMETER DESCRIPTION <code>layer_stats</code> <p>List of layer statistics.</p> <p> TYPE: <code>list[LayerStatistics]</code> </p> <code>memory_stats</code> <p>Unused, accepted for interface consistency.</p> <p> TYPE: <code>dict[int, float] | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>StrategyResult</code> <p>StrategyResult containing strategy_map and optional block_data.</p>"},{"location":"api/strategies/#flextensor.strategy.simple.NthLayerStrategy","title":"NthLayerStrategy","text":"<pre><code>NthLayerStrategy(nth_layer: int, threshold_mb: float = 0.1, n_blocks: int = 4)\n</code></pre> <p>Offloading strategy that offloads every Nth layer.</p> <p>Initialize NthLayerStrategy.</p> PARAMETER DESCRIPTION <code>nth_layer</code> <p>Interval between offloaded layers.</p> <p> TYPE: <code>int</code> </p> <code>threshold_mb</code> <p>Minimum tensor size threshold in MB.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.1</code> </p> <code>n_blocks</code> <p>Number of blocks for block-based loaders.</p> <p> TYPE: <code>int</code> DEFAULT: <code>4</code> </p>"},{"location":"api/strategies/#flextensor.strategy.simple.NthLayerStrategy.compute","title":"compute","text":"<pre><code>compute(layer_stats: list[LayerStatistics], memory_stats: dict[int, float] | None = None) -&gt; StrategyResult\n</code></pre> <p>Compute Nth layer offload strategy.</p> PARAMETER DESCRIPTION <code>layer_stats</code> <p>List of layer statistics.</p> <p> TYPE: <code>list[LayerStatistics]</code> </p> <code>memory_stats</code> <p>Unused, accepted for interface consistency.</p> <p> TYPE: <code>dict[int, float] | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>StrategyResult</code> <p>StrategyResult containing strategy_map and optional block_data.</p>"},{"location":"api/strategies/#global-strategies","title":"Global Strategies","text":"<p>Optimizer parameter values</p> <p><code>GlobalTensorSelectionStrategy</code> accepts an <code>optimizer</code> parameter with two accepted values: <code>\"DE\"</code> (differential evolution via <code>scipy.optimize.differential_evolution</code>) and <code>\"SA\"</code> (dual annealing via <code>scipy.optimize.dual_annealing</code>). <code>\"DE\"</code> is recommended for most cases as it natively handles binary variables. <code>\"SA\"</code> treats variables as continuous and is less efficient for binary decision problems; it may be useful for small problems with few tensors.</p>"},{"location":"api/strategies/#flextensor.strategy.global_strategy.GlobalOffloadStrategy","title":"GlobalOffloadStrategy","text":"<pre><code>GlobalOffloadStrategy(n_blocks: int = 4, max_gpu_mem_bytes: int | None = None, target_gpu_mem_bytes: int | None = None, scale: float = 1.0, threshold_mb: float = 0.1, min_blocks: int | None = None, max_blocks: int | None = None, assignment_strategy: AssignmentStrategy | None = None)\n</code></pre> <p>Global offload strategy using assignment-based layer-to-block mapping for pipelined execution.</p> <p>Pipeline Execution Model: - Layer N computes using Block X (data transferred during Layer N-1) - While computing Layer N, we transfer Block Y for Layer N+1 - Constraint: Block Y \u2260 Block X (can't transfer and compute same block simultaneously) - Block X becomes free after Layer N finishes \u2192 can be reused for Layer N+2</p> <p>This means: - Minimum 2 blocks required for pipelining - With 2 blocks: alternating pattern (A, B, A, B, ...) - With 3+ blocks: more flexibility for scheduling</p> <p>The strategy uses an AssignmentStrategy (default: OptimizedRoundRobinAssignment) to determine layer-to-block assignments, then calculates block sizes based on the maximum tensor size assigned to each block.</p> <p>Initialize GlobalOffloadStrategy.</p> PARAMETER DESCRIPTION <code>n_blocks</code> <p>Number of reusable memory blocks (minimum 2 for pipelining).</p> <p> TYPE: <code>int</code> DEFAULT: <code>4</code> </p> <code>max_gpu_mem_bytes</code> <p>Hard GPU memory limit in bytes (never exceed). When <code>None</code>, latency mode \u2014 offload what fits in <code>scale * duration</code>.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>target_gpu_mem_bytes</code> <p>Soft GPU memory target in bytes. If set without <code>max_gpu_mem_bytes</code>, the target is used as the hard limit. Defaults to <code>max_gpu_mem_bytes</code> when <code>None</code>.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>scale</code> <p>Duration multiplier (baseline). Controls how much of the previous layer's compute time is available for transfers. <code>1.0</code> = exact fit.</p> <p> TYPE: <code>float</code> DEFAULT: <code>1.0</code> </p> <code>threshold_mb</code> <p>Minimum tensor size threshold in MB. Tensors smaller than this are kept on GPU and not offloaded.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.1</code> </p> <code>min_blocks</code> <p>Minimum number of blocks to use (default: 2). - 2 blocks: With alternating pattern (A,B,A,B,...)</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>max_blocks</code> <p>Maximum number of blocks to use (default: n_blocks).</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>assignment_strategy</code> <p>Strategy for layer-to-block assignment. Default: OptimizedRoundRobinAssignment (best GPU utilization). Available strategies: - OptimizedRoundRobinAssignment: Optimized cyclic pattern (recommended) - StrictRoundRobinAssignment: Strict i % n_blocks pattern</p> <p> TYPE: <code>AssignmentStrategy | None</code> DEFAULT: <code>None</code> </p> RAISES DESCRIPTION <code>ValueError</code> <p>If n_blocks &lt; 2 (minimum required for pipelining).</p> <code>ValueError</code> <p>If min_blocks &gt; max_blocks.</p> <code>ValueError</code> <p>If min_blocks &lt; 2 or max_blocks &gt; n_blocks.</p> <code>ValueError</code> <p>If scale &lt;= 0.</p> <code>ValueError</code> <p>If target_gpu_mem_bytes &gt; max_gpu_mem_bytes.</p>"},{"location":"api/strategies/#flextensor.strategy.global_strategy.GlobalOffloadStrategy.calculate_layer_tensor_size","title":"calculate_layer_tensor_size  <code>staticmethod</code>","text":"<pre><code>calculate_layer_tensor_size(tensors: list[TensorStatistics]) -&gt; int\n</code></pre> <p>Calculate total size of tensors in a layer.</p>"},{"location":"api/strategies/#flextensor.strategy.global_strategy.GlobalOffloadStrategy.calculate_peak_memory_pipelined","title":"calculate_peak_memory_pipelined  <code>staticmethod</code>","text":"<pre><code>calculate_peak_memory_pipelined(block_sizes: list[int], non_offloaded_memory: int) -&gt; int\n</code></pre> <p>Calculate peak GPU memory for pipelined execution.</p> <p>Peak memory includes: - All blocks (must be pre-allocated on GPU for pipelining) - Non-offloaded tensors (always stay on GPU)</p> PARAMETER DESCRIPTION <code>block_sizes</code> <p>Size of each block in bytes (integers).</p> <p> TYPE: <code>list[int]</code> </p> <code>non_offloaded_memory</code> <p>Memory used by tensors that are not offloaded.</p> <p> TYPE: <code>int</code> </p> RETURNS DESCRIPTION <code>int</code> <p>Total peak GPU memory in bytes (integer).</p>"},{"location":"api/strategies/#flextensor.strategy.global_strategy.GlobalOffloadStrategy.compute","title":"compute","text":"<pre><code>compute(layer_stats: list[LayerStatistics], memory_stats: dict[int, float] | None = None) -&gt; StrategyResult\n</code></pre> <p>Compute offload strategy using pipelined block optimization.</p> <p>When <code>max_gpu_mem_bytes</code> is set (memory mode), the strategy will automatically increase <code>scale</code> beyond its initial value if the initial offloading is insufficient to meet the memory constraint.</p> PARAMETER DESCRIPTION <code>layer_stats</code> <p>List of layer statistics containing tensor info and durations.</p> <p> TYPE: <code>list[LayerStatistics]</code> </p> <code>memory_stats</code> <p>Optional dict mapping tensor size (bytes) to transfer time (ms). If provided, uses this data. Otherwise falls back to extraction from layer stats.</p> <p> TYPE: <code>dict[int, float] | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>StrategyResult</code> <p>StrategyResult containing strategy_map and block_data.</p>"},{"location":"api/strategies/#flextensor.strategy.global_strategy.GlobalTensorSelectionStrategy","title":"GlobalTensorSelectionStrategy","text":"<pre><code>GlobalTensorSelectionStrategy(max_gpu_mem_bytes: int | None = None, target_gpu_mem_bytes: int | None = None, scale: float = 1.0, scale_ub: float | None = None, n_blocks: int = 4, min_blocks: int | None = None, max_blocks: int | None = None, threshold_mb: float = 0.1, pop_size: int = 50, epoch: int = 200, max_early_stop: int = 25, optimizer: str = 'DE', seed_ratio: float = 0.25, seed: int | None = None, assignment_strategy: AssignmentStrategy | None = None, transfer_window: TransferWindowCalculator | None = None)\n</code></pre> <p>Tensor-level metaheuristic optimization strategy for GPU memory offloading.</p> <p>This strategy optimizes the binary decision of whether to offload each tensor, maximizing GPU memory utilization while respecting memory limits and ensuring transfers fit within pipelined execution windows.</p> <p>Unlike GlobalOffloadStrategy which optimizes block sizes and layer assignments, this strategy performs fine-grained tensor selection to find the optimal subset of tensors to offload.</p> <p>Decision variables: - Binary x[i] for each tensor: 1 = offload, 0 = keep on GPU - Continuous scale: multiplier for compute time (transfer window)</p> <p>Constraints: - Hard: Peak GPU memory &lt;= max_gpu_mem_bytes (when set) - Hard: Transfer time for layer N &lt;= (layer N-1's compute time) * scale</p> <p>Objective (maximize): - GPU utilization in target range [target_gpu_mem, max_gpu_mem] - Offload ratio (secondary) - Prefer lower scale (less latency overhead)</p> <p>Memory Model: - Tensors NOT offloaded stay on GPU permanently (preloaded) - Peak GPU = sum(all kept tensors) + sum(block sizes) - Blocks are pre-allocated for pipelined transfers</p> <p>Supported Optimizers (via scipy): - DE: Differential Evolution (scipy.optimize.differential_evolution) -     RECOMMENDED default. Natively handles binary variables via integrality     constraints. Best for high-dimensional problems with many tensors. - SA: Simulated Annealing (scipy.optimize.dual_annealing) -     Treats variables as continuous; less efficient for binary decisions.     May be useful for small problems with few tensors.</p> <p>Initialize GlobalTensorSelectionStrategy.</p> PARAMETER DESCRIPTION <code>max_gpu_mem_bytes</code> <p>Hard GPU memory limit in bytes (never exceed). When <code>None</code>, the memory constraint is disabled and the optimizer only respects the transfer time constraint (latency mode).</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>target_gpu_mem_bytes</code> <p>Soft GPU memory target in bytes. The optimizer aims for this peak memory to avoid over-offloading. If set without <code>max_gpu_mem_bytes</code>, the target is used as the hard limit. When <code>None</code>, defaults to <code>max_gpu_mem_bytes</code>.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>scale</code> <p>Duration multiplier (baseline / lower bound for the optimizer). <code>0.9</code> = 10% safety margin, <code>1.0</code> = exact fit (default).</p> <p> TYPE: <code>float</code> DEFAULT: <code>1.0</code> </p> <code>scale_ub</code> <p>Upper bound for scale the optimizer may explore. When <code>None</code> (default), automatically determined by mode: - Latency mode (<code>max_gpu_mem_bytes=None</code>): <code>scale</code> (no overhead) - Memory mode (<code>max_gpu_mem_bytes</code> set): <code>100.0</code> (wide search) When explicitly set, used as-is. Example: <code>scale=0.9, scale_ub=1.0</code> means transfers should fit within 90-100% of compute time.</p> <p> TYPE: <code>float | None</code> DEFAULT: <code>None</code> </p> <code>n_blocks</code> <p>Number of memory blocks (max). Used if assignment_strategy is None.</p> <p> TYPE: <code>int</code> DEFAULT: <code>4</code> </p> <code>min_blocks</code> <p>Minimum number of blocks to try (default: 2).</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>max_blocks</code> <p>Maximum number of blocks to try (default: n_blocks).</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>threshold_mb</code> <p>Minimum tensor size threshold in MB (smaller tensors always kept on GPU).</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.1</code> </p> <code>pop_size</code> <p>Population size for optimization.</p> <p> TYPE: <code>int</code> DEFAULT: <code>50</code> </p> <code>epoch</code> <p>Maximum number of optimization epochs.</p> <p> TYPE: <code>int</code> DEFAULT: <code>200</code> </p> <code>max_early_stop</code> <p>Stop if no improvement for this many epochs.</p> <p> TYPE: <code>int</code> DEFAULT: <code>25</code> </p> <code>optimizer</code> <p>Optimizer to use (default: \"DE\"). Available options: - \"DE\": Differential Evolution (scipy.optimize.differential_evolution).     RECOMMENDED. Natively handles binary variables via integrality     constraints. Best for high-dimensional problems with many tensors. - \"SA\": Simulated Annealing (scipy.optimize.dual_annealing).     Treats variables as continuous; less efficient for binary decisions.     May be useful for small problems with few tensors.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'DE'</code> </p> <code>seed_ratio</code> <p>Ratio of population to seed with feasible solutions (0.0-1.0). Lower ratio = more random exploration.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.25</code> </p> <code>seed</code> <p>Random seed for reproducibility. If None, uses non-deterministic seed.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>assignment_strategy</code> <p>Strategy for assigning layers to blocks. Default: OptimizedRoundRobinAssignment (tries min_blocks to max_blocks). Options: StrictRoundRobinAssignment, OptimizedRoundRobinAssignment.</p> <p> TYPE: <code>AssignmentStrategy | None</code> DEFAULT: <code>None</code> </p> <code>transfer_window</code> <p>Calculator for per-layer transfer windows. Default: SingleLayerWindow (previous layer's duration only). Use GapAwareWindow to exploit empty layers for larger transfer budgets.</p> <p> TYPE: <code>TransferWindowCalculator | None</code> DEFAULT: <code>None</code> </p>"},{"location":"api/strategies/#flextensor.strategy.global_strategy.GlobalTensorSelectionStrategy.scale","title":"scale  <code>property</code> <code>writable</code>","text":"<pre><code>scale: float\n</code></pre> <p>Duration multiplier (lower bound). Alias for <code>scale_lb</code>.</p>"},{"location":"api/strategies/#flextensor.strategy.global_strategy.GlobalTensorSelectionStrategy.compute","title":"compute","text":"<pre><code>compute(layer_stats: list[LayerStatistics], memory_stats: dict[int, float] | None = None) -&gt; StrategyResult\n</code></pre> <p>Run the core optimizer. See <code>compute()</code> for the public API.</p>"},{"location":"api/strategies/#adaptive-strategy","title":"Adaptive Strategy","text":""},{"location":"api/strategies/#flextensor.strategy.adaptive.AdaptiveStrategy","title":"AdaptiveStrategy","text":"<pre><code>AdaptiveStrategy(scale: float = 1.0, loader_type: str = 'allocation_block_transfer', cyclic: bool = False, group_size: int = 1, threshold_mb: float = 0.1, min_blocks: int = 2, n_blocks: int = 4, max_gpu_mem_bytes: int | None = None, target_gpu_mem_bytes: int | None = None, extra_optimization: bool = False)\n</code></pre> <p>Run all available strategies and return the best result.</p> <p>For block-based loaders (the common path), the following candidates are evaluated:</p> <ul> <li>:class:<code>KnapsackBlockStrategy</code></li> <li>:class:<code>GlobalOffloadStrategy</code> (Optimized assignment)</li> <li>:class:<code>GlobalOffloadStrategy</code> (Strict assignment)</li> <li>:class:<code>GlobalTensorSelectionStrategy</code> (Optimized assignment)</li> <li>:class:<code>GlobalTensorSelectionStrategy</code> (Strict assignment)</li> </ul> <p>For non-block loaders only :class:<code>KnapsackStrategy</code> is used.</p> <p>After all candidates run, the one with the lowest overhead that also satisfies the memory constraint (if any) is selected.  The winning strategy's name is available via :attr:<code>selected_strategy_name</code>.</p> Example <p>strategy = AdaptiveStrategy( ...     scale=1.0, ...     max_gpu_mem_bytes=50 * 1024**3, ...     n_blocks=4, ... ) result = strategy.compute(layer_stats, memory_stats) print(strategy.selected_strategy_name) 'KnapsackBlock'</p> <p>Initialize AdaptiveStrategy.</p> PARAMETER DESCRIPTION <code>scale</code> <p>Duration multiplier (baseline). <code>0.9</code> = 10% safety margin, <code>1.0</code> = exact fit, <code>2.0</code> = allow 100% overhead.</p> <p> TYPE: <code>float</code> DEFAULT: <code>1.0</code> </p> <code>loader_type</code> <p>Type of tensor loader. Determines which strategy candidates are instantiated.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'allocation_block_transfer'</code> </p> <code>cyclic</code> <p>Whether to apply cyclic strategy extension (KnapsackStrategy only).</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>group_size</code> <p>Number of layers to merge into groups (Knapsack strategies).</p> <p> TYPE: <code>int</code> DEFAULT: <code>1</code> </p> <code>threshold_mb</code> <p>Minimum tensor size threshold in MB.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.1</code> </p> <code>min_blocks</code> <p>Minimum number of blocks for OptimizedRoundRobinAssignment search range. Does not affect Strict or Knapsack candidates.</p> <p> TYPE: <code>int</code> DEFAULT: <code>2</code> </p> <code>n_blocks</code> <p>Number of memory blocks.</p> <p> TYPE: <code>int</code> DEFAULT: <code>4</code> </p> <code>max_gpu_mem_bytes</code> <p>Hard GPU memory limit in bytes (never exceed). When <code>None</code>, latency mode \u2014 respect <code>scale</code> strictly.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>target_gpu_mem_bytes</code> <p>Soft GPU memory target in bytes. If set without <code>max_gpu_mem_bytes</code>, the target is used as the hard limit.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> <code>extra_optimization</code> <p>If True, include additional slower strategy candidates (GlobalTensorSelectionStrategy) that may find better solutions at the cost of significantly longer optimization time. Default is False.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p>"},{"location":"api/strategies/#flextensor.strategy.adaptive.AdaptiveStrategy.selected_strategy_name","title":"selected_strategy_name  <code>property</code>","text":"<pre><code>selected_strategy_name: str\n</code></pre> <p>Name of the strategy that produced the best result.</p> RAISES DESCRIPTION <code>RuntimeError</code> <p>If accessed before :meth:<code>compute</code> has been called.</p>"},{"location":"api/strategies/#flextensor.strategy.adaptive.AdaptiveStrategy.all_scores","title":"all_scores  <code>property</code>","text":"<pre><code>all_scores: list[StrategyScore]\n</code></pre> <p>Scores from all candidates evaluated in the last :meth:<code>compute</code> call.</p>"},{"location":"api/strategies/#flextensor.strategy.adaptive.AdaptiveStrategy.compute","title":"compute","text":"<pre><code>compute(layer_stats: list[LayerStatistics], memory_stats: dict[int, float] | None = None) -&gt; StrategyResult\n</code></pre> <p>Run all strategy candidates and return the best result.</p> PARAMETER DESCRIPTION <code>layer_stats</code> <p>List of layer statistics containing tensor info and durations.</p> <p> TYPE: <code>list[LayerStatistics]</code> </p> <code>memory_stats</code> <p>Optional dict mapping tensor size (bytes) to transfer time (ms).</p> <p> TYPE: <code>dict[int, float] | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>StrategyResult</code> <p>class:<code>StrategyResult</code> from the best-scoring candidate.</p>"},{"location":"api/strategies/#assignment-strategies","title":"Assignment Strategies","text":""},{"location":"api/strategies/#flextensor.strategy.assignment.AssignmentStrategy","title":"AssignmentStrategy","text":"<p>               Bases: <code>Protocol</code></p> <p>Protocol for layer-to-block assignment strategies.</p>"},{"location":"api/strategies/#flextensor.strategy.assignment.AssignmentStrategy.compute","title":"compute","text":"<pre><code>compute(layer_sizes: list[int], n_blocks: int) -&gt; list[int]\n</code></pre> <pre><code>Compute layer-to-block assignment.\n</code></pre> PARAMETER DESCRIPTION <code>layer_sizes</code> <p>Size of each layer in bytes.</p> <p> TYPE: <code>list[int]</code> </p> <code>n_blocks</code> <p>Number of available blocks.</p> <p> TYPE: <code>int</code> </p> RETURNS DESCRIPTION <code>list[int]</code> <p>List of block indices, one per layer.</p>"},{"location":"api/strategies/#flextensor.strategy.assignment.StrictRoundRobinAssignment","title":"StrictRoundRobinAssignment","text":"<p>Strict round-robin assignment.</p> <p>Assigns layers to blocks using strict pattern: layer_i -&gt; block (i % n_blocks). Always uses all available blocks in a fixed cyclic pattern (0,1,2,3,0,1,2,3,...). Does not optimize for memory - all blocks sized to fit the largest layer.</p>"},{"location":"api/strategies/#flextensor.strategy.assignment.StrictRoundRobinAssignment.compute","title":"compute","text":"<pre><code>compute(layer_sizes: list[int], n_blocks: int) -&gt; list[int]\n</code></pre> <p>Compute simple cyclic assignment.</p>"},{"location":"api/strategies/#flextensor.strategy.assignment.OptimizedRoundRobinAssignment","title":"OptimizedRoundRobinAssignment","text":"<pre><code>OptimizedRoundRobinAssignment(min_blocks: int = 3, max_blocks: int = 4, distribution_weight: float = 0.5)\n</code></pre> <p>Optimized round-robin assignment that minimizes memory while satisfying constraints.</p> <p>Features: - Tries different numbers of blocks (min_blocks to max_blocks) - Uses exhaustive search for small problems, greedy for larger ones - Balances memory efficiency with reuse distance (distribution_weight)</p> PARAMETER DESCRIPTION <code>min_blocks</code> <p>Minimum number of blocks to use.</p> <p> TYPE: <code>int</code> DEFAULT: <code>3</code> </p> <code>max_blocks</code> <p>Maximum number of blocks to use.</p> <p> TYPE: <code>int</code> DEFAULT: <code>4</code> </p> <code>distribution_weight</code> <p>Weight for reuse distance vs memory (0.0-1.0). 0.0 = pure memory optimization, 1.0 = pure distribution optimization.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.5</code> </p>"},{"location":"api/strategies/#flextensor.strategy.assignment.OptimizedRoundRobinAssignment.compute","title":"compute","text":"<pre><code>compute(layer_sizes: list[int], n_blocks: int) -&gt; list[int]\n</code></pre> <p>Compute optimized assignment within block count range.</p>"},{"location":"api/strategies/#transfer-windows","title":"Transfer Windows","text":""},{"location":"api/strategies/#flextensor.strategy.transfer_window.TransferWindowCalculator","title":"TransferWindowCalculator","text":"<p>               Bases: <code>Protocol</code></p> <p>Protocol for computing effective transfer windows per layer.</p> <p>In the pipelined offload model, the transfer for layer i starts during an earlier layer's compute.  How much earlier depends on the strategy:</p> <ul> <li><code>SingleLayerWindow</code> assumes transfer happens during layer i-1 only.</li> <li><code>GapAwareWindow</code> extends the window backward through consecutive   layers that carry no transfer of their own.</li> </ul>"},{"location":"api/strategies/#flextensor.strategy.transfer_window.TransferWindowCalculator.compute_window","title":"compute_window","text":"<pre><code>compute_window(layer_idx: int, layer_offload_sizes: ndarray, layer_durations: ndarray) -&gt; float\n</code></pre> <p>Compute the effective transfer window for a given layer.</p> PARAMETER DESCRIPTION <code>layer_idx</code> <p>Index of the layer whose transfer window to compute.</p> <p> TYPE: <code>int</code> </p> <code>layer_offload_sizes</code> <p>Per-layer offload sizes in bytes.  For pre-optimisation checks this reflects permanent (known) offload; during optimisation it reflects the candidate solution's offload pattern.</p> <p> TYPE: <code>ndarray</code> </p> <code>layer_durations</code> <p>Per-layer compute durations in milliseconds.</p> <p> TYPE: <code>ndarray</code> </p> RETURNS DESCRIPTION <code>float</code> <p>Effective transfer duration in milliseconds.</p>"},{"location":"api/strategies/#flextensor.strategy.transfer_window.SingleLayerWindow","title":"SingleLayerWindow","text":"<p>Transfer window limited to the immediately preceding layer's compute duration.</p> <p>This is the conservative (original) behaviour: the transfer for layer i must complete within layer i-1's compute time.</p>"},{"location":"api/strategies/#flextensor.strategy.transfer_window.SingleLayerWindow.compute_window","title":"compute_window","text":"<pre><code>compute_window(layer_idx: int, layer_offload_sizes: ndarray, layer_durations: ndarray) -&gt; float\n</code></pre> <p>Return <code>layer_durations[layer_idx - 1]</code>.</p>"},{"location":"api/strategies/#flextensor.strategy.transfer_window.GapAwareWindow","title":"GapAwareWindow","text":"<p>Transfer window that exploits gap layers for a larger budget.</p> <p>Sums backward from layer i-1 through consecutive layers whose next layer has no offloaded tensors (i.e. no competing transfer). This captures both permanent gaps (layers with no offloadable tensors at all) and dynamic gaps (layers the optimiser chose not to offload in the current candidate solution).</p> <p>A layer j is considered \"busy\" (stops the backward scan) when <code>layer_offload_sizes[j + 1] &gt; 0</code> \u2014 meaning that during layer j's compute, the transfer stream is occupied moving layer j+1's data.</p>"},{"location":"api/strategies/#flextensor.strategy.transfer_window.GapAwareWindow.compute_window","title":"compute_window","text":"<pre><code>compute_window(layer_idx: int, layer_offload_sizes: ndarray, layer_durations: ndarray) -&gt; float\n</code></pre> <p>Return the sum of durations of consecutive free layers before layer_idx.</p>"},{"location":"api/strategies/#evaluation-utilities","title":"Evaluation Utilities","text":""},{"location":"api/strategies/#flextensor.strategy.protocol.StrategyComputeError","title":"StrategyComputeError","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when a strategy cannot produce a valid result for the given inputs.</p> <p>This covers recoverable, expected failures such as insufficient layers for pipelining or missing memory-transfer data.  :class:<code>AdaptiveStrategy</code> catches this exception to skip a failing candidate and try the next one.</p> <p>Programming errors (<code>TypeError</code>, <code>AttributeError</code>, etc.) should not be wrapped in this exception \u2014 they must propagate so that bugs surface during development.</p>"},{"location":"api/strategies/#flextensor.strategy.evaluation.StrategyScore","title":"StrategyScore  <code>dataclass</code>","text":"<pre><code>StrategyScore(strategy_name: str, peak_memory_bytes: int, estimated_overhead: float, consecutive_violations: int, is_valid: bool)\n</code></pre> <p>Evaluation metrics for a single strategy result.</p> ATTRIBUTE DESCRIPTION <code>strategy_name</code> <p>Human-readable name of the strategy that produced the result.</p> <p> TYPE: <code>str</code> </p> <code>peak_memory_bytes</code> <p>Estimated peak GPU memory in bytes (blocks + non-offloaded tensors).</p> <p> TYPE: <code>int</code> </p> <code>estimated_overhead</code> <p>Fractional overhead from synchronous transfers. <code>0.0</code> means all transfers fit within compute windows (perfect pipelining). <code>0.05</code> means 5% extra time beyond the baseline compute duration.</p> <p> TYPE: <code>float</code> </p> <code>consecutive_violations</code> <p>Number of adjacent layer pairs assigned to the same block.</p> <p> TYPE: <code>int</code> </p> <code>is_valid</code> <p><code>True</code> when there are no violations and memory is within the limit.</p> <p> TYPE: <code>bool</code> </p>"},{"location":"api/strategies/#flextensor.strategy.evaluation.evaluate_strategy_result","title":"evaluate_strategy_result","text":"<pre><code>evaluate_strategy_result(result: StrategyResult, layer_stats: list[LayerStatistics], strategy_name: str, interpolator: MemoryTransferInterpolator | None = None, max_gpu_mem_bytes: int | None = None) -&gt; StrategyScore\n</code></pre> <p>Score a strategy result for comparison.</p> PARAMETER DESCRIPTION <code>result</code> <p>The :class:<code>StrategyResult</code> to evaluate.</p> <p> TYPE: <code>StrategyResult</code> </p> <code>layer_stats</code> <p>Layer statistics from profiling (used for durations and tensor sizes).</p> <p> TYPE: <code>list[LayerStatistics]</code> </p> <code>strategy_name</code> <p>Display name for this strategy candidate.</p> <p> TYPE: <code>str</code> </p> <code>interpolator</code> <p>Transfer-time interpolator.  Required for overhead calculation. When <code>None</code>, overhead is estimated as <code>0.0</code> (cannot be computed).</p> <p> TYPE: <code>MemoryTransferInterpolator | None</code> DEFAULT: <code>None</code> </p> <code>max_gpu_mem_bytes</code> <p>Hard GPU memory limit.  When set, results exceeding this limit are marked invalid.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>A</code> <p>class:<code>StrategyScore</code> summarising the result quality.</p> <p> TYPE: <code>StrategyScore</code> </p>"},{"location":"api/strategies/#flextensor.strategy.utils.strategy_has_transfer_gaps","title":"strategy_has_transfer_gaps","text":"<pre><code>strategy_has_transfer_gaps(strategy_map: dict[str, list[TensorStatistics]], layer_stats: list[LayerStatistics]) -&gt; bool\n</code></pre> <p>Check if a strategy has unused layer slots where transfer rearrangement would help.</p> <p>In the pipelined model, each of the first <code>N-1</code> layers can serve as a transfer slot (transferring the next layer's data during its compute). If some of those slots are unused (because the corresponding next layer has no offloaded tensors), <code>rearrange_transfers</code> can move other transfers into those gaps for better amortisation.</p> PARAMETER DESCRIPTION <code>strategy_map</code> <p>Mapping of layer labels to tensors being transferred during that layer's compute.</p> <p> TYPE: <code>dict[str, list[TensorStatistics]]</code> </p> <code>layer_stats</code> <p>Layer statistics for all layers in the model.</p> <p> TYPE: <code>list[LayerStatistics]</code> </p> RETURNS DESCRIPTION <code>bool</code> <p><code>True</code> if there are free slots that rearrangement could exploit.</p>"},{"location":"explanation/configuration/","title":"Configuration","text":""},{"location":"explanation/configuration/#understanding-flextensor-configuration","title":"Understanding FlexTensor Configuration","text":"<p>This document explains the configuration system in FlexTensor\u2014how it works, what each option controls, and how to make informed decisions about configuring tensor offloading for your workload.</p>"},{"location":"explanation/configuration/#overview","title":"Overview","text":"<p>FlexTensor uses <code>OffloadConfig</code> to control all aspects of tensor offloading behavior. The configuration system is designed around three principles:</p> <ol> <li>Sensible defaults: Works out-of-the-box for most use cases</li> <li>Progressive disclosure: Simple cases need few options; advanced tuning is available when needed</li> <li>Multiple sources: Configure via Python code, files, or environment variables</li> </ol> <pre><code>from flextensor import OffloadConfig, offload\n\n# Minimal configuration - uses sensible defaults\nconfig = OffloadConfig()\n\n# Tuned configuration for specific requirements\nconfig = OffloadConfig(\n    gpu_device=0,\n    warmup_iters=1,\n    profile_iters=10,\n)\n\nmodel = offload(model, config=config)\n</code></pre> <p>Single-thread only</p> <p>FlexTensor is not thread-safe. The entire offloading lifecycle \u2014 setup, warmup, profiling, and inference \u2014 must run on one thread. Do not call <code>offload()</code>, run forward passes on a patched model, or access the same offload manager from multiple threads concurrently. If you need per-thread offloading, use a separate named manager and model per thread (see Troubleshooting).</p>"},{"location":"explanation/configuration/#configuration-loading","title":"Configuration Loading","text":"<p>FlexTensor supports three ways to load configuration, with clear precedence rules.</p>"},{"location":"explanation/configuration/#precedence-order","title":"Precedence Order","text":"<p>When multiple sources provide the same option, higher precedence wins:</p> <ol> <li>Explicit kwargs (highest) \u2014 <code>OffloadConfig(gpu_device=1)</code></li> <li>Environment variables \u2014 <code>FT_GPU_DEVICE=1</code></li> <li>Configuration file \u2014 <code>gpu_device = 1</code> in file</li> <li>Default values (lowest) \u2014 Built-in defaults</li> </ol>"},{"location":"explanation/configuration/#loading-from-environment-variables","title":"Loading from Environment Variables","text":"<p>Environment variables use the <code>FT_</code> prefix by default:</p> <pre><code>export FT_ENABLED=1\nexport FT_GPU_DEVICE=0\nexport FT_WARMUP_ITERS=2\n</code></pre> <pre><code>from flextensor import load_config_from_env\n\nconfig = load_config_from_env()  # Reads FT_* variables\n</code></pre> <p>Environment-only Loading</p> <p>When loading from environment variables without a config file, <code>enabled</code> defaults to <code>False</code>. You must explicitly set <code>FT_ENABLED=1</code> to enable offloading. This prevents accidental offloading when environment variables are partially set.</p>"},{"location":"explanation/configuration/#loading-from-files","title":"Loading from Files","text":"<p>FlexTensor supports INI, JSON, and YAML formats:</p> INI FormatYAML FormatJSON Format <pre><code>; flextensor.conf\n[flextensor]\nenabled = true\ngpu_device = 0\nwarmup_iters = 1\nprofile_iters = 10\n</code></pre> <pre><code># flextensor.yaml\nenabled: true\ngpu_device: 0\nwarmup_iters: 1\nprofile_iters: 10\n</code></pre> <pre><code>{\n  \"enabled\": true,\n  \"gpu_device\": 0,\n  \"warmup_iters\": 1,\n  \"profile_iters\": 10\n}\n</code></pre> <pre><code>from flextensor import load_config, load_config_from_file\n\n# Auto-detect format from extension\nconfig = load_config(\"flextensor.yaml\")\n\n# Or use the file-only loader (no env override)\nconfig = load_config_from_file(\"flextensor.conf\")\n</code></pre>"},{"location":"explanation/configuration/#combined-loading","title":"Combined Loading","text":"<p>The <code>load_config()</code> function combines all sources with proper precedence:</p> <pre><code>from flextensor import load_config\n\n# File + env vars + kwargs (precedence: kwargs &gt; env &gt; file &gt; defaults)\nconfig = load_config(\n    config_path=\"flextensor.yaml\",  # Base configuration\n    use_env=True,                     # Override with FT_* env vars\n    gpu_device=1,                     # Override with explicit value\n)\n</code></pre>"},{"location":"explanation/configuration/#core-configuration-options","title":"Core Configuration Options","text":""},{"location":"explanation/configuration/#enabling-offloading","title":"Enabling Offloading","text":"Option Type Default Description <code>enabled</code> bool <code>True</code> Master switch for offloading <code>gpu_device</code> int <code>0</code> GPU device index to use <code>module_patterns</code> list[str] <code>[\"*\"]</code> Module path patterns to offload (supports <code>*</code> and <code>?</code> wildcards). Default <code>[\"*\"]</code> works for quick experimentation; use specific patterns (e.g., <code>model.layers.*</code>) for transformer pipelining."},{"location":"explanation/configuration/#module-patterns","title":"Module Patterns","text":"<p>The <code>module_patterns</code> option specifies which modules in the model to offload. Patterns support <code>*</code> (match any sequence) and <code>?</code> (match a single character) wildcards:</p> <pre><code>config = OffloadConfig(\n    module_patterns=[\"layers.*\", \"embed\", \"head\"],\n)\nmodel = offload(model, config=config)\n</code></pre> <p>Patterns can also be set via environment variable as a comma-separated list:</p> <pre><code>FT_MODULE_PATTERNS=\"layers.*,embed,head\" python my_script.py\n</code></pre> <p>vLLM worker default patterns</p> <p>The <code>FlexTensorOffloadWorker</code> uses these patterns by default when no custom <code>FT_MODULE_PATTERNS</code> is set. They are designed for LLaMA-style model layouts as served by vLLM:</p> <pre><code>config = OffloadConfig(\n    module_patterns=[\n        \"model.embed_tokens\",\n        \"model.layers.*\",\n        \"model.norm\",\n        \"lm_head\",\n        \"logits_processor\",\n    ]\n)\n</code></pre> <p>These patterns give each transformer layer its own offload trap, enabling FlexTensor to overlap CPU\u2192GPU transfers with GPU computation. For models with a different layout, inspect module names with <code>model.named_modules()</code> and set <code>FT_MODULE_PATTERNS</code> accordingly.</p> <p>The default <code>[\"*\"]</code> matches all top-level child modules. It is a reasonable starting point for quick experimentation, but for transformer models in production, prefer specific patterns such as <code>model.layers.*</code>. With <code>[\"*\"]</code>, every top-level child is wrapped in a single coarse trap, which prevents per-layer pipelining. With patterns like <code>model.layers.*</code>, each layer gets its own trap, and FlexTensor can overlap CPU\u2192GPU transfers for layer N+1 while the GPU computes layer N.</p> <p>When <code>enabled=False</code>, FlexTensor passes through to normal PyTorch execution with no overhead. This is useful for:</p> <ul> <li>Toggling offloading in production without code changes</li> <li>A/B testing offloading vs. baseline</li> <li>Disabling offloading on machines with sufficient GPU memory</li> </ul>"},{"location":"explanation/configuration/#memory-management","title":"Memory Management","text":"Option Type Default Description <code>pinned_memory</code> bool <code>True</code> Use pinned memory for CPU tensors <code>shm_enabled</code> bool <code>False</code> Enable cross-process weight sharing via POSIX shared memory <code>shm_namespace</code> str | None <code>None</code> Base namespace for SHM blocks (auto-derived if None) <code>shm_wait_timeout</code> float <code>0.0</code> Hard timeout (seconds) for followers waiting on creator <code>release_tensors</code> bool <code>True</code> Release GPU tensors after layer execution <code>max_gpu_mem_fraction</code> float | None <code>0.9</code> GPU memory budget as a fraction of total device memory (e.g. <code>0.9</code> = 90%). <code>None</code> = latency mode (no constraint). <code>max_gpu_mem_bytes</code> int | None <code>None</code> Deprecated since v0.4 \u2014 use <code>max_gpu_mem_fraction</code> instead. Will be removed in v0.5."},{"location":"explanation/configuration/#pinned-memory","title":"Pinned Memory","text":"<p>When <code>pinned_memory=True</code>, CPU tensors use page-locked memory, which enables:</p> <ul> <li>Faster CPU\u2194GPU transfers (DMA \u2014 Direct Memory Access \u2014 without CPU involvement)</li> <li>Overlapping computation with transfers</li> </ul> <p>The trade-off is increased CPU memory usage and allocation time. Disable for systems with limited CPU memory:</p> <pre><code>config = OffloadConfig(pinned_memory=False)  # Lower CPU memory overhead\n</code></pre>"},{"location":"explanation/configuration/#gpu-memory-limit","title":"GPU Memory Limit","text":"<p><code>max_gpu_mem_fraction</code> controls how much of the GPU's total memory FlexTensor may use. It accepts a <code>float</code> in the range <code>(0.0, 1.0]</code>, where <code>0.9</code> means \"use at most 90% of total device memory.\"</p> <p>When set to a fraction, the strategy operates in memory mode and keeps peak GPU usage within that budget. The fraction is resolved to an absolute byte count at runtime via <code>torch.cuda.get_device_properties()</code>, so the same config works portably across GPU SKUs with different memory capacities.</p> <pre><code>config = OffloadConfig(\n    max_gpu_mem_fraction=0.9,  # Default: 90% of total GPU memory\n)\n</code></pre> <p>When set to <code>None</code>, the strategy switches to latency mode and optimises for throughput based on the <code>knapsack_scale</code> factor, with no explicit memory cap:</p> <pre><code>config = OffloadConfig(\n    max_gpu_mem_fraction=None,  # Latency mode: no memory constraint\n)\n</code></pre> <p>Default changed in v0.4</p> <p>Before v0.4, <code>max_gpu_mem_fraction</code> did not exist and <code>max_gpu_mem_bytes</code> defaulted to <code>None</code> (latency mode). As of v0.4, the default is <code>0.9</code> (memory mode). If your workload previously relied on latency mode by default, set <code>max_gpu_mem_fraction=None</code> explicitly.</p> <p>This option can also be set via the <code>FT_MAX_GPU_MEM_FRACTION</code> environment variable:</p> <pre><code>FT_MAX_GPU_MEM_FRACTION=0.8   # Use 80% of GPU memory (memory mode)\nFT_MAX_GPU_MEM_FRACTION=none   # Switch to latency mode (also accepts \"null\" or \"\")\n</code></pre> <p>Deprecated: <code>max_gpu_mem_bytes</code></p> <p><code>max_gpu_mem_bytes</code> and its env var <code>FT_MAX_GPU_MEM_BYTES</code> are deprecated since v0.4 and will be removed in v0.5. Replace any use of <code>max_gpu_mem_bytes=N</code> with <code>max_gpu_mem_fraction=N / total_gpu_bytes</code>, or use <code>max_gpu_mem_fraction=0.9</code> to target 90% of device memory portably.</p>"},{"location":"explanation/configuration/#shared-memory-cross-process-weight-sharing","title":"Shared Memory (Cross-Process Weight Sharing)","text":"<p>When <code>shm_enabled=True</code>, FlexTensor stores model weights in POSIX shared memory so multiple processes (e.g., vLLM data-parallel replicas) can share a single copy of the weights in CPU RAM. The first process to start becomes the creator and loads the model normally; subsequent processes become followers and attach to existing shared memory blocks without re-loading from disk.</p> <pre><code>config = OffloadConfig(shm_enabled=True)  # Enable cross-process weight sharing\n</code></pre> <p>The <code>shm_namespace</code> is auto-derived from the model path, config fields, and manager name, ensuring that different models, config variations, or named <code>OffloadManager</code> instances get separate shared memory regions. Override with an explicit namespace when needed:</p> <pre><code>config = OffloadConfig(shm_enabled=True, shm_namespace=\"my_model_v1\")\n</code></pre> <p>When <code>shm_wait_timeout</code> is <code>0.0</code> (the default), followers rely on heartbeat liveness detection rather than a hard wall-clock timeout.</p> <p>These options can also be set via <code>FT_SHM_ENABLED</code>, <code>FT_SHM_NAMESPACE</code>, and <code>FT_SHM_WAIT_TIMEOUT</code> environment variables.</p>"},{"location":"explanation/configuration/#profiling-configuration","title":"Profiling Configuration","text":"Option Type Default Description <code>warmup_iters</code> int <code>1</code> Iterations to discover tensor-layer relationships <code>profile_iters</code> int <code>10</code> Iterations to measure execution timing <p>FlexTensor learns your model's behavior during initial iterations:</p> <pre><code>First N iterations:  WARMUP (warmup_iters) \u2192 PROFILE (profile_iters) \u2192 INFERENCE\nRemaining iterations: Optimized execution with learned strategy\n</code></pre>"},{"location":"explanation/configuration/#tuning-iteration-counts","title":"Tuning Iteration Counts","text":"<p><code>warmup_iters</code>: Usually 1 is sufficient. Increase if your model has:</p> <ul> <li>Dynamic control flow affecting tensor access patterns</li> <li>Variable-length inputs that change which tensors are used</li> </ul> <p><code>profile_iters</code>: More iterations = more accurate timing estimates. Consider:</p> <ul> <li>Noisy environments: Increase to 20-50 for shared/cloud GPUs</li> <li>Deterministic workloads: 5-10 is often sufficient</li> <li>Quick experimentation: Reduce to 3-5 for faster iteration</li> </ul> <pre><code># Production deployment (accurate profiling)\nconfig = OffloadConfig(warmup_iters=1, profile_iters=20)\n\n# Development/debugging (fast iteration)\nconfig = OffloadConfig(warmup_iters=1, profile_iters=3)\n</code></pre>"},{"location":"explanation/configuration/#transfer-modes","title":"Transfer Modes","text":"Option Type Default Description <code>transfer_mode</code> str <code>\"allocation_block_transfer\"</code> Tensor transfer strategy <code>num_blocks</code> int <code>4</code> Number of memory blocks for block transfer <code>min_blocks</code> int <code>4</code> Minimum blocks for assignment optimization <code>enable_direct_mode</code> bool <code>True</code> Use direct tensor access (lower overhead) <p>FlexTensor supports different transfer strategies:</p> Mode Description Best For <code>strategy</code> Basic strategy-based loading Simple models, debugging <code>allocation_block_transfer</code> Pre-allocated GPU blocks Most production workloads <code>raw_block_transfer</code> Direct memory management Maximum efficiency <p>The default <code>allocation_block_transfer</code> provides a good balance of performance and memory efficiency by pre-allocating GPU memory blocks and reusing them across layers.</p>"},{"location":"explanation/configuration/#minimum-blocks","title":"Minimum Blocks","text":"<p><code>min_blocks</code> sets the lower bound of the block-count search range used by the assignment optimizer. Lower values give the optimizer more freedom and reduce GPU memory consumed by FlexTensor's memory blocks, but may hurt pipelining throughput. The value must be at least 2 (pipelined execution requires two blocks).</p> <pre><code>config = OffloadConfig(\n    num_blocks=4,\n    min_blocks=2,  # Let optimizer try fewer blocks to save GPU memory\n)\n</code></pre> <p>This option can also be set via the <code>FT_MIN_BLOCKS</code> environment variable.</p>"},{"location":"explanation/configuration/#gap-layers-and-transfer-windows","title":"Gap Layers and Transfer Windows","text":"<p>Gap layers are layers that contain no offloadable tensors. When a model has sequential gap layers between offloadable layers, the effective transfer window \u2014 the time available to pre-fetch the next layer's tensors \u2014 is larger than a single layer's execution time.</p> <p><code>GapAwareWindow</code> exploits this by computing transfer windows that span gap layers, allowing FlexTensor to start pre-fetching earlier. <code>strategy_has_transfer_gaps()</code> detects whether a computed strategy contains such gaps. When gap layers are detected during inference setup, <code>rearrange_transfers</code> is automatically enabled to take advantage of the extended windows.</p>"},{"location":"explanation/configuration/#strategy-configuration","title":"Strategy Configuration","text":"Option Type Default Description <code>knapsack_scale</code> float <code>1.0</code> Scale factor for knapsack algorithm <code>load_strategy</code> Strategy <code>None</code> Override automatic strategy selection"},{"location":"explanation/configuration/#understanding-strategies","title":"Understanding Strategies","text":"<p>FlexTensor includes several offloading strategies. The table below is a conceptual comparison of the most commonly used strategies; it is not a complete list. See the Strategies reference page for the full API.</p> Strategy Algorithm Best For <code>KnapsackStrategy</code> Dynamic programming optimization General use <code>GreedyStrategy</code> Largest tensors first Quick approximation <code>NthLayerStrategy</code> Offload every Nth layer Predictable patterns <code>AdaptiveKnapsackStrategy</code> Knapsack with runtime adaptation Variable workloads <code>AdaptiveStrategy</code> Evaluates multiple candidates, selects best Default automatic selection <code>GlobalOffloadStrategy</code> Global optimizer across all layers Maximizing overall memory reduction <code>GlobalTensorSelectionStrategy</code> Metaheuristic search (<code>\"DE\"</code> or <code>\"SA\"</code>) Highest-quality solution when runtime allows <p>When <code>load_strategy=None</code> (the default), FlexTensor runs <code>AdaptiveStrategy</code> internally. <code>AdaptiveStrategy</code> evaluates multiple candidate strategies and selects the one with the lowest estimated overhead that satisfies the memory constraint. To include slower but potentially higher-quality <code>GlobalTensorSelectionStrategy</code> candidates in that evaluation, create an <code>AdaptiveStrategy</code> with <code>extra_optimization=True</code> and assign it to <code>load_strategy</code>:</p> <pre><code>from flextensor import OffloadConfig, AdaptiveStrategy\n\nconfig = OffloadConfig(\n    load_strategy=AdaptiveStrategy(extra_optimization=True)\n)\n</code></pre> <p>Override <code>load_strategy</code> only when you have specific requirements:</p> <pre><code>from flextensor import OffloadConfig, GreedyStrategy\n\n# Force greedy strategy for faster startup (less optimal offloading)\nconfig = OffloadConfig(load_strategy=GreedyStrategy())\n</code></pre>"},{"location":"explanation/configuration/#advanced-transfer-options","title":"Advanced Transfer Options","text":"Option Type Default Description <code>rearrange_transfers</code> bool <code>False</code> Optimize transfer scheduling <code>compute_transfer_gap</code> int <code>1</code> Minimum gap between compute and transfer <p>These options control transfer scheduling optimizations:</p> <ul> <li><code>rearrange_transfers</code>: When enabled, FlexTensor may reorder tensor transfers to better overlap with computation. FlexTensor also auto-enables <code>rearrange_transfers</code> when it detects permanent gap layers (layers with no offloadable tensors) during inference setup. You may therefore observe this behavior even when you have not set the option explicitly.</li> <li><code>compute_transfer_gap</code>: Ensures transfers are initiated N layers before tensors are needed, hiding transfer latency</li> </ul>"},{"location":"explanation/configuration/#profile-storage","title":"Profile Storage","text":"Option Type Default Description <code>profile_storage_dir</code> str <code>None</code> Directory for profile persistence <code>profile_read_only</code> bool <code>False</code> Only load profiles, don't save <p>Profile storage enables skipping the warmup/profile phases on subsequent runs by saving and loading offload profiles. Profiles are stored as JSON files in the specified directory.</p> <pre><code>config = OffloadConfig(\n    profile_storage_dir=\"/tmp/flextensor_profiles\",\n    profile_read_only=False,  # Allow saving profiles (default)\n)\n\n# First run: warmup \u2192 profile \u2192 save \u2192 inference\nom = flextensor.get_offload_manager()\nmodel = om.offload(model, config=config)\nfor _ in range(config.warmup_iters + config.profile_iters):\n    model(sample_input)\nom.save_profile()  # Saves to profile_storage_dir\n\n# Subsequent runs: load profile \u2192 inference (faster startup)\nom = flextensor.get_offload_manager()\nom.set_config(config)\nom.load_profile(model=model)  # Loads from profile_storage_dir\n</code></pre> <p>Set <code>profile_read_only=True</code> to prevent accidental profile overwrites in production deployments.</p>"},{"location":"explanation/configuration/#debugging-options","title":"Debugging Options","text":"Option Type Default Description <code>enable_tracing</code> bool <code>False</code> Enable tensor access tracing <code>enable_instrumentation</code> bool <code>False</code> Capture component initialization args <code>instrumentation_output_dir</code> str <code>\".flextensor/instrumentation\"</code> Instrumentation output directory <code>enable_diagnostics</code> bool <code>False</code> Log memory transfer statistics, layer duration statistics, and block assignment table after strategy computation <p>These options help diagnose offloading behavior:</p> <ul> <li><code>enable_tracing</code>: Records tensor access patterns during profiling.</li> <li><code>enable_instrumentation</code>: Captures the arguments passed to FlexTensor components at initialization time and writes them to <code>instrumentation_output_dir</code>. Useful for reproducing configuration state.</li> <li><code>enable_diagnostics</code>: Logs three tables after strategy computation: a Memory Transfer Statistics table (tensor size \u2192 transfer time and bandwidth), a Layer Duration Statistics table (per-trap timing: min, max, median, avg, std, coefficient of variation), and the block assignment table (at NOTICE level 25). The Layer Duration Statistics table lists every offload trap created during profiling \u2014 it is the authoritative way to confirm which module patterns were applied as traps. Useful for diagnosing per-layer pipelining setup and inspecting why specific tensors were assigned to specific pipeline blocks.</li> </ul> <p>The distinction between <code>enable_diagnostics</code> and <code>enable_instrumentation</code> is scope: <code>enable_diagnostics</code> reports strategy decisions (which tensors landed in which block and why); <code>enable_instrumentation</code> captures component initialization arguments (how each component was configured).</p> <pre><code># Enable detailed tracing for debugging\nconfig = OffloadConfig(\n    enable_tracing=True,\n    enable_instrumentation=True,\n)\n\n# Inspect strategy decisions\nconfig = OffloadConfig(enable_diagnostics=True)\n</code></pre> <p>Performance Impact</p> <p>Tracing and instrumentation add significant overhead. Use only for debugging, not production.</p>"},{"location":"explanation/configuration/#tensor-discovery","title":"Tensor Discovery","text":"Option Type Default Description <code>enable_untraced_tensor_discovery</code> bool <code>True</code> Discover untraced tensors (e.g., FP8 weights passed to Triton kernels) <code>enable_module_tracker</code> bool <code>True</code> Enable ModuleTracker for manual traps when forward patching is not used <p>Tensor discovery ensures that all tensors are properly tracked, even those accessed by custom kernels that bypass PyTorch's dispatch system (such as Triton kernels used in FP8 inference). In most cases, leave both options enabled.</p> <p>You might disable <code>enable_untraced_tensor_discovery</code> if:</p> <ul> <li>Your model does not use FP8 or custom kernels</li> <li>You are debugging and want to isolate issues</li> <li>You have verified all tensors are traced normally</li> </ul> <p>See Tensor Discovery for a detailed explanation of how the discovery mechanism works.</p>"},{"location":"explanation/configuration/#configuration-recipes","title":"Configuration Recipes","text":"<p>For ready-to-use starting configurations covering memory-constrained systems, performance-focused deployments, quick experimentation, and production, see How to Configure FlexTensor for Common Scenarios.</p>"},{"location":"explanation/configuration/#summary","title":"Summary","text":"Category Key Options Typical Tuning Core <code>enabled</code>, <code>gpu_device</code> Set based on deployment Memory <code>pinned_memory</code>, <code>release_tensors</code>, <code>max_gpu_mem_fraction</code>, <code>shm_enabled</code> Balance memory vs. performance Profiling <code>warmup_iters</code>, <code>profile_iters</code> More iters = more accurate Transfer <code>transfer_mode</code>, <code>num_blocks</code>, <code>min_blocks</code> Default works for most cases Debug <code>enable_tracing</code> Only when troubleshooting <p>Start with defaults, measure performance, and tune based on your specific workload characteristics. The profiling system will adapt to your model's actual behavior, so explicit strategy configuration is rarely needed.</p>"},{"location":"explanation/states/","title":"Internal States","text":""},{"location":"explanation/states/#understanding-flextensors-internal-states","title":"Understanding FlexTensor's Internal States","text":"<p>This document explains the internal state machine that FlexTensor uses to manage tensor offloading. These states are implementation details that users don't need to interact with directly\u2014FlexTensor handles state transitions automatically. However, understanding them helps explain what happens during model execution and aids in debugging.</p>"},{"location":"explanation/states/#overview","title":"Overview","text":"<p>FlexTensor uses an internal state machine with four states: Not Initialized, Warmup, Profile, and Inference. After initialization, the system automatically progresses through three active states (Warmup, Profile, Inference) during the first few model iterations.</p>"},{"location":"explanation/states/#why-states","title":"Why States?","text":"<p>Tensor offloading presents a fundamental challenge: to efficiently move tensors between CPU and GPU, we need to know which tensors to offload and when to transfer them. Making poor decisions leads to either memory exhaustion (offloading too little) or performance degradation (offloading too much or at the wrong time).</p> <p>FlexTensor solves this by learning about your model's tensor usage patterns before optimizing:</p> <ol> <li>Discovers which tensors belong to which layers (Warmup state)</li> <li>Measures execution timing with tensor loading (Profile state)</li> <li>Applies an optimized offloading strategy (Inference state)</li> </ol> <p>This learning process happens transparently during the first few iterations of model execution.</p>"},{"location":"explanation/states/#state-progression","title":"State Progression","text":"<pre><code>NOT_INITIALIZED \u2192 WARMUP \u2192 PROFILE \u2192 INFERENCE\n                    \u2193          \u2193\n              (warmup_iters) (profile_iters)\n</code></pre> <p>The <code>OffloadManager</code> tracks the current state internally and transitions automatically based on iteration counts configured via <code>OffloadConfig</code>:</p> <pre><code>config = OffloadConfig(\n    warmup_iters=1,    # Iterations in warmup state\n    profile_iters=10,  # Iterations in profile state\n)\n</code></pre>"},{"location":"explanation/states/#warmup-state","title":"Warmup State","text":""},{"location":"explanation/states/#purpose","title":"Purpose","text":"<p>The warmup state identifies the relationship between layers and tensors. It answers the question: \"Which tensors does each layer use?\"</p>"},{"location":"explanation/states/#what-happens-internally","title":"What Happens Internally","text":"<ol> <li>Model Preprocessing: Tensors are moved to CPU and optionally pinned in memory for faster transfers</li> <li>Tensor Tracking: Each tensor operation is intercepted via <code>WarmupTrap</code></li> <li>On-Demand Transfer: When a layer needs a tensor, it's copied to GPU, used, then released</li> <li>Statistics Collection: The system records which tensor IDs are accessed by each layer</li> </ol>"},{"location":"explanation/states/#internal-mechanics","title":"Internal Mechanics","text":"<p>During warmup, the <code>WarmupTrap</code> context manager:</p> <ul> <li>Intercepts all PyTorch operations via <code>__torch_function__</code></li> <li>Tracks tensor IDs used in each operation</li> <li>Copies tensors to GPU on-demand when needed for computation</li> <li>Measures baseline layer execution time</li> <li>Collects tensor-to-layer mapping data</li> </ul> <pre><code># Internal flow during warmup (simplified)\nwith WarmupTrap(tensor_manager, layer_name, device_gpu):\n    # Tensors are copied to GPU as needed\n    # Tensor IDs are recorded\n    # Timing is measured\n    output = layer(input)\n</code></pre>"},{"location":"explanation/states/#why-it-matters","title":"Why It Matters","text":"<p>Without warmup, we wouldn't know which tensors to preload for each layer. This mapping is essential for the profile state to measure realistic transfer overhead.</p>"},{"location":"explanation/states/#outputs-and-model-state","title":"Outputs and Model State","text":"<p>At the end of the warmup state, FlexTensor has collected:</p> Output Description <code>tensors_map</code> Dictionary mapping tensor IDs to CPU tensor references <code>layer_statistics_collector</code> Contains tensor-to-layer mappings and baseline timing per layer <code>model_ids</code> Set of all tensor IDs belonging to the model <p>Model state after warmup: - All model tensors reside on CPU memory - Tensors are pinned (if <code>pinned_memory=True</code>) for faster GPU transfers - The model structure is unchanged\u2014only tensor locations have moved - No GPU memory is permanently allocated yet (tensors were copied on-demand and released)</p> <p>This CPU-resident model with collected statistics is the foundation for the profile state.</p>"},{"location":"explanation/states/#profile-state","title":"Profile State","text":""},{"location":"explanation/states/#purpose_1","title":"Purpose","text":"<p>The profile state measures how long each layer takes to execute with tensor loading overhead included. It answers: \"How much time does each layer need, including tensor transfers?\"</p>"},{"location":"explanation/states/#what-happens-internally_1","title":"What Happens Internally","text":"<ol> <li>Statistics Initialization: Layer statistics from warmup are processed</li> <li>Tensor Layer Loader Setup: A loader is configured with the tensor-to-layer mapping</li> <li>Detailed Timing: Each layer's execution time is measured with CUDA synchronization</li> <li>Duration Collection: Statistics are accumulated across multiple iterations for accuracy</li> </ol>"},{"location":"explanation/states/#internal-mechanics_1","title":"Internal Mechanics","text":"<p>The profile state uses either <code>Trap</code> (indirect mode) or <code>TrapDirect</code> (direct mode):</p> <p>Indirect Mode (<code>Trap</code>): - Uses PyTorch's <code>TorchFunctionMode</code> to intercept operations - Replaces CPU tensor references with GPU copies on-the-fly - Suitable for models where tensor access patterns vary</p> <p>Direct Mode (<code>TrapDirect</code>): - Uses regular context managers without function interception - Model is pre-patched to use GPU tensor references directly - Lower overhead, suitable for most transformer architectures</p> <pre><code># Internal flow during profile (simplified)\nwith Trap(tensor_manager, layer_name, device_gpu):\n    # tensor_layer_loader.enter() - preload tensors\n    timer_start = time.time_ns()\n\n    output = layer(input)\n\n    torch.cuda.synchronize()\n    timer_end = time.time_ns()\n    # tensor_layer_loader.exit() - cleanup\n    # Duration recorded for layer\n</code></pre>"},{"location":"explanation/states/#why-it-matters_1","title":"Why It Matters","text":"<p>Profile data enables the offloading strategy (e.g., Knapsack, Greedy, Adaptive, Global) to make informed decisions about:</p> <ul> <li>Which tensors to keep on GPU vs. offload to CPU</li> <li>When to initiate transfers to hide latency</li> <li>How to batch transfers for efficiency</li> </ul>"},{"location":"explanation/states/#outputs-and-model-state_1","title":"Outputs and Model State","text":"<p>At the end of the profile state, FlexTensor has collected:</p> Output Description <code>layer_stats</code> Filtered layer statistics with tensor IDs and accumulated timing data <code>tensor_statistics_map</code> Per-tensor statistics (size, transfer time estimates) <code>tensor_layer_loader</code> Configured loader with tensor-to-layer mapping <p>Model state after profile: - Model tensors still reside on CPU memory - In direct mode: model may have been copied with shared tensor references for profiling - <code>layer_statistics_collector</code> contains averaged timing across all profile iterations - System is ready to compute the optimal offloading strategy</p> <p>The collected timing data is the input for strategy computation in the inference state.</p>"},{"location":"explanation/states/#inference-state","title":"Inference State","text":""},{"location":"explanation/states/#purpose_2","title":"Purpose","text":"<p>The inference state applies the computed offloading strategy for production execution. It answers: \"How do we execute optimally based on what we learned?\"</p>"},{"location":"explanation/states/#what-happens-internally_2","title":"What Happens Internally","text":"<ol> <li>Strategy Computation: The load strategy (e.g., Knapsack) determines which tensors to load at each layer</li> <li>Release Strategy: Complementary strategy determines when to release GPU memory</li> <li>Tensor Loader Selection: The appropriate loader is configured based on <code>transfer_mode</code></li> <li>Model Finalization: The model is patched with optimized tensor access patterns</li> </ol>"},{"location":"explanation/states/#internal-mechanics_2","title":"Internal Mechanics","text":"<p>The inference state uses <code>TrapInfer</code> or <code>TrapInferDirect</code>:</p> <p>Key Differences from Profile: - No timing collection (removes measurement overhead) - Tensor loading follows the precomputed schedule - Memory is released according to the release strategy - Optimized for throughput rather than data collection</p>"},{"location":"explanation/states/#transfer-modes","title":"Transfer Modes","text":"<p>FlexTensor supports different transfer strategies internally:</p> Mode Description <code>strategy</code> Basic strategy-based loading and releasing <code>allocation_block_transfer</code> Pre-allocated GPU blocks with batch transfers <code>raw_block_transfer</code> Direct memory management for maximum efficiency"},{"location":"explanation/states/#why-it-matters_2","title":"Why It Matters","text":"<p>The inference state is where the performance gains are realized. By using the learned statistics and optimized strategies, FlexTensor achieves:</p> <ul> <li>Minimal GPU memory usage (tensors offloaded to CPU when not needed)</li> <li>Low latency overhead (transfers scheduled to overlap with computation)</li> <li>Consistent performance (deterministic based on learned patterns)</li> </ul>"},{"location":"explanation/states/#outputs-and-model-state_2","title":"Outputs and Model State","text":"<p>At the end of state transition to inference, FlexTensor has computed:</p> Output Description <code>load_strategy</code> Per-layer decisions on which tensors to load to GPU <code>release_strategy</code> Per-layer decisions on which tensors to release from GPU <code>tensor_layer_loader</code> Production-optimized loader (e.g., <code>TensorStrategyLoader</code> or block-based) <code>stats</code> Computed layer statistics with sizes and timing <p>Model state in inference: - Model is finalized via <code>prepare_final_model()</code> with optimized tensor access - Tensors are loaded to GPU on-demand per the computed schedule - GPU memory is dynamically allocated and released according to the release strategy - The model reference in tensor manager is cleared (<code>self.model = None</code>) as it's no longer needed for profiling</p> <p>Runtime behavior: - Tensors are preloaded to GPU before each layer executes (<code>tensor_layer_loader.enter()</code>) - After layer execution, tensors may be released based on the release strategy (<code>tensor_layer_loader.exit()</code>) - No timing collection overhead\u2014optimized for production throughput</p> <p>This is the steady-state for all subsequent model executions.</p>"},{"location":"explanation/states/#state-transitions","title":"State Transitions","text":"<p>The <code>OffloadManager</code> handles state transitions automatically and internally:</p> <pre><code>def update_state(self):\n    \"\"\"Check and update state if necessary.\"\"\"\n    if self._current_state in {OffloadState.NOT_INITIALIZED, OffloadState.INFERENCE}:\n        return\n    self._iteration_count += 1\n\n    if self._current_state == OffloadState.WARMUP and self._iteration_count &gt;= self.config.warmup_iters:\n        self._transition_to_profile()\n    elif self._current_state == OffloadState.PROFILE and self._iteration_count &gt;= self.config.profile_iters:\n        self._transition_to_inference()\n</code></pre> <p>Each transition: 1. Prepares the tensor manager for the new state 2. Updates the model with appropriate hooks 3. Resets the iteration counter</p> <p>Users don't need to call this method\u2014it's invoked automatically when using <code>offload_block</code> or patched modules.</p>"},{"location":"explanation/states/#configuration","title":"Configuration","text":"<p>While users don't interact with states directly, they can influence state behavior through configuration:</p>"},{"location":"explanation/states/#warmup-iterations-warmup_iters","title":"Warmup Iterations (<code>warmup_iters</code>)","text":"<ul> <li>Default: 1</li> <li>Purpose: How many iterations to run in warmup state</li> <li>Guidance: Usually 1 is sufficient unless your model has dynamic tensor access patterns</li> </ul>"},{"location":"explanation/states/#profile-iterations-profile_iters","title":"Profile Iterations (<code>profile_iters</code>)","text":"<ul> <li>Default: 10</li> <li>Purpose: How many iterations to collect timing data</li> <li>Guidance: More iterations = more accurate statistics, but longer startup time</li> </ul>"},{"location":"explanation/states/#example","title":"Example","text":"<pre><code>from flextensor import OffloadConfig, get_offload_manager, offload\n\n# Configuration affects internal state duration\nconfig = OffloadConfig(\n    warmup_iters=1,\n    profile_iters=5,\n    module_patterns=[\"layers.*\"],\n)\n\nmodel = offload(model, config=config)\n\n# First warmup_iters + profile_iters iterations: internal learning\n# Subsequent iterations: optimized inference\nfor batch in dataloader:\n    output = model(batch)\n</code></pre>"},{"location":"explanation/states/#design-consequences","title":"Design Consequences","text":"<p>Understanding the state machine explains some behaviors that might otherwise seem surprising.</p> <p>Profile iteration count affects strategy quality. The profile state accumulates timing statistics across <code>profile_iters</code> iterations. Too few iterations can produce inaccurate estimates, especially on systems with thermal throttling or when sharing the GPU with other processes. If you observe variable inference performance, increasing <code>profile_iters</code> gives the strategy more data to work with.</p> <p>Tensor-to-layer mapping is fixed after warmup. The warmup state builds a complete map of which tensors belong to which layers. Any module added to the model after <code>offload()</code> is called will not be part of this map and will not be offloaded. The system does not re-run warmup when the model changes.</p> <p>State transitions are irreversible within a run. Once FlexTensor enters inference state, it does not return to warmup or profile. To re-profile (for example, after changing the model), call <code>release()</code> and then <code>offload()</code> again.</p> <p>Profile persistence can skip warmup and profile. By saving the offloading profile after the first run (using <code>save_profile()</code>), subsequent runs can load the saved state (using <code>load_profile()</code>) and skip the warmup and profile phases entirely. The loaded state contains the tensor-to-layer mappings, timing statistics, and computed strategy, allowing the system to proceed directly to inference-ready configuration. See <code>OffloadConfig.profile_storage_dir</code> for configuration.</p> <p>For practical guidance on avoiding mistakes related to these behaviors, see Troubleshooting.</p>"},{"location":"explanation/states/#summary","title":"Summary","text":"State Purpose Internal Trap Class Key Output Warmup Discover tensor-layer relationships <code>WarmupTrap</code> Tensor ID mapping Profile Measure execution timing <code>Trap</code> / <code>TrapDirect</code> Layer statistics Inference Apply optimized strategy <code>TrapInfer</code> / <code>TrapInferDirect</code> Production execution <p>This state machine approach enables FlexTensor to intelligently offload tensors without requiring manual configuration. By learning from your model's actual behavior, it achieves near-optimal memory efficiency with minimal performance overhead -- all transparently managed as an internal implementation detail.</p>"},{"location":"explanation/tensor-discovery/","title":"Tensor Discovery","text":""},{"location":"explanation/tensor-discovery/#understanding-untraced-tensor-discovery","title":"Understanding Untraced Tensor Discovery","text":"<p>This document explains how FlexTensor discovers tensors that aren't traced through normal PyTorch function interception. Understanding this mechanism helps explain why certain model architectures work seamlessly with FlexTensor and aids in debugging edge cases.</p>"},{"location":"explanation/tensor-discovery/#the-problem-untraced-tensors","title":"The Problem: Untraced Tensors","text":"<p>During the warmup state, FlexTensor intercepts PyTorch operations via <code>__torch_function__</code> to build a map of which tensors belong to which layers. This works well for standard PyTorch operations, but some tensors slip through:</p>"},{"location":"explanation/tensor-discovery/#why-some-tensors-go-untraced","title":"Why Some Tensors Go Untraced","text":"<ol> <li> <p>Direct kernel calls: Custom CUDA or Triton kernels that bypass PyTorch's dispatch system. FP8 (8-bit floating point) inference frameworks like Transformer Engine often use Triton kernels that access weight tensors directly.</p> </li> <li> <p>Optimized fused operations: Fused attention or MLP implementations may access parameters through C++ bindings that don't invoke <code>__torch_function__</code>.</p> </li> <li> <p>Lazy tensor access: Parameters that are only accessed under certain conditions during inference but not during warmup.</p> </li> </ol> <p>The consequence: if FlexTensor doesn't know a tensor belongs to a layer, it can't offload it properly. The tensor either stays on GPU (wasting memory) or gets offloaded incorrectly (causing errors).</p>"},{"location":"explanation/tensor-discovery/#example-fp8-weight-scales","title":"Example: FP8 Weight Scales","text":"<p>Consider a model using FP8 quantization:</p> <pre><code>class FP8Linear(torch.nn.Module):\n    def __init__(self, in_features, out_features):\n        super().__init__()\n        self.weight = torch.nn.Parameter(...)  # FP8 tensor\n        self.weight.scale = torch.tensor(...)  # Scale factor for dequantization\n\n    def forward(self, x):\n        # Triton kernel accesses weight.scale directly\n        return triton_fp8_matmul(x, self.weight, self.weight.scale)\n</code></pre> <p>During warmup, FlexTensor traces <code>self.weight</code> through PyTorch operations. But <code>self.weight.scale</code> is accessed by the Triton kernel, which doesn't trigger <code>__torch_function__</code>. Without tensor discovery, the scale tensor would be left on CPU when the weight is loaded to GPU, causing a device mismatch error.</p>"},{"location":"explanation/tensor-discovery/#the-solution-multi-strategy-discovery","title":"The Solution: Multi-Strategy Discovery","text":"<p>FlexTensor uses three discovery strategies, tried in sequence. Each strategy succeeds for different model configurations, and the system falls back to the next if one doesn't find all untraced tensors.</p> <pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502                    Untraced Tensors                        \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                            \u2502\n                            \u25bc\n              \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n              \u2502  Strategy 1: Auto Trap      \u2502\n              \u2502  (Forward Patching)         \u2502\n              \u2502  Most accurate, uses        \u2502\n              \u2502  module.named_parameters()  \u2502\n              \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                            \u2502\n                      Found all? \u2500\u2500Yes\u2500\u2500\u25b6 Done\n                            \u2502\n                           No\n                            \u2502\n                            \u25bc\n              \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n              \u2502  Strategy 2: Module Tracker \u2502\n              \u2502  (Manual Traps)             \u2502\n              \u2502  Forward hooks record which \u2502\n              \u2502  modules execute per trap   \u2502\n              \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                            \u2502\n                      Found all? \u2500\u2500Yes\u2500\u2500\u25b6 Done\n                            \u2502\n                           No\n                            \u2502\n                            \u25bc\n              \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n              \u2502  Strategy 3: Prefix Match   \u2502\n              \u2502  (Fallback)                 \u2502\n              \u2502  Match tensor names by      \u2502\n              \u2502  patterns from traced ones  \u2502\n              \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                            \u2502\n                            \u25bc\n                          Done\n</code></pre>"},{"location":"explanation/tensor-discovery/#strategy-1-auto-trap-discovery","title":"Strategy 1: Auto Trap Discovery","text":""},{"location":"explanation/tensor-discovery/#when-it-works","title":"When It Works","text":"<p>This strategy applies when you use FlexTensor's <code>offload()</code> API with module patterns:</p> <pre><code>from flextensor import offload, OffloadConfig\n\nconfig = OffloadConfig(module_patterns=[\"layers.*\"])\nmodel = offload(model, config=config)\n</code></pre> <p>The <code>offload()</code> function patches each matched module's <code>forward</code> method and marks it with <code>_ft_original_forward_func</code> and <code>_ft_offload_name</code> attributes. This is called \"forward patching\" or \"auto trap.\"</p>"},{"location":"explanation/tensor-discovery/#how-it-works","title":"How It Works","text":"<p>For each patched module, FlexTensor:</p> <ol> <li>Identifies modules with <code>_ft_original_forward_func</code> attribute (marker for forward patching)</li> <li>Retrieves the offload name from <code>_ft_offload_name</code></li> <li>Calls <code>module.named_parameters()</code> to get all tensor IDs</li> <li>Includes inner tensor fields (like <code>weight.scale</code>) by inspecting custom attributes</li> </ol> <pre><code># Internal discovery logic (simplified)\nfor module in model.modules():\n    if hasattr(module, \"_ft_original_forward_func\"):\n        label = module._ft_offload_name\n        for param in module.parameters():\n            tensor_ids[label].add(id(param))\n            # Also add inner fields like weight.scale\n            for field in get_custom_tensor_fields(param):\n                tensor_ids[label].add(id(field))\n</code></pre>"},{"location":"explanation/tensor-discovery/#why-its-most-accurate","title":"Why It's Most Accurate","text":"<p>This strategy has a direct, structural relationship: each patched module knows exactly which tensors it owns. There's no guessing or pattern matching\u2014just iterating <code>named_parameters()</code>.</p>"},{"location":"explanation/tensor-discovery/#strategy-2-module-tracker-discovery","title":"Strategy 2: Module Tracker Discovery","text":""},{"location":"explanation/tensor-discovery/#when-it-works_1","title":"When It Works","text":"<p>This strategy applies when you use manual traps (context managers) instead of forward patching:</p> <pre><code>manager = get_offload_manager()\nmodel = manager.offload(model, config)\n\nfor batch in dataloader:\n    with manager.offload_block(\"layer_0\"):\n        x = model.layer_0(x)\n    with manager.offload_block(\"layer_1\"):\n        x = model.layer_1(x)\n</code></pre> <p>In this mode, FlexTensor doesn't know which module corresponds to which trap name\u2014the user defines that relationship through code structure.</p>"},{"location":"explanation/tensor-discovery/#how-it-works_1","title":"How It Works","text":"<p>The <code>ModuleTracker</code> class solves this by using forward hooks:</p> <ol> <li>Registration: At warmup start, forward hooks are registered on all modules with parameters</li> <li>Tracking: When a trap context is entered, the tracker records the current trap name</li> <li>Recording: Forward hooks fire as modules execute, recording which modules ran under which trap</li> <li>Discovery: After warmup, each trap's modules are known, and their parameters can be retrieved</li> </ol> <pre><code># How ModuleTracker builds the mapping (simplified)\nclass ModuleTracker:\n    def enter_trap(self, trap_name):\n        self._current_trap = trap_name\n\n    def _forward_hook(self, module, inputs, output):\n        if self._current_trap:\n            self._trap_to_modules[self._current_trap].add(module)\n\n    def get_trap_tensor_ids(self, tensors_map):\n        result = {}\n        for trap_name, modules in self._trap_to_modules.items():\n            tensor_ids = set()\n            for module in modules:\n                for param in module.parameters(recurse=False):\n                    if id(param) in tensors_map:\n                        tensor_ids.add(id(param))\n            result[trap_name] = tensor_ids\n        return result\n</code></pre>"},{"location":"explanation/tensor-discovery/#why-its-needed","title":"Why It's Needed","text":"<p>Manual traps offer flexibility\u2014you can group arbitrary code under a trap name. But this flexibility means FlexTensor can't statically determine which tensors belong where. The ModuleTracker bridges this gap by observing actual execution.</p>"},{"location":"explanation/tensor-discovery/#strategy-3-prefix-matching-discovery","title":"Strategy 3: Prefix Matching Discovery","text":""},{"location":"explanation/tensor-discovery/#when-it-works_2","title":"When It Works","text":"<p>This is the fallback strategy for edge cases where: - Forward patching isn't used - Module execution doesn't cleanly map to trap contexts - Tensors exist outside standard module hierarchies</p>"},{"location":"explanation/tensor-discovery/#how-it-works_2","title":"How It Works","text":"<p>The strategy infers tensor naming patterns from tensors that were traced:</p> <ol> <li>Collect traced tensor names: For each layer, gather names of traced tensors (e.g., <code>\"layers.0.attn.q.weight\"</code>, <code>\"layers.0.attn.k.weight\"</code>)</li> <li>Find common prefix: Identify the shared prefix up to a structural boundary (typically a numeric index like <code>\"layers.0\"</code>)</li> <li>Match untraced tensors: Any untraced tensor whose name starts with that prefix belongs to that layer</li> </ol> <pre><code># Example prefix inference\ntraced_names = [\"layers.0.attn.q.weight\", \"layers.0.attn.k.weight\", \"layers.0.ffn.w1.weight\"]\n# Inferred prefix: \"layers.0\"\n\n# Untraced tensor \"layers.0.attn.q.scale\" matches prefix \u2192 belongs to layer \"0\"\n</code></pre>"},{"location":"explanation/tensor-discovery/#why-its-a-fallback","title":"Why It's a Fallback","text":"<p>Prefix matching relies on naming conventions. It assumes: - Tensor names follow a hierarchical structure - Tensors in the same layer share a common prefix - The prefix boundary is at a numeric index (like <code>layers.0</code>)</p> <p>These assumptions hold for most transformer architectures but may fail for unconventional naming schemes or models with flat parameter hierarchies.</p>"},{"location":"explanation/tensor-discovery/#inner-tensor-fields","title":"Inner Tensor Fields","text":"<p>A cross-cutting concern across all strategies is handling \"inner tensor fields\"\u2014tensors attached as attributes to other tensors.</p>"},{"location":"explanation/tensor-discovery/#what-they-are","title":"What They Are","text":"<p>Some quantization formats store metadata as tensor attributes:</p> <pre><code>weight = torch.nn.Parameter(torch.randn(10, 10))\nweight.scale = torch.tensor(1.0)  # Inner tensor field\nweight.zero_point = torch.tensor(0)  # Another inner field\n</code></pre>"},{"location":"explanation/tensor-discovery/#how-theyre-discovered","title":"How They're Discovered","text":"<p>All three strategies call <code>get_inner_tensor_field_ids()</code> after finding a parameter:</p> <pre><code>def get_inner_tensor_field_ids(tensor):\n    \"\"\"Find tensor attributes that aren't part of base torch.Tensor.\"\"\"\n    inner_ids = []\n    custom_fields = set(dir(tensor)) - set(dir(torch.Tensor))\n    for field_name in custom_fields:\n        if not field_name.startswith(\"_\"):\n            field = getattr(tensor, field_name, None)\n            if isinstance(field, torch.Tensor):\n                inner_ids.append(id(field))\n    return inner_ids\n</code></pre> <p>This ensures that when a weight tensor is offloaded, its scale and other metadata tensors move together.</p>"},{"location":"explanation/tensor-discovery/#configuration","title":"Configuration","text":"<p>Tensor discovery is enabled by default but can be configured:</p> <pre><code>from flextensor import OffloadConfig\n\nconfig = OffloadConfig(\n    enable_untraced_tensor_discovery=True,  # Master switch for discovery\n    enable_module_tracker=True,             # Enable Strategy 2 (ModuleTracker)\n)\n</code></pre>"},{"location":"explanation/tensor-discovery/#when-to-disable","title":"When to Disable","text":"<p>You might disable tensor discovery if: - Your model doesn't use FP8 or custom kernels - You're debugging and want to isolate issues - You've verified all tensors are traced normally</p> <p>Disabling reduces overhead slightly but is rarely necessary.</p>"},{"location":"explanation/tensor-discovery/#integration-with-flextensor-states","title":"Integration with FlexTensor States","text":"<p>Tensor discovery happens at a specific point in the warmup \u2192 profile transition:</p> <pre><code>WARMUP STATE\n\u251c\u2500\u2500 WarmupTrap intercepts operations\n\u251c\u2500\u2500 Traced tensors recorded via __torch_function__\n\u251c\u2500\u2500 ModuleTracker records module \u2192 trap mapping (Strategy 2)\n\u2514\u2500\u2500 Warmup complete\n\nTRANSITION TO PROFILE\n\u251c\u2500\u2500 discover_untraced_tensors_for_layers() called\n\u2502   \u251c\u2500\u2500 Strategy 1: Check for forward-patched modules\n\u2502   \u251c\u2500\u2500 Strategy 2: Query ModuleTracker for module mappings\n\u2502   \u2514\u2500\u2500 Strategy 3: Prefix match remaining tensors\n\u251c\u2500\u2500 Layer statistics augmented with discovered tensors\n\u251c\u2500\u2500 ModuleTracker hooks removed (no longer needed)\n\u2514\u2500\u2500 Profile state begins with complete tensor mapping\n\nPROFILE STATE\n\u2514\u2500\u2500 All tensors (traced + discovered) properly loaded per layer\n</code></pre> <p>The discovery runs once, at the warmup-to-profile boundary. By the time inference runs, FlexTensor has a complete picture of which tensors belong to which layers.</p>"},{"location":"explanation/tensor-discovery/#debugging-untraced-tensors","title":"Debugging Untraced Tensors","text":"<p>If you suspect tensor discovery issues:</p>"},{"location":"explanation/tensor-discovery/#symptoms","title":"Symptoms","text":"<ul> <li>CUDA device mismatch errors during inference</li> <li>Missing tensors (tensor is on CPU when kernel expects GPU)</li> <li>Memory not being reclaimed (tensors stuck on GPU)</li> </ul>"},{"location":"explanation/tensor-discovery/#verification","title":"Verification","text":"<p>Check the tensor mapping after warmup:</p> <pre><code>manager = get_offload_manager()\nmodel = manager.offload(model, config)\n\n# Run warmup\nfor i in range(config.warmup_iters):\n    model(dummy_input)\n\n# Examine layer statistics\n# Note: _tensor_manager is an internal attribute intended for debugging only.\n# It is not part of the public API and may change without notice.\nlayer_stats = manager._tensor_manager.layer_statistics_collector.get_layer_stats()\nfor stat in layer_stats:\n    print(f\"Layer {stat.label}: {len(stat.tensor_ids)} tensors\")\n</code></pre> <p>If a layer has fewer tensors than expected, discovery may have missed some.</p>"},{"location":"explanation/tensor-discovery/#solutions","title":"Solutions","text":"<ol> <li>Use forward patching when possible\u2014it's the most reliable strategy</li> <li>Verify module structure ensures modules execute within the correct trap context</li> <li>Check tensor naming follows hierarchical conventions if using prefix matching</li> </ol>"},{"location":"explanation/tensor-discovery/#summary","title":"Summary","text":"Strategy Trigger Condition Accuracy Performance Auto Trap (Forward Patching) Model uses <code>offload()</code> API Highest Fast Module Tracker Manual traps + no forward patching High Moderate Prefix Matching Fallback when above don't cover all Variable Fast <p>Tensor discovery ensures FlexTensor handles modern model architectures\u2014including FP8 quantization and custom kernels\u2014without requiring users to manually specify tensor-to-layer mappings. The multi-strategy approach provides robustness across different usage patterns while maintaining accuracy for the most common cases.</p>"},{"location":"how-to/configure-for-common-scenarios/","title":"Configure for Common Scenarios","text":""},{"location":"how-to/configure-for-common-scenarios/#how-to-configure-flextensor-for-common-scenarios","title":"How to Configure FlexTensor for Common Scenarios","text":"<p>This guide shows you how to apply starting configurations for common deployment scenarios. It assumes you are already familiar with <code>OffloadConfig</code> and the basic <code>offload()</code> workflow. If you are new to FlexTensor, start with the Quick Start first.</p>"},{"location":"how-to/configure-for-common-scenarios/#prerequisites","title":"Prerequisites","text":"<ul> <li>FlexTensor installed</li> <li>A PyTorch model ready to offload</li> <li>Basic familiarity with <code>OffloadConfig</code> options (see Configuration for full option reference)</li> </ul>"},{"location":"how-to/configure-for-common-scenarios/#memory-constrained-systems","title":"Memory-Constrained Systems","text":"<p>Use this configuration when GPU memory is the primary constraint and you need to maximize memory savings, even at the cost of some throughput.</p> <pre><code>from flextensor import OffloadConfig, offload\n\nconfig = OffloadConfig(\n    release_tensors=True,       # Release GPU memory promptly\n    pinned_memory=True,         # Fast CPU-GPU transfers\n    profile_iters=15,           # Accurate profiling\n    max_gpu_mem_fraction=0.8,   # Use at most 80% of total GPU memory\n    min_blocks=2,               # Fewer blocks to save GPU memory\n)\n\nmodel = offload(model, config=config)\n</code></pre> <p>Key choices:</p> <ul> <li><code>release_tensors=True</code> frees GPU memory after each layer executes, reducing peak usage.</li> <li><code>pinned_memory=True</code> uses page-locked CPU memory to accelerate transfers via Direct Memory Access (DMA).</li> <li><code>max_gpu_mem_fraction=0.8</code> switches the strategy to memory mode, keeping peak GPU usage within 80% of total device memory. Using a fraction rather than a byte count makes the config portable across GPU SKUs.</li> <li><code>min_blocks=2</code> lets the optimizer try fewer memory blocks, reducing GPU memory consumed by FlexTensor itself.</li> </ul>"},{"location":"how-to/configure-for-common-scenarios/#performance-focused","title":"Performance-Focused","text":"<p>Use this configuration when latency matters more than memory savings and you want to minimize the overhead that offloading adds to the model forward pass.</p> <pre><code>from flextensor import OffloadConfig, offload\n\nconfig = OffloadConfig(\n    transfer_mode=\"allocation_block_transfer\",\n    num_blocks=8,               # More blocks for parallelism\n    enable_direct_mode=True,    # Lower overhead\n    rearrange_transfers=True,   # Optimize transfer scheduling\n)\n\nmodel = offload(model, config=config)\n</code></pre> <p>Key choices:</p> <ul> <li><code>num_blocks=8</code> increases the number of pre-allocated GPU memory blocks, allowing more transfers to overlap with computation.</li> <li><code>rearrange_transfers=True</code> lets FlexTensor reorder transfer scheduling to better hide latency.</li> </ul>"},{"location":"how-to/configure-for-common-scenarios/#quick-experimentation","title":"Quick Experimentation","text":"<p>Use this configuration during development when you need fast iteration and startup time, and profiling accuracy is less important.</p> <pre><code>from flextensor import OffloadConfig, offload\n\nconfig = OffloadConfig(\n    warmup_iters=1,\n    profile_iters=3,            # Minimal profiling for fast startup\n)\n\nmodel = offload(model, config=config)\n</code></pre> <p>Key choices:</p> <ul> <li><code>profile_iters=3</code> reduces the startup cost before reaching inference state.</li> <li>This produces a less accurate offloading strategy than higher iteration counts, so it is not recommended for production.</li> </ul>"},{"location":"how-to/configure-for-common-scenarios/#production-deployment","title":"Production Deployment","text":"<p>Use this configuration when deploying to a stable environment where startup time is less critical and you want accurate, reproducible offloading behavior.</p> <pre><code>from flextensor import OffloadConfig, offload\n\nconfig = OffloadConfig(\n    warmup_iters=1,\n    profile_iters=20,           # Accurate initial profiling\n)\n\nmodel = offload(model, config=config)\n</code></pre> <p>Key choices:</p> <ul> <li><code>profile_iters=20</code> gives the profiler more timing samples, which is especially important on shared or cloud GPUs where timing noise is higher.</li> </ul> <p>Use profile persistence for faster startup</p> <p>Save profiles after the initial warmup/profile run using <code>save_profile()</code>, then load them on subsequent runs with <code>load_profile()</code> to skip the warmup and profile phases entirely. Set <code>profile_storage_dir</code> in your config and ensure <code>profile_read_only=False</code> to enable saving.</p>"},{"location":"how-to/configure-for-common-scenarios/#next-steps","title":"Next Steps","text":"<ul> <li>Configuration reference \u2014 full description of every <code>OffloadConfig</code> option</li> <li>Troubleshooting \u2014 diagnose performance and memory issues</li> </ul>"},{"location":"how-to/troubleshooting/","title":"Troubleshooting","text":""},{"location":"how-to/troubleshooting/#how-to-troubleshoot-flextensor","title":"How to Troubleshoot FlexTensor","text":"<p>This guide provides practical solutions to common problems you may encounter when using FlexTensor. Each section focuses on a specific troubleshooting scenario with step-by-step instructions to diagnose and resolve issues.</p> <p>When to use this guide: You're experiencing unexpected behavior, errors, or need to inspect FlexTensor's internal operations for debugging.</p>"},{"location":"how-to/troubleshooting/#prerequisites","title":"Prerequisites","text":"<ul> <li>FlexTensor installed</li> <li>A model configured for offloading</li> <li>Basic familiarity with FlexTensor's configuration (see Quick Start)</li> </ul>"},{"location":"how-to/troubleshooting/#debug-component-initialization","title":"Debug Component Initialization","text":"<p>Problem: You need to understand what configuration values FlexTensor is using internally, verify that components are initialized correctly, or debug unexpected behavior during offloading setup.</p> <p>Solution: Use FlexTensor's instrumentation system to capture all initialization arguments and configuration values used by internal components.</p>"},{"location":"how-to/troubleshooting/#when-to-use-this-approach","title":"When to Use This Approach","text":"<p>Use instrumentation debugging when: - Component initialization fails with unclear error messages - You suspect configuration values aren't being applied correctly - You need to verify which strategy or loader is being used - You're reporting a bug and need detailed diagnostic information</p>"},{"location":"how-to/troubleshooting/#steps","title":"Steps","text":""},{"location":"how-to/troubleshooting/#1-enable-instrumentation-in-your-config","title":"1. Enable Instrumentation in Your Config","text":"<p>Set <code>enable_instrumentation=True</code> in your <code>OffloadConfig</code>:</p> <pre><code>from flextensor import OffloadConfig, offload\n\nconfig = OffloadConfig(\n    enable_instrumentation=True,\n    module_patterns=[\"layers.*\"],\n    # ... your other settings\n)\n\nmodel = offload(model, config=config)\n</code></pre> <p>Alternative: Use environment variables</p> <p>You can also enable instrumentation via environment variables without modifying code:</p> <pre><code>export FT_ENABLE_INSTRUMENTATION=1\nexport FT_INSTRUMENTATION_OUTPUT_DIR=/tmp/my_debug_output  # optional\n</code></pre> <p>Then load the config from environment:</p> <pre><code>from flextensor import load_config, offload\n\nconfig = load_config()  # reads FT_* environment variables\nmodel = offload(model, config=config)\n</code></pre>"},{"location":"how-to/troubleshooting/#2-run-your-model-through-warmup-and-profiling","title":"2. Run Your Model Through Warmup and Profiling","text":"<p>Execute your model for the configured number of warmup and profile iterations:</p> <pre><code>for _ in range(config.warmup_iters + config.profile_iters):\n    output = model(input_data)\n</code></pre> <p>When FlexTensor transitions to inference state, it automatically dumps the instrumentation data.</p>"},{"location":"how-to/troubleshooting/#3-locate-the-output-file","title":"3. Locate the Output File","text":"<p>The instrumentation data is written to the <code>instrumentation_output_dir</code> directory (default: <code>.flextensor/instrumentation</code>). The file structure is:</p> <pre><code>.flextensor/\n\u2514\u2500\u2500 instrumentation/\n    \u2514\u2500\u2500 20251209_143052/\n        \u2514\u2500\u2500\u2500\u2500 components.20251209_143052_pid12345.json\n</code></pre> <p>Each run creates a timestamped subdirectory with the JSON file inside.</p>"},{"location":"how-to/troubleshooting/#4-interpret-the-captured-data","title":"4. Interpret the Captured Data","text":"<p>The JSON file contains:</p> <pre><code>{\n  \"timestamp\": \"2025-12-09T14:30:52.123456\",\n  \"flextensor_version\": \"0.1.0\",\n  \"host_memory\": {\n    \"host_memory_total\": 270122237952,\n    \"host_memory_used\": 13204619264,\n    \"host_memory_available\": 256917618688,\n    \"swap_total\": 8589934592,\n    \"swap_used\": 107374182,\n    \"swap_free\": 8482560410\n  },\n  \"components\": [\n    {\n      \"class_name\": \"KnapsackStrategy\",\n      \"module_path\": \"flextensor.strategy.KnapsackStrategy\",\n      \"init_timestamp\": \"2025-12-09T14:30:50.100000\",\n      \"args\": {\n        \"scale\": 1.0,\n        \"cyclic\": false,\n        \"group_size\": 1,\n        \"threshold_mb\": 0.1\n      }\n    },\n    {\n      \"class_name\": \"TensorManager\",\n      \"module_path\": \"flextensor.tensor_manager.TensorManager\",\n      \"init_timestamp\": \"2025-12-09T14:30:50.200000\",\n      \"args\": {\n        \"device_gpu\": \"cuda:0\",\n        \"pinned_memory\": true,\n        \"loader_type\": \"allocation_block_transfer\",\n        \"release_tensors\": true,\n        \"direct_mode\": true\n      }\n    }\n  ],\n  \"memory_transfer_stats\": {\n    \"1024\": 0.015,\n    \"4096\": 0.023,\n    \"1048576\": 0.187\n  }\n}\n</code></pre> <p>Each component record includes: - <code>class_name</code>: The component class - <code>module_path</code>: Full module path - <code>init_timestamp</code>: When the component was initialized - <code>args</code>: All initialization arguments with their values</p> <p>The <code>host_memory</code> object captures a point-in-time snapshot of host physical memory (<code>host_memory_*</code>) and swap space (<code>swap_*</code>) at the time the dump is written. All values are in bytes. Divide by <code>1024 ** 3</code> to convert to GiB.</p> <p>The <code>memory_transfer_stats</code> object maps tensor sizes (bytes) to GPU\u2194CPU transfer times (ms), as measured during profiling. This key is present when the dump is written at inference transition (the automatic path); it may be absent in manual dumps.</p>"},{"location":"how-to/troubleshooting/#advanced-options","title":"Advanced Options","text":""},{"location":"how-to/troubleshooting/#manual-dump","title":"Manual Dump","text":"<p>To dump instrumentation data at any point (not just at inference transition):</p> <pre><code>from flextensor.instrumentation import dump_instrumentation\n\ndump_instrumentation(\"/path/to/dir\")\n</code></pre>"},{"location":"how-to/troubleshooting/#custom-output-directory","title":"Custom Output Directory","text":"<p>Change the output location via config:</p> <pre><code>config = OffloadConfig(\n    enable_instrumentation=True,\n    instrumentation_output_dir=\"/tmp/my_debug_output\",\n)\n</code></pre>"},{"location":"how-to/troubleshooting/#disable-instrumentation","title":"Disable Instrumentation","text":"<p>For production, ensure instrumentation is disabled (the default):</p> <pre><code>config = OffloadConfig(\n    enable_instrumentation=False,  # default\n)\n</code></pre> <p>Or simply omit the <code>enable_instrumentation</code> parameter.</p>"},{"location":"how-to/troubleshooting/#resolve-out-of-memory-errors","title":"Resolve Out-of-Memory Errors","text":"<p>Problem: CUDA out-of-memory errors during warmup, profiling, or inference.</p>"},{"location":"how-to/troubleshooting/#step-1-narrow-the-module-patterns","title":"Step 1: Narrow the module patterns","text":"<p>If <code>module_patterns=[\"*\"]</code> is offloading embedding or output layers that need to stay on GPU, narrow the scope. For example, when using the vLLM worker with a LLaMA-style model, these patterns target each transformer layer and special modules while leaving the rest unaffected:</p> <pre><code>config = OffloadConfig(\n    module_patterns=[\n        \"model.embed_tokens\",\n        \"model.layers.*\",\n        \"model.norm\",\n        \"lm_head\",\n    ],\n)\n</code></pre> <p>For models with a different structure, use <code>model.named_modules()</code> to inspect module names and adapt the patterns accordingly.</p>"},{"location":"how-to/troubleshooting/#step-2-reduce-the-number-of-transfer-blocks","title":"Step 2: Reduce the number of transfer blocks","text":"<p>Fewer pre-allocated GPU blocks reduces peak memory during block-based transfer:</p> <pre><code>config = OffloadConfig(\n    transfer_mode=\"allocation_block_transfer\",\n    num_blocks=2,   # Reduce from the default of 4\n)\n</code></pre> <p>If memory errors persist after these steps, check that no other processes are holding GPU memory:</p> <pre><code>nvidia-smi\n</code></pre>"},{"location":"how-to/troubleshooting/#fix-cuda-device-mismatch-errors","title":"Fix CUDA Device Mismatch Errors","text":"<p>Problem: A runtime error such as <code>RuntimeError: Expected all tensors to be on the same device</code> during inference.</p> <p>This typically occurs when FlexTensor offloads a tensor but a custom kernel (for example, a Triton or CUDA kernel) accesses a related metadata tensor that was not discovered during warmup.</p>"},{"location":"how-to/troubleshooting/#step-1-verify-tensor-discovery-is-enabled","title":"Step 1: Verify tensor discovery is enabled","text":"<pre><code>config = OffloadConfig(\n    enable_untraced_tensor_discovery=True,  # default\n    enable_module_tracker=True,             # default\n)\n</code></pre> <p>Both options are enabled by default. If you disabled them, re-enable them.</p>"},{"location":"how-to/troubleshooting/#step-2-use-the-offload-api-with-forward-patching","title":"Step 2: Use the <code>offload()</code> API with forward patching","text":"<p>Auto trap discovery (the most reliable strategy) only activates when you use <code>flextensor.offload()</code> with explicit <code>module_patterns</code>:</p> <pre><code>config = OffloadConfig(module_patterns=[\"layers.*\"])\nmodel = flextensor.offload(model, config=config)\n</code></pre> <p>Using manual <code>offload_block</code> context managers without forward patching relies on Module Tracker or Prefix Matching, which may miss tensors with unconventional naming.</p>"},{"location":"how-to/troubleshooting/#step-3-debug-with-instrumentation","title":"Step 3: Debug with instrumentation","text":"<p>Enable debug instrumentation to capture component initialization details and verify the tensor-to-layer mapping:</p> <pre><code>config = OffloadConfig(\n    enable_instrumentation=True,\n    module_patterns=[\"layers.*\"],\n)\nmodel = offload(model, config=config)\n</code></pre> <p>See Debug Component Initialization for how to interpret the output.</p> <p>For a detailed explanation of how tensor discovery works, see Tensor Discovery.</p>"},{"location":"how-to/troubleshooting/#diagnose-high-performance-overhead","title":"Diagnose High Performance Overhead","text":"<p>Problem: Model inference is slower than expected. FlexTensor targets less than 5% latency overhead over a baseline without offloading.</p>"},{"location":"how-to/troubleshooting/#step-1-check-that-inference-state-has-been-reached","title":"Step 1: Check that inference state has been reached","text":"<p>Warmup and profile iterations are intentionally slower\u2014overhead measurements are only meaningful after all warmup and profile iterations complete:</p> <pre><code>config = OffloadConfig(warmup_iters=1, profile_iters=10)\nmodel = flextensor.offload(model, config=config)\n\n# First warmup_iters + profile_iters iterations are measurement phases\nfor i, batch in enumerate(dataloader):\n    output = model(batch)\n    if i == config.warmup_iters + config.profile_iters:\n        print(\"Inference state reached \u2014 timing is now production overhead\")\n</code></pre>"},{"location":"how-to/troubleshooting/#step-2-increase-profile-iterations-for-noisy-environments","title":"Step 2: Increase profile iterations for noisy environments","text":"<p>On shared or cloud GPUs, thermal throttling and multi-tenancy can cause noisy timing data. Increase <code>profile_iters</code> for a more accurate strategy:</p> <pre><code>config = OffloadConfig(profile_iters=20)  # default: 10\n</code></pre>"},{"location":"how-to/troubleshooting/#step-3-tune-transfer-mode-and-block-count","title":"Step 3: Tune transfer mode and block count","text":"<p>The <code>allocation_block_transfer</code> mode with more blocks can reduce stalls by overlapping transfers with computation:</p> <pre><code>config = OffloadConfig(\n    transfer_mode=\"allocation_block_transfer\",\n    num_blocks=8,                  # More blocks = more parallelism\n    rearrange_transfers=True,      # Schedule transfers earlier\n    compute_transfer_gap=2,        # Initiate transfer 2 layers ahead\n)\n</code></pre>"},{"location":"how-to/troubleshooting/#fix-unexpected-behavior-after-modifying-a-patched-model","title":"Fix Unexpected Behavior After Modifying a Patched Model","text":"<p>Problem: You added, removed, or replaced a module in your model after calling <code>offload()</code>, and the new module is not being offloaded, or you observe incorrect behavior such as device mismatch errors or missing tensor coverage.</p>"},{"location":"how-to/troubleshooting/#why-this-happens","title":"Why This Happens","text":"<p>When you call <code>flextensor.offload(model, config=config)</code>, FlexTensor patches the <code>forward</code> methods of matched modules and builds a tensor-to-layer map during warmup. This map is fixed at the end of the warmup state. Any module added to the model after <code>offload()</code> is called is not part of that map and will not be offloaded.</p> <pre><code># BAD: new layer is not covered by offloading\nconfig = flextensor.OffloadConfig(module_patterns=[\"layers.*\"])\nmodel = flextensor.offload(model, config=config)\nmodel.layers.append(NewLayer())  # NewLayer will not be offloaded\n</code></pre>"},{"location":"how-to/troubleshooting/#solution-release-and-re-apply-offloading","title":"Solution: Release and Re-apply Offloading","text":"<p>Call <code>release()</code> on the offload manager to remove all patches and clear the tensor map, modify the model, then call <code>offload()</code> again:</p> <pre><code>import flextensor\n\nconfig = flextensor.OffloadConfig(module_patterns=[\"layers.*\"])\n\n# GOOD: patch after all modifications are done\nmodel.layers.append(NewLayer())\nmodel = flextensor.offload(model, config=config)\n</code></pre> <p>If you have already called <code>offload()</code> and need to modify the model afterward:</p> <pre><code>import flextensor\n\nconfig = flextensor.OffloadConfig(module_patterns=[\"layers.*\"])\nmodel = flextensor.offload(model, config=config)\n\n# Later: modify the model\nom = flextensor.get_offload_manager()\nom.release()                          # Remove all patches and tensor map\nmodel.layers.append(NewLayer())\nmodel = flextensor.offload(model, config=config)  # Re-apply\n</code></pre>"},{"location":"how-to/troubleshooting/#prevention","title":"Prevention","text":"<p>The safest approach is to finalize your model architecture before calling <code>offload()</code>. Apply all <code>append</code>, <code>insert</code>, <code>pop</code>, or attribute assignments to the model before patching it.</p>"},{"location":"how-to/troubleshooting/#fix-cross-thread-manager-access-errors","title":"Fix Cross-Thread Manager Access Errors","text":"<p>Problem: A <code>RuntimeError</code> with a message like <code>OffloadManager 'default' belongs to thread 12345, but accessed from thread 67890</code>, or silent data corruption when using FlexTensor from multiple threads.</p> <p>FlexTensor is not thread-safe</p> <p>The entire offloading lifecycle \u2014 setup, warmup, profiling, and inference \u2014 must run on a single thread per manager. FlexTensor's internal state (tensor maps, CUDA streams, memory blocks, profiling counters) is not protected against concurrent access. Using a patched model from multiple threads in parallel can cause silent data corruption, CUDA errors, or incorrect inference results even if no <code>RuntimeError</code> is raised.</p> <p>The <code>get_offload_manager()</code> thread-ownership guard catches the most common mistake (calling the API from the wrong thread), but it does not protect against running forward passes on a patched model concurrently.</p>"},{"location":"how-to/troubleshooting/#step-1-identify-the-conflicting-access","title":"Step 1: Identify the conflicting access","text":"<p>The error message includes both thread IDs. Check your code for places where <code>get_offload_manager()</code> (or the convenience functions <code>offload()</code>, <code>init()</code>, <code>set_config()</code>, etc.) is called from a background thread, callback, or worker that differs from the thread that originally created the manager. Also check for forward passes on the patched model from multiple threads.</p>"},{"location":"how-to/troubleshooting/#step-2-use-a-separate-name-model-and-thread-per-manager","title":"Step 2: Use a separate name, model, and thread per manager","text":"<p>If you need parallel offloading, each thread must have its own manager and its own model instance:</p> <pre><code>import threading\nimport flextensor\n\ndef worker(thread_name: str, model):\n    config = flextensor.OffloadConfig(module_patterns=[\"layers.*\"])\n    model = flextensor.offload(model, config=config, name=thread_name)\n    for batch in dataloader:\n        output = model(batch)  # Safe: single thread owns this manager + model\n\nt1 = threading.Thread(target=worker, args=(\"worker-1\", build_model()))\nt2 = threading.Thread(target=worker, args=(\"worker-2\", build_model()))\nt1.start()\nt2.start()\n</code></pre>"},{"location":"how-to/troubleshooting/#step-3-restructure-to-single-thread-access","title":"Step 3: Restructure to single-thread access","text":"<p>If threads must share a single model, perform all FlexTensor operations (offloading, warmup, profiling, and inference) on one thread and dispatch results to other threads afterward. Do not run forward passes on a patched model from multiple threads concurrently.</p>"},{"location":"how-to/troubleshooting/#collect-gpu-memory-snapshots-in-vllm","title":"Collect GPU Memory Snapshots in vLLM","text":"<p>Problem: You need a record of GPU memory state at each vLLM worker lifecycle point (after device initialization, model load, KV cache allocation, and warmup) to understand memory pressure, verify headroom, or compare baseline versus offloading deployments.</p> <p>Solution: Use one of the snapshot worker classes, which capture <code>MemorySnapshot</code> readings at each lifecycle stage and write them to a JSON file.</p>"},{"location":"how-to/troubleshooting/#choose-the-right-worker-class","title":"Choose the Right Worker Class","text":"Scenario Worker class Standard vLLM, no offloading <code>SnapshotWorker</code> vLLM with FlexTensor offloading <code>FlexTensorSnapshotWorker</code> <p>Both classes collect snapshots at the same lifecycle points: after <code>init_device</code>, <code>load_model</code>, <code>determine_available_memory</code>, <code>initialize_from_config</code> (KV cache), and <code>compile_or_warm_up_model</code>.</p>"},{"location":"how-to/troubleshooting/#steps_1","title":"Steps","text":""},{"location":"how-to/troubleshooting/#1-set-the-output-directory","title":"1. Set the output directory","text":"<p>Snapshots are only written to disk when <code>FT_VLLM_SNAPSHOT_OUTPUT_DIR</code> is set. If the variable is unset or empty, snapshots are collected in memory but discarded when the process exits.</p> <pre><code>export FT_VLLM_SNAPSHOT_OUTPUT_DIR=/tmp/gpu_snapshots\n</code></pre>"},{"location":"how-to/troubleshooting/#2-start-vllm-with-the-snapshot-worker","title":"2. Start vLLM with the snapshot worker","text":"<p>For a standard vLLM deployment (no FlexTensor offloading):</p> <pre><code>vllm serve &lt;model&gt; \\\n    --worker-cls flextensor.contrib.vllm.snapshot.SnapshotWorker\n</code></pre> <p>For a FlexTensor offloading deployment:</p> <pre><code>FT_ENABLED=1 vllm serve &lt;model&gt; \\\n    --worker-cls flextensor.contrib.vllm.snapshot.FlexTensorSnapshotWorker\n</code></pre>"},{"location":"how-to/troubleshooting/#3-inspect-the-output","title":"3. Inspect the output","text":"<p>After the final warmup step, each worker rank writes a JSON file to <code>FT_VLLM_SNAPSHOT_OUTPUT_DIR</code>:</p> <pre><code>/tmp/gpu_snapshots/\n\u2514\u2500\u2500 gpu_snapshots_rank0_device0_20260303_120000.json\n</code></pre> <p>The file contains metadata about the worker and a list of snapshot entries, one per lifecycle label:</p> <pre><code>{\n  \"worker_type\": \"FlexTensorSnapshotWorker\",\n  \"model\": \"meta-llama/Llama-3.1-8B\",\n  \"rank\": 0,\n  \"local_rank\": 0,\n  \"device\": \"cuda:0\",\n  \"snapshots\": [\n    {\n      \"label\": \"after_init_device\",\n      \"gpu_memory\": {\n        \"free_memory\": 79285829632,\n        \"cuda_memory\": 1073741824,\n        \"torch_memory\": 536870912,\n        \"non_torch_memory\": 536870912,\n        \"total_memory\": 85899345920,\n        \"torch_peak\": 536870912,\n        \"timestamp\": 1741003201.123456\n      },\n      \"host_memory\": {\n        \"host_memory_total\": 270122237952,\n        \"host_memory_used\": 13204619264,\n        \"host_memory_available\": 256917618688,\n        \"swap_total\": 8589934592,\n        \"swap_used\": 107374182,\n        \"swap_free\": 8482560410\n      }\n    }\n  ]\n}\n</code></pre> <p>All fields in <code>gpu_memory</code> and <code>host_memory</code> are in bytes. Divide by <code>1024 ** 3</code> to convert to GiB.</p> <p>Multi-rank deployments</p> <p>In tensor-parallel deployments, each rank writes its own file. The filename includes <code>rank</code> and <code>device</code> to distinguish them.</p>"}]}